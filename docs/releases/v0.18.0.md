# NEOS Work 0.18.0 — EngineSessions + EnginePlugins extracts

**Release:** **0.18.0**  
**Train:** **0.18.0** (M0 + M1 complete)  
**Audience:** developers  
**Date line:** monorepo `package.json` version **0.18.0**

Continues the **EngineClient modularization** train: sessions and plugins move
to dedicated clients. No product API breaks for callers that import from
`engine.js`.

| Doc | Purpose |
|---|---|
| [Plan v0.18](../plans/PLAN_FOR_V0_18_0.md) | M0 + M1 checklist (closed) |
| [Migration v0.18](../migration/v0.18.0.md) | Hierarchy + upgrade notes |
| [Impl M0](../implementation/v0.18/v0.18.0.md) | Sessions client |
| [Impl M1](../implementation/v0.18/v0.18.1.md) | Plugins client |

---

## Train map

| Version | Theme | What shipped |
|---|---|---|
| **0.18.0** | Sessions + plugins extract | `engine-sessions.ts` · `engine-plugins.ts` · hierarchy |

---

## Highlights for developers

### Desktop hierarchy (M0 + M1)

```text
EngineTransport → EngineProjectClient → EngineWorkflowClient
  → EngineSettingsClient → EngineMediaClient
  → EngineSessionsClient → EnginePluginsClient → EngineClient
```

Public imports stay:

```ts
import { EngineClient, SessionData, AgentChunk } from '../lib/engine.js';
```

```bash
pnpm --filter @neos-work/desktop exec vitest run src/lib/engine.test.ts
pnpm --filter @neos-work/desktop typecheck
```

### Inventory

```bash
pnpm inventory:check
pnpm test:inventory
```

Requires `catalogs.v18Features` (plan, migration, release, sessions + plugins
files, hierarchy, impl M0/M1).

### Still out of scope

- Run registry deepen (multi-node event SSE / durable log)  
- Sticky SSE implementation  
- Further EngineClient extracts (design systems / artifacts / memory / blocks)  
- CRDT multi-caret  

---

## Upgrade checklist

1. From **0.17.x**: no data migration.  
2. Desktop: no import renames if you use `engine.js`.  
3. `pnpm inventory:check`.

No automatic SQLite / workflow migration required from 0.17.x.
