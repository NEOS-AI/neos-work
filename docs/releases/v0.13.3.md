# NEOS Work 0.13.3 — v0.13 train closeout

**Release:** **0.13.3**  
**Train:** **0.13.0–0.13.3** (M0–M3 complete)  
**Audience:** operators and developers  
**Date line:** monorepo `package.json` version **0.13.3**

Closes the **contract gates for shared-edit** train. Product wire for agent
locks, run bind, and tools/files was already shipped in 0.10–0.11; this train
makes regressions fail **PR CI** via `pnpm e2e:contract`.

| Doc | Purpose |
|---|---|
| [Plan v0.13](../plans/PLAN_FOR_V0_13_0.md) | M0–M3 checklist (closed) |
| [Migration v0.13](../migration/v0.13.0.md) | Contract suite map + upgrade |
| [ADR shared-edit](../adr/0001-shared-edit-strategy.md) | Lock + LWW + agent enforce |

---

## Train map (0.13.0–0.13.3)

| Version | Theme | What shipped |
|---|---|---|
| **0.13.0** | Contract M0 | Agent 423, run bind, tools/files write |
| **0.13.1** | Snapshot / summary | Locks flags + collabSessionId |
| **0.13.2** | CI hygiene | Case map docs + shared Zod unit tests |
| **0.13.3** | Closeout | Migration + `v13Features` + this release |

Impl notes: [`v0.13.0`](../implementation/v0.13/v0.13.0.md) … [`v0.13.3`](../implementation/v0.13/v0.13.3.md).

---

## Highlights for developers

### PR CI contract

```bash
pnpm e2e:contract
```

Ten cases covering write hash, lock 409/423, collab snapshots, run bind, and
tool-file writes. See migration for the full table.

### Inventory

```bash
pnpm inventory:check
```

Requires `catalogs.v13Features` (plan, migration, release, contract cases,
shared parse tests, impl M0–M3).

### Still out of scope

- Sticky SSE implementation  
- Live multi-replica in PR CI (nightly remains)  
- CRDT multi-caret  

---

## Upgrade checklist

1. From **0.12.x**: no data migration.  
2. Confirm CI still runs `e2e:contract`.  
3. `pnpm inventory:check`.

No automatic SQLite / workflow migration required from 0.12.x.
