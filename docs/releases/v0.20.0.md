# NEOS Work 0.20.0 — EngineCatalog + project client split

**Release:** **0.20.0**  
**Train:** **0.20.0** (Track C + Track D complete)  
**Audience:** developers  
**Date line:** monorepo `package.json` version **0.20.0**

| Doc | Purpose |
|---|---|
| [Plan v0.20](../plans/PLAN_FOR_V0_20_0.md) | C + D checklist (closed) |
| [Migration v0.20](../migration/v0.20.0.md) | Hierarchy notes |
| [Impl C](../implementation/v0.20/v0.20.0.md) | EngineCatalogClient |
| [Impl D](../implementation/v0.20/v0.20.1.md) | Project client split |

---

## Highlights

### Track C — Thin EngineClient

```text
… → EngineOpsClient → EngineCatalogClient → EngineClient
```

`EngineClient` is now an empty extends shell; blocks/templates/memory live on
`EngineCatalogClient`.

### Track D — Project split

```text
EngineProjectCoreClient → Collab → Files → EngineProjectClient
```

`EngineWorkflowClient extends EngineProjectClient` unchanged.

### Inventory

```bash
pnpm inventory:check
pnpm test:inventory
```

### Still out of scope

- Durable run event log / Postgres  
- Sticky LB implementation  
- CRDT multi-caret  
- Web surface expansion  

---

## Upgrade checklist

1. From **0.19.x**: no data migration.  
2. Desktop: no import renames if using `engine.js`.  
3. `pnpm inventory:check`.
