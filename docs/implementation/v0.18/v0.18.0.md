# v0.18.0 Implementation — M0 EngineSessionsClient

## Summary

**M0 — Sessions / chat / agent / workspaces client extract.** Moves health,
sessions, messages, chat/agent SSE, tool confirm, workspaces, and CLI agents
off `EngineClient` into `EngineSessionsClient`.

Baseline: **0.17.0**  
Scope: desktop `apps/desktop/src/lib` (+ inventory/docs closeout with M1).

---

## Task map

| Item | Status |
|---|---|
| `engine-sessions.ts` (`EngineSessionsClient`) | **Done** |
| Types moved (`SessionData`, `MessageData`, `AgentStep`, `AgentTask`, `AgentChunk`) | **Done** |
| `EngineSessionsClient extends EngineMediaClient` | **Done** |
| Public re-exports from `engine.js` | **Done** |

---

## Hierarchy (after M0 + M1)

```text
EngineTransport
  → EngineProjectClient
    → EngineWorkflowClient
      → EngineSettingsClient
        → EngineMediaClient
          → EngineSessionsClient   // NEW (M0)
            → EnginePluginsClient  // M1
              → EngineClient
```

## Methods on `EngineSessionsClient`

**Health:** `health`, `checkConnection`

**Sessions:** `listSessions`, `createSession`, `deleteSession`

**Messages:** `listMessages`

**Chat / agent:** `chat`, `runAgent`, `cancelSession`, `confirmTool`

**Workspaces:** `listWorkspaces`, `createWorkspace`, `updateWorkspace`, `deleteWorkspace`

**CLI agents:** `listCliAgents`

## Verify

```bash
pnpm --filter @neos-work/desktop exec vitest run src/lib/engine.test.ts
pnpm --filter @neos-work/desktop typecheck
pnpm inventory:check
```

## Related

- M1: [`v0.18.1.md`](./v0.18.1.md)  
- Plan: [`PLAN_FOR_V0_18_0.md`](../../plans/PLAN_FOR_V0_18_0.md)  
