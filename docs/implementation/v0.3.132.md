# v0.3.132 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **control-char-before-trim** coverage expansion
and tests for NodeConfigPanel free-text config (design system select, slack
channel/templates, media prompt, deploy fields), BlockParamForm trimmed keys
and null-byte string params, HarnessSelector option hygiene, plus residual
server/core suites (blocks create null-byte fields, sessions non-string path,
agent-steps non-string sessionId, skill parser null-byte anywhere).

기준: v0.3.131 (+ fa303c2 selector harden)

---

## Changes

### Task 1: Desktop workflow UI (polish + tests)

- NodeConfigPanel: free-text gates already present for systemPrompt / query /
  harnessId / channel / textTemplate / media prompt & TTS text / deploy
  projectName & content; design-system option filter + trim
- NodeConfigPanel tests: design-system control options omitted; pad-ds select;
  slack/media/deploy null-byte & control-char rejection
- BlockParamForm: prefer trimmed param keys for value lookup/patch; select
  options trim; null-byte string params never applied
- BlockParamForm tests: null-byte string rejection; control-char select options
  and keys hidden; padded key read/write via trimmed key
- HarnessSelector tests: control-char harness ids omitted; padded value match

### Task 2: Server / packages residual coverage

- sessions: non-string workspace path coerce + control-before-trim
- agent-steps: non-string sessionId → required error
- blocks: create-path null-byte promptTemplate / inputDescription /
  outputDescription rejection
- artifacts: rerun meta message assertion
- skill parser: null-byte anywhere in file (frontmatter included) → null

### Task 3: Version

- monorepo **0.3.131 → 0.3.132**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/components/workflow/NodeConfigPanel.test.tsx` | 수정 |
| `apps/desktop/src/components/workflow/BlockParamForm.tsx` | 수정 |
| `apps/desktop/src/components/workflow/BlockParamForm.test.tsx` | 수정 |
| `apps/desktop/src/components/workflow/HarnessSelector.test.tsx` | 수정 |
| `apps/server/src/db/sessions.test.ts` | 수정 |
| `apps/server/src/db/agent-steps.test.ts` | 수정 |
| `apps/server/src/db/blocks.test.ts` | 수정 |
| `apps/server/src/routes/artifacts.test.ts` | 수정 |
| `packages/core/src/skills/parser.test.ts` | 수정 |
| monorepo versions | 0.3.132 |
