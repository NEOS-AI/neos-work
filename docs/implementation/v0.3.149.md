# v0.3.149 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **list-search haystack scrub alignment**,
**deploy URL gate**, **webhook secret sanitize**, **artifact content null
strip**, and **engine residual coverage**.

기준: v0.3.148

---

## Changes

### Task 1: List search haystack

- `scrubSearchHaystack`: strip nulls, collapse CR/LF, cap length
- `filterWorkflowList` / `filterBySearchText` / `filterByTextMatch` match on
  scrubbed fields (visible letters still searchable; aligns with display scrub)

### Task 2: Deployments URL safety

- `safeDeployUrl`: only http(s), reject control chars / overlong / invalid URLs
- Table links use gated href + scrubbed host/title labels

### Task 3: Webhook / Artifact / DesignSystemEditor / GenUI

- NodeConfigPanel: sanitize webhook secret at store time (null + CR/LF strip)
- ArtifactPreview: strip nulls from loaded content; scrub clipboard copy
- DesignSystemEditor: strip nulls when loading DESIGN.md content
- GenUIForm: scrub select option labels after control-char filter

### Task 4: Residual unit coverage

- browser-tool extract_text/links edge paths
- shell stdout truncation / missing cwd
- coding write catch, KIS period normalize, block skillId type drop
- agent harness prompt cap, deploy empty-body error / bad URL, gate edges

### Task 5: Version

- monorepo **0.3.148 → 0.3.149**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/lib/workflow-list-filter.ts` | 수정 |
| `apps/desktop/src/lib/workflow-list-filter.test.ts` | 수정 |
| `apps/desktop/src/pages/Deployments.tsx` | 수정 |
| `apps/desktop/src/pages/Deployments.test.tsx` | 수정 |
| `apps/desktop/src/components/workflow/NodeConfigPanel.tsx` | 수정 |
| `apps/desktop/src/components/workflow/NodeConfigPanel.test.tsx` | 수정 |
| `apps/desktop/src/components/workflow/ArtifactPreview.tsx` | 수정 |
| `apps/desktop/src/components/workflow/GenUIForm.tsx` | 수정 |
| `apps/desktop/src/pages/DesignSystemEditor.tsx` | 수정 |
| `apps/desktop/src/pages/DesignSystemEditor.test.tsx` | 수정 |
| packages residual tests | 수정 |
| monorepo versions | 0.3.149 |
