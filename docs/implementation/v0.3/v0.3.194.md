# v0.3.194 Implementation

## Summary

**PLAN_FOR_V0_4_0 Task 8** — Desktop UX for Domain Packs & Workers: pack-tab
palette, worker selector + solo/coordinator mode, templates v2 (+ coordinator
& research templates), engine `listWorkers` / `listDomainPacks`, migration toast,
and i18n strings.

기준: v0.3.193  
범위: Task 8 (desktop palette / config / templates / client).  
아직 미구현: Typed ports MVP (Task 9), 0.4.0 cut + migration guide (Task 10).

---

## Changes

### Task 1: Engine client

- `listWorkers(?domain)` → `GET /api/workers`
- `listDomainPacks()` → `GET /api/domain-packs`
- `listHarnesses()` prefers workers API, falls back to `/api/harness`

### Task 2: Palette by pack

- Pack tabs: All / Finance / Coding / Research / General / Control / Delivery
- Agent presets drop as `type: 'agent'` + `workerId` / `mode` (including Coordinator ◎)
- Gate labels: logic OR vs race OR clarified

### Task 3: Node config

- Worker selector (label **Worker**, loads via `listWorkers`)
- **Mode** select: solo | coordinator + coordinator hint
- Canvas coordinator badge (◎)

### Task 4: Templates v2

- All templates: `schemaVersion: 2`, `primaryDomain`, `type: 'agent'` + `workerId`
- New: **Coordinator Implement**, **Analyst → Risk → Slack**, **Web Research Pack**
- Domain filter includes `research`

### Task 5: Filters / i18n / toast

- `worker-filter.ts` re-exports harness-filter
- Domain filter prefs include **research**
- Migration toast once per workflow id (sessionStorage)
- en/ko `workflow.*` worker strings

### Task 6: Version

- monorepo **0.3.193 → 0.3.194**

---

## Verification

- Server templates: **12** passed
- Desktop NodeConfig / domain-filter / harness-filter: **53** passed

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/lib/engine.ts` | 수정 |
| `apps/desktop/src/lib/worker-filter.ts` | 신규 |
| `apps/desktop/src/lib/domain-filter-prefs.ts` | 수정 |
| `apps/desktop/src/pages/WorkflowEditor.tsx` | 수정 |
| `apps/desktop/src/components/workflow/NodeConfigPanel.tsx` | 수정 |
| `apps/desktop/src/components/workflow/HarnessSelector.tsx` | 수정 |
| `apps/desktop/src/pages/Templates.tsx` | 수정 |
| `apps/server/src/routes/templates.ts` | 수정 |
| `packages/ui/src/i18n/locales/{en,ko}/common.json` | 수정 |
| monorepo versions | 0.3.194 |

---

## Next (PLAN_FOR_V0_4_0)

- Task 9: Typed ports MVP (`PortDef`, strictPorts, editor warning)
- Task 10: migration guide + monorepo **0.4.0**
