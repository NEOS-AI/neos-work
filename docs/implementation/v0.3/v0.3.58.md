# v0.3.58 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **Deploy API network hygiene** (Task 8),
**Settings clear-secret via blank PUT** (Tasks 3/7–8), and **MCP OAuth refresh/
status/disconnect field trim + URL validation** (Task 9).

기준: v0.3.57

---

## Changes

### Also from v0.3.57 review

- Plugin LLM: whitespace Anthropic no longer blocks OpenAI fallback
- Artifacts: list query/id trim; whitespace content on create rejected
- Deploy helpers: network try/catch + token trim
- MCP OAuth refresh: http(s) validation + network catch
- Settings: blank sensitive PUT deletes key


### Task 1: Deploy network hygiene (Task 8)

- Wrap Vercel/Cloudflare `fetch` in try/catch for deploy + status polls
- Network errors rethrow as clean `Error` messages
- Trim API tokens before Authorization headers

### Task 2: Settings blank secret clears key (Tasks 3 / 7–8)

- **PUT /api/settings/:key** for sensitive keys: whitespace/empty value
  **deletes** the setting (`data.deleted: true`) so it is truly unset

### Task 3: MCP OAuth residual (Task 9)

- **status / refresh / disconnect**: trim `serverId`
- **refresh**: trim `tokenEndpoint` / `clientId`; require `http(s)`;
  catch network errors on token exchange

### Task 4: Unit tests

- Deploy network failure cases
- Settings blank sensitive PUT deletes
- MCP OAuth refresh validation cases

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/server/src/lib/deploy.ts` | 수정 |
| `apps/server/src/lib/deploy.test.ts` | 수정 |
| `apps/server/src/routes/settings.ts` | 수정 |
| `apps/server/src/routes/settings.test.ts` | 수정 |
| `apps/server/src/routes/mcp.ts` | 수정 |
| `apps/server/src/routes/mcp.test.ts` | 수정 |
| monorepo versions | 0.3.58 |
