# v0.3.150 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **RunLogPanel safe href gate**, **PipelineRunner
event id/surface sanitize**, **Workflows create API error alert**,
**NodeConfigPanel config field display hygiene**, and residual engine coverage.

기준: v0.3.149

---

## Changes

### Task 1: RunLogPanel linkify

- New `safeLogHref()` — http(s) only via `URL()`, reject control/whitespace,
  strip trailing punctuation, cap length at 2048
- Overlong / invalid URLs render as plain text (no `<a>`)

### Task 2: PipelineRunner SSE hygiene

- `safePipelineId` / `safePipelineSurface` for runId, stageId, surface
- Stage names scrubbed on start; stage outputs scrubbed on complete
- Waiting GenUI only opens for known surfaces (`form` / `choice` / `confirmation`)
- Resume path gates plugin/run/stage ids before API call

### Task 3: Workflows create

- Failed `createWorkflow` alerts a scrubbed API error (was silent)

### Task 4: NodeConfigPanel display

- Node label field shows scrubbed value
- Multi-line config fields use null-strip display helper
- Single-line config fields use collapse scrub helper

### Task 5: Unit tests

- `safeLogHref` + overlong linkify cases
- Pipeline control stageId / unknown surface ignored
- Workflows create control-char error alert

### Task 6: Residual package coverage

- browser-tool / memory / tool registry edges
- finance / block registry / harness / message-text / web-search residual cases

### Task 7: Version

- monorepo **0.3.149 → 0.3.150**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/components/workflow/RunLogPanel.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RunLogPanel.test.tsx` | 수정 |
| `apps/desktop/src/components/workflow/PipelineRunner.tsx` | 수정 |
| `apps/desktop/src/components/workflow/PipelineRunner.test.tsx` | 수정 |
| `apps/desktop/src/components/workflow/NodeConfigPanel.tsx` | 수정 |
| `apps/desktop/src/pages/Workflows.tsx` | 수정 |
| `apps/desktop/src/pages/Workflows.test.tsx` | 수정 |
| packages residual tests | 수정 |
| monorepo versions | 0.3.150 |
