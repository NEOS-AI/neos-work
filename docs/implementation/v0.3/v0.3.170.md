# v0.3.170 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **throw-path failure surfaces** (WorkflowEditor
toolbar Preflight, RunHistoryPanel clear-by-status), **Routines dual-catalog load
isolation**, **Settings control-char save alerts**, and **MCP client edge tests**.

기준: v0.3.169

---

## Changes

### Task 1: WorkflowEditor — toolbar Preflight

- Wrap `preflightWorkflow` call in try/catch
- Network/throw path → scrubbed alert (`Preflight failed` fallback)
- Existing non-ok / issues / OK paths unchanged

### Task 2: RunHistoryPanel — Clear completed / failed / cancelled

- Each clear-by-status handler wrapped in try/catch
- Throw path → scrubbed `Clear failed` alert; runs list preserved
- Existing non-ok scrubbed error path retained

### Task 3: Routines load isolation

- `listRoutines` and `listWorkflows` resolved independently (`Promise.all` of
  settled outcomes) so a workflows network throw cannot clear the routines list
- New `workflowsError` state for catalog failures
- Create modal: scrubbed red note under Workflow select; select disabled when
  catalog failed

### Task 4: Settings control-char save feedback

- `ApiKeyInput` save: control-char value → `window.alert('Key contains invalid control characters')` (no silent no-op)
- `SimpleKeyInput` save: control-char value → `window.alert('Value contains invalid control characters')`

### Task 5: MCP client edge tests

- Whitespace-only stdio command → requires a command
- Disconnect when close resolves cleanly
- Stdio args filter nullish/control-char entries
- listTools drops whitespace-only descriptions
- callTool missing text/data content items

### Task 6: Unit tests

- WorkflowEditor: toolbar preflight throw + non-Error throw fallback
- RunHistoryPanel: clear completed / failed / cancelled throw paths
- Routines: list survives workflows throw; create modal surfaces scrubbed errors
- Settings: control-char key/value save alerts
- MCP client edge cases above

### Task 7: Version

- monorepo **0.3.169 → 0.3.170**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/pages/WorkflowEditor.tsx` | 수정 |
| `apps/desktop/src/pages/WorkflowEditor.test.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RunHistoryPanel.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RunHistoryPanel.test.tsx` | 수정 |
| `apps/desktop/src/pages/Routines.tsx` | 수정 |
| `apps/desktop/src/pages/Routines.test.tsx` | 수정 |
| `apps/desktop/src/pages/Settings.tsx` | 수정 |
| `apps/desktop/src/pages/Settings.test.tsx` | 수정 |
| `packages/mcp-client/src/client.test.ts` | 수정 |
| monorepo versions | 0.3.170 |
