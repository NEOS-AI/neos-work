# v0.3.62 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **MCP OAuth callback security** (Task 9) —
HTML-escape user-controlled values, trim OAuth query params, network-safe token
exchange — plus **oauth/start invalid JSON → 400**, **plugin resume path/plugin
hygiene** (Task 5), and **deploy list limit trim**.

기준: v0.3.61

---

## Changes

### Task 1: MCP OAuth callback security (Task 9)

- Trim `code` / `state` / `error` query params (whitespace-only treated as missing)
- **`escapeHtml`** for OAuth error text and token-error body (XSS hygiene)
- Token exchange: dedicated network failure page (502); escape provider error body
- Reject missing/blank `access_token` in token response
- Trim stored access/refresh tokens and scope/type fields

### Task 2: OAuth start hygiene (Task 9)

- Invalid JSON body → **400** (was caught as 500 internal)

### Task 3: Plugin resume (Task 5)

- Trim plugin path `id`; verify plugin exists before `resumeRun`
- Invalid resume JSON → 400

### Task 4: Deploy list

- Trim `?limit=` before parse/clamp

### Task 5: Artifacts / workflow import JSON

- **Artifacts create**: explicit invalid JSON → 400
- **Workflow import**: distinguish invalid JSON (400) vs unsupported format

### Task 6: Unit tests

- `escapeHtml` special characters
- oauth/start invalid JSON
- oauth/callback HTML escape + blank query
- plugin resume invalid JSON / unknown plugin
- artifacts create invalid JSON
- workflow import invalid JSON

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/server/src/lib/errors.ts` | 수정 |
| `apps/server/src/lib/errors.test.ts` | 수정 |
| `apps/server/src/routes/mcp.ts` | 수정 |
| `apps/server/src/routes/mcp.test.ts` | 수정 |
| `apps/server/src/routes/plugins.ts` | 수정 |
| `apps/server/src/routes/plugins.test.ts` | 수정 |
| `apps/server/src/routes/deploy.ts` | 수정 |
| `apps/server/src/routes/artifacts.ts` | 수정 |
| `apps/server/src/routes/artifacts.test.ts` | 수정 |
| `apps/server/src/routes/workflow.ts` | 수정 |
| `apps/server/src/routes/workflow.test.ts` | 수정 |
| monorepo versions | 0.3.62 |
