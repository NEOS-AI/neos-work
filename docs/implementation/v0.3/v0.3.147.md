# v0.3.147 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **Sessions model/thinking chrome + search
haystack scrub**, **WorkflowEditor rename/schedule name seed scrub**, and
**filesystem / executor residual coverage**.

기준: v0.3.146

---

## Changes

### Task 1: Sessions

- Search filter scrubs title/model/provider haystack (null-bytes never poison
  matching)
- Chat composer model label scrubs provider · model (catalog or session API)
- Thinking mode badge uses scrubbed label; empty after scrub hides badge

### Task 2: WorkflowEditor

- Inline rename input seeded with scrubbed workflow name
- Schedule modal name seed scrubs workflow name before `"… schedule"` suffix

### Task 3: Residual unit coverage

- filesystem: control-char list/search/move inputs; non-string path coercion;
  glob match cap (≤200) when `fs.glob` available
- executor: control-char node type rejection; agent/message type resolution;
  legacy `gate_or` first-success input
- Routines: harden runId/crystallize alert assertions (exact scrubbed values)

### Task 4: Version

- monorepo **0.3.146 → 0.3.147**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/pages/Sessions.tsx` | 수정 |
| `apps/desktop/src/pages/Sessions.test.tsx` | 수정 |
| `apps/desktop/src/pages/WorkflowEditor.tsx` | 수정 |
| `apps/desktop/src/pages/WorkflowEditor.test.tsx` | 수정 |
| `apps/desktop/src/pages/Routines.test.tsx` | 수정 |
| `packages/core/src/tools/filesystem.test.ts` | 수정 |
| `packages/workflow-engine/src/executor.test.ts` | 수정 |
| monorepo versions | 0.3.147 |
