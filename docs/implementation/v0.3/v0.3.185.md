# v0.3.185 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: extend **`safeEntityId` gates** to primary
editor/session/load paths — WorkflowEditor load/save/run/export/preflight,
Sessions messages/chat/agent/confirmTool, DesignSystemEditor load/save, Skills
via shared helper, Blocks/Harnesses update, Settings OAuth status + connect,
Deployments poll, RevisionPanel list.

기준: v0.3.184

---

## Changes

### Task 1: WorkflowEditor

- Route load: invalid `id` → load error (no API)
- Rename / save / run / preflight / export JSON+ZIP gate `workflow.id`

### Task 2: Sessions

- `listMessages` / `chat` / `runAgent` gate `session.id`
- `confirmTool` gates session id + tool use id

### Task 3: DesignSystemEditor

- Load + save content gate route `id`

### Task 4: Skills / Blocks / Harnesses

- Skills toggle/delete/upgrade use `safeEntityId` (overlong covered)
- Blocks update gates edit target id
- Harnesses update gates existing id

### Task 5: Settings MCP + Deployments + Revisions

- OAuth status probe skips invalid server ids
- OAuth connect button gates before modal open
- Deployments auto-poll skips invalid deployment ids
- RevisionPanel list gates `workflowId`

### Task 6: Unit tests

- Settings dirty-id MCP OAuth/toggle/delete coverage updated
- **243 passed** across related suites

### Task 7: Version

- monorepo **0.3.184 → 0.3.185**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/pages/WorkflowEditor.tsx` | 수정 |
| `apps/desktop/src/pages/Sessions.tsx` | 수정 |
| `apps/desktop/src/pages/DesignSystemEditor.tsx` | 수정 |
| `apps/desktop/src/pages/Skills.tsx` | 수정 |
| `apps/desktop/src/pages/Blocks.tsx` | 수정 |
| `apps/desktop/src/pages/Harnesses.tsx` | 수정 |
| `apps/desktop/src/pages/Settings.tsx` | 수정 |
| `apps/desktop/src/pages/Settings.test.tsx` | 수정 |
| `apps/desktop/src/pages/Deployments.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RevisionPanel.tsx` | 수정 |
| monorepo versions | 0.3.185 |
