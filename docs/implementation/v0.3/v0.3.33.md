# v0.3.33 Implementation

## Summary

PLAN_FOR_V0_3_0 residual UX polish: **domain filter prefs** for Blocks / Templates /
Harnesses, **blocks source filter prefs**, and **Escape** on Sessions new-session,
MCP OAuth, Blocks, and Harnesses modals.

기준: v0.3.32

---

## Changes

### Task 1: Domain filter prefs (list UX)

- **`domain-filter-prefs.ts`**: `loadDomainFilter` / `saveDomainFilter`
  scopes: `blocks`, `templates`, `harnesses`
- localStorage keys: `neos-blocks-domain`, `neos-templates-domain`, `neos-harnesses-domain`
- Domain chips restore last selection on each page

### Task 2: Blocks source filter prefs (PLAN Task 12)

- **`loadBlocksSourceFilter` / `saveBlocksSourceFilter`** → `neos-blocks-source`
- Built-in / Custom / All chips restore on revisit

### Task 3: Modal Escape (UX)

- **Sessions** NewSessionModal: Escape closes
- **Settings** MCP OAuth connect modal: Escape closes (when not connecting)
- **Blocks** create/edit modal: Escape closes
- **Harnesses** create/edit modal: Escape closes

### Task 4: Unit tests

- `domain-filter-prefs.test.ts` — defaults, per-scope round-trip, source filter

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/lib/domain-filter-prefs.ts` | 신규 |
| `apps/desktop/src/lib/domain-filter-prefs.test.ts` | 신규 |
| `apps/desktop/src/pages/Blocks.tsx` | 수정 |
| `apps/desktop/src/pages/Templates.tsx` | 수정 |
| `apps/desktop/src/pages/Harnesses.tsx` | 수정 |
| `apps/desktop/src/pages/Sessions.tsx` | 수정 |
| `apps/desktop/src/pages/Settings.tsx` | 수정 |
| monorepo versions | 0.3.33 |
| `docs/implementation/v0.3.33.md` | 신규 |

---

## Version

- **0.3.32 → 0.3.33**

---

## Non-Goals

- Single-node-only artifact re-execution
- Video media / Marketplace
- Full OD §22 SSRF hardening
- Full OD Atom pipeline atoms
