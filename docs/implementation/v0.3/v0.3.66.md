# v0.3.66 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **finance block symbol/period hygiene**
(Task 12 finance domain), **MCP/agent-step/workspace-memory id trim**, and
**autoLayout dangling-edge safety** (Task 15).

기준: v0.3.65

---

## Changes

### Task 1: Finance native blocks (Task 12)

- `resolveSymbol`: trim params/inputs; whitespace-only → missing
- `clampPeriod`: integer clamp for MA/RSI/MACD/risk periods (safe bounds)
- MA type: only `EMA` | `SMA` (default SMA)
- Portfolio symbols: trim array entries; drop blanks
- KIS credentials: trim app key/secret before use

### Task 2: MCP / agent steps / workspace memory

- MCP `get`/`toggle`/`delete` helpers: trim id; blank → not found
- Agent steps: trim session/step ids; blank sessionId on create throws
- Workspace memory DB: trim workspaceId/key; blank rejected; list/search limit clamp

### Task 3: Auto layout (Task 15)

- Empty nodes → `[]`
- Direction normalize (`TB` default)
- Skip edges with blank/missing endpoints so dagre does not throw

### Task 4: Unit tests

- Finance whitespace symbol + trim
- Agent steps blank ids
- Workspace memory blank/trim keys
- autoLayout dangling edges

---

## File Index

| 파일 | 상태 |
|---|---|
| `packages/workflow-engine/src/blocks/finance/index.ts` | 수정 |
| `packages/workflow-engine/src/blocks/finance/finance.test.ts` | 수정 |
| `apps/server/src/routes/mcp.ts` | 수정 |
| `apps/server/src/db/agent-steps.ts` | 수정 |
| `apps/server/src/db/agent-steps.test.ts` | 수정 |
| `apps/server/src/db/memory.ts` | 수정 |
| `apps/server/src/db/memory.test.ts` | 수정 |
| `apps/desktop/src/lib/layout.ts` | 수정 |
| `apps/desktop/src/lib/layout.test.ts` | 수정 |
| monorepo versions | 0.3.66 |
