# v0.3.12 Implementation

## Summary

PLAN_FOR_V0_3_0 잔여 공백: 워크플로우 **preflight** (구조+시크릿 준비), Agent **model**
주입, Run **export JSON / clear completed**, Workflows **search·domain filter**,
병렬 브랜치 **template**.

기준: v0.3.11

---

## Changes

### Task 1: Workflow preflight (plan polish / Tasks 7–8, 13)

- **`assessWorkflowPreflight`** — trigger/output, dangling edges, secret readiness
  (Tavily, Slack, Discord, OpenAI media, deploy tokens, Anthropic/Google agents;
  CLI providers skipped)
- **`POST /api/workflow/:id/preflight`**
- Editor toolbar **✓ Preflight**
- **`EngineClient.preflightWorkflow`**

### Task 2: Agent model selection (multi-LLM)

- **AgentNode** passes `nodeConfig.model` or `settings.model` (defaults.model) to
  `AgentOrchestrator`

### Task 3: Run history export & clear

- **`DELETE /api/workflow/:id/runs?status=`** + `deleteRuns()`
- RunHistory: **Clear completed**, per-run **export JSON**

### Task 4: Workflows list UX

- Search by name/description
- Domain filter chips (all / finance / coding / general)

### Task 5: Parallel template (PLAN Task 11)

- Template **Parallel Research Branches** (parallel_start → 2× web_search → parallel_end)

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/server/src/lib/workflow-preflight.ts` | 신규 |
| `apps/server/src/lib/workflow-preflight.test.ts` | 신규 |
| `apps/server/src/routes/workflow.ts` | 수정 |
| `apps/server/src/db/workflows.ts` | 수정 |
| `apps/server/src/db/workflows-duplicate.test.ts` | 수정 |
| `apps/server/src/routes/templates.ts` | 수정 |
| `packages/workflow-engine/src/nodes/agent.ts` | 수정 |
| `apps/desktop/src/lib/engine.ts` | 수정 |
| `apps/desktop/src/pages/WorkflowEditor.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RunHistoryPanel.tsx` | 수정 |
| `apps/desktop/src/pages/Workflows.tsx` | 수정 |
| monorepo package.json versions | 0.3.12 |

---

## Version

- **0.3.11 → 0.3.12**

---

## Non-Goals

- Single-node-only artifact re-execution
- Video media / Marketplace
- Full OD §22 SSRF hardening
- Full OD Atom pipeline atoms
