# v0.3.134 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **SSE parse control-char hygiene** on EngineClient,
**run history** status/error display scrub + filter normalize, **artifact** HTML/
markdown routing control-char gates, and **escapeHtml** null-byte strip.

기준: v0.3.133

---

## Changes

### Task 1: Engine SSE (desktop client)

- `parseSseDataPayload`: reject null-byte payloads; trim CR; support `data:` /
  `data: ` prefixes
- `parseSseEventName`: control-char event names → empty
- streamChat / runAgent / runWorkflow / runPlugin use shared parsers
- plugin SSE: control-char runId ignored when capturing pipeline.started

### Task 2: Run history

- `normalizeRunStatus` / control-char filter → no-op (list all)
- loadRunStatusFilter ignores control-char storage
- filterRunsByStatus compares normalized (trimmed lower) statuses
- RunHistoryPanel: scrub status / run id / firstError for list display

### Task 3: Artifacts / server errors

- isHtmlContent: control contentType ignored; null-byte body never HTML
- isMarkdownContent: control contentType/name rejected
- downloadTextFile: scrub filename / mime / null-byte body
- escapeHtml: strip null bytes before entity escaping

### Task 4: Unit tests

- parseSseDataPayload / event name; runWorkflow skips null-byte lines
- run-history-filter control filter + padded status match
- ArtifactPreview control routing
- RunHistoryPanel error scrub
- escapeHtml null-byte

### Task 5: Version

- monorepo **0.3.133 → 0.3.134**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/lib/engine.ts` | 수정 |
| `apps/desktop/src/lib/engine.test.ts` | 수정 |
| `apps/desktop/src/lib/run-history-filter.ts` | 수정 |
| `apps/desktop/src/lib/run-history-filter.test.ts` | 수정 |
| `apps/desktop/src/components/workflow/RunHistoryPanel.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RunHistoryPanel.test.tsx` | 수정 |
| `apps/desktop/src/components/workflow/ArtifactPreview.tsx` | 수정 |
| `apps/desktop/src/components/workflow/ArtifactPreview.test.tsx` | 수정 |
| `apps/server/src/lib/errors.ts` | 수정 |
| `apps/server/src/lib/errors.test.ts` | 수정 |
| monorepo versions | 0.3.134 |
