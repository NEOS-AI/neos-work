# v0.3.168 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **clipboard fail feedback consistency**,
**Settings engine health error surface**, **MCP OAuth status probe isolation**,
and **ArtifactPreview download failure feedback**.

기준: v0.3.167

---

## Changes

### Task 1: ArtifactPreview

- Copy button shows `Copy failed` (red) on reject / missing API
- Status toast still set to `Copy failed`
- Guard `navigator.clipboard.writeText` presence before write
- Download: try/catch → `Download failed` status toast (blob URL throw path)

### Task 2: RunDetailPanel / NodeConfigPanel

- Guard missing clipboard API (same pattern as RunLogPanel / Sessions)
- NodeConfig webhook copyText uses writeText guard

### Task 3: RunLogPanel

- Scrub payload (`scrubDisplayText`, 100k cap) before clipboard write

### Task 4: Settings — EngineStatusSection

- Health throw / non-`ok` status → scrubbed red banner (`Health check failed`)
- Version / uptime cleared on failure (show `—`)

### Task 5: Settings — MCP OAuth status

- Per-server `getMcpOAuthStatus` wrapped in try/catch so a status probe throw
  never wipes the server list or surfaces a false MCP load error
- Clear `oauthStatuses` when list load fails

### Task 6: Unit tests

- ArtifactPreview copy success (scrub null) + fail feedback + download fail
- RunLog scrub-before-write
- RunDetail missing clipboard → Copy failed
- Settings: health throw scrub, non-ok health, MCP list survives OAuth throw
- **99 passed** across related suites

### Task 7: Version

- monorepo **0.3.167 → 0.3.168**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/components/workflow/ArtifactPreview.tsx` | 수정 |
| `apps/desktop/src/components/workflow/ArtifactPreview.test.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RunDetailPanel.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RunDetailPanel.test.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RunLogPanel.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RunLogPanel.test.tsx` | 수정 |
| `apps/desktop/src/components/workflow/NodeConfigPanel.tsx` | 수정 |
| `apps/desktop/src/pages/Settings.tsx` | 수정 |
| `apps/desktop/src/pages/Settings.test.tsx` | 수정 |
| monorepo versions | 0.3.168 |
