# v0.3.71 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **workflow create name/domain hygiene**,
**AgentNode provider normalize** (Task 3/14), **deployment update/create trim**
(Task 8), **executor blank-edge input safety** (Task 11), **routine scheduler
id/schedule trim** (Task 2), and **agent-step status/error trim**.

기준: v0.3.70

---

## Changes

### Task 1: Workflow create (Tasks 10/16)

- `createWorkflow`: reject blank/whitespace name
- Domain allow-list `finance|coding|general` (lower-case)
- Coerce non-array nodes/edges to `[]`
- Description trim when provided

### Task 2: AgentNode provider hygiene (Tasks 3/14)

- CLI + LLM provider strings trimmed + lower-cased
  (` CLI-Claude ` → `cli-claude`, ` OpenAI ` → `openai`)
- `buildAdapter` trims `llmProvider` from settings

### Task 3: Deployments DB (Task 8)

- Create: provider lower-case/trim; blank provider rejected; status trim
- Update: trim url/deploymentId/status/statusMessage; blank status keeps prior

### Task 4: Executor edges (Task 11)

- Skip blank source/target edges when collecting node inputs
- Same filter for parallel branch input wiring

### Task 5: Routine scheduler (Task 2)

- Trim routineId on schedule/run/remove; blank id no-op
- Trim cron expression before validate/register
- Timezone still defaults blank → UTC

### Task 6: Agent steps + design system

- `updateAgentStep`: blank status rejected; trim error text
- `updateDesignSystemContent`: coerce content to string

### Task 7: Unit tests

- createWorkflow blank name + domain normalize
- deployment update trim + blank provider
- agent padded CLI provider
- executor blank edges
- scheduler blank id + schedule trim
- agent-step blank status / error trim

### Also from v0.3.71 review

- **Executor**: AND/OR/skip use trimmed edge sources for `failedNodes` checks
  (padded source endpoints no longer miss upstream failures)
- **Deployments create**: provider allow-list only `vercel|cloudflare` after lower-case

### Task 8: Unit tests (review)

- Executor padded edge failure propagation to AND gate
- Deploy create rejects unknown provider (`aws`)

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/server/src/db/workflows.ts` | 수정 |
| `apps/server/src/db/workflows-runs.test.ts` | 수정 |
| `apps/server/src/db/deployments.ts` | 수정 |
| `apps/server/src/db/deployments.test.ts` | 수정 |
| `apps/server/src/db/agent-steps.ts` | 수정 |
| `apps/server/src/db/agent-steps.test.ts` | 수정 |
| `apps/server/src/lib/routine-scheduler.ts` | 수정 |
| `apps/server/src/lib/routine-scheduler.test.ts` | 수정 |
| `apps/server/src/lib/design-system-store.ts` | 수정 |
| `packages/workflow-engine/src/nodes/agent.ts` | 수정 |
| `packages/workflow-engine/src/nodes/agent.test.ts` | 수정 |
| `packages/workflow-engine/src/executor.ts` | 수정 |
| `packages/workflow-engine/src/executor.test.ts` | 수정 |
| monorepo versions | 0.3.71 |
