# v0.3.188 Implementation

## Summary

Residual polish on **v0.3.187 / PLAN_FOR_V0_4_0 Task 1–2** foundation: wire
desktop UI for the unified `agent` node and `workerId` config keys so migrated
graphs and the new palette entry are editable; expand Run Log filter coverage
and EngineClient id-gate tests.

기준: v0.3.187

---

## Changes

### Task 1: Unified `agent` node config UI

- `NodeConfigPanel`: treat `agent` like legacy `agent_*` for provider / model /
  harness / system prompt fields (palette Agent entry was otherwise blank)
- Prefer **`workerId`** for HarnessSelector value; write `workerId` and clear
  legacy `harnessId` on change (align with migrate + AgentNode resolve)

### Task 2: Harness domain filter for v2 agent

- `allowedDomainsForAgentNode('agent')` → finance / coding / research / general
- Explicit `agent_coding` branch (unknown types still default coding+general)

### Task 3: Tests

- RunLogPanel: `worker.*` + `node.warning` filter chip coverage
- harness-filter: unified agent pack set + filter list
- NodeConfigPanel: workerId selection / write path
- EngineClient: broader pathSegment fail-closed matrix for remaining entity APIs

### Task 4: Version

- monorepo **0.3.187 → 0.3.188**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/components/workflow/NodeConfigPanel.tsx` | 수정 |
| `apps/desktop/src/components/workflow/NodeConfigPanel.test.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RunLogPanel.test.tsx` | 수정 |
| `apps/desktop/src/lib/harness-filter.ts` | 수정 |
| `apps/desktop/src/lib/harness-filter.test.ts` | 수정 |
| `apps/desktop/src/lib/engine.test.ts` | 수정 |
| monorepo versions | 0.3.188 |
