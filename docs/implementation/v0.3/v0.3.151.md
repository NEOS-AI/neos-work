# v0.3.151 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **core `scrubErrorMessage` for tool/agent
errors**, **ToolRegistry execute error scrub**, **AgentOrchestrator step/final
error scrub**, and **Workflows/Templates create-import-duplicate failure alerts**.

기준: v0.3.150

---

## Changes

### Task 1: Core error scrub helper

- New `scrubErrorMessage(raw, maxChars?)` in `packages/core/src/tools/base.ts`
  — strip null, collapse CR/LF, length-cap (default 2000)
- Exported from `@neos-work/core` tools surface

### Task 2: ToolRegistry

- Scrub `result.error` on successful tool returns that include error strings
- Scrub thrown Error messages in catch path

### Task 3: AgentOrchestrator

- Step catch / final step_error / top-level agent failure paths scrub control
  chars (cap 4k for top-level)

### Task 4: Workflows page

- Duplicate failure → scrubbed alert (was silent)
- JSON import: reject null-byte payload; scrub API / parse errors to alert

### Task 5: Templates page

- Template "Use" create failure → scrubbed alert (was silent)

### Task 6: Unit tests

- Registry control-char throw/return errors
- Workflows duplicate + JSON import failure alerts
- Templates create failure scrubbed alert

### Task 7: Version

- monorepo **0.3.150 → 0.3.151**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `packages/core/src/tools/base.ts` | 수정 |
| `packages/core/src/tools/index.ts` | 수정 |
| `packages/core/src/tools/registry.ts` | 수정 |
| `packages/core/src/tools/registry.test.ts` | 수정 |
| `packages/core/src/agent/orchestrator.ts` | 수정 |
| `apps/desktop/src/pages/Workflows.tsx` | 수정 |
| `apps/desktop/src/pages/Workflows.test.tsx` | 수정 |
| `apps/desktop/src/pages/Templates.tsx` | 수정 |
| `apps/desktop/src/pages/Templates.test.tsx` | 수정 |
| monorepo versions | 0.3.151 |
