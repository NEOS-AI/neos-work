# v0.3.195 Implementation

## Summary

**PLAN_FOR_V0_4_0 Task 9** — Typed ports MVP (Q5 locked): `PortDef` helpers,
runtime `node.warning` / optional `strictPorts` hard-fail, editor edge mismatch
warnings, and canvas yellow port badge.

기준: v0.3.194  
범위: Task 9 (typed ports MVP).  
아직 미구현: Task 10 (migration guide + monorepo **0.4.0** cut).

---

## Changes

### Task 1: Port helpers (`packages/workflow-engine/src/ports.ts`)

- `parsePortDefs` / `portsFromOutputSchema`
- `resolveNodeInputPorts` / `resolveNodeOutputPorts` (config override, worker
  `outputSchema`, block `paramDefs`)
- `checkEdgePortMismatch` / `typesCompatible`
- `validateNodePorts` (required inputs, output type / required fields)
- `isStrictPortsEnabled` (`strictPorts` / `STRICT_PORTS` = 1|true|yes|on)

### Task 2: Executor

- Pre-execute: required input ports → `node.warning`; **strict** → `node.failed`
- Post-execute: output schema checks same pattern
- Uses `resolveWorker` / `resolveBlock` for inferred ports

### Task 3: Desktop editor

- `WorkflowValidation`: `port_type_mismatch` warning on edges
- Canvas: yellow border + ⚠ when node has `port_*` issues

### Task 4: Tests & version

- ports unit tests + executor port cases
- WorkflowValidation port mismatch test
- monorepo **0.3.194 → 0.3.195**

### Residual polish

- Agent free-form **string** output vs object port schema is not a type error
  (JSON-as-text is coerced for required-field checks)
- Strict post-execute port fail **deletes** node output so downstream skips
- Coordinator mode inferred when `workerId === general_coordinator` (config + badge)
- Worker select auto-sets mode to coordinator for built-in coordinator worker

---

## Verification

- `@neos-work/workflow-engine`: **475** tests passed
- Desktop WorkflowValidation + engine + worker-filter: **131** passed

---

## File Index

| 파일 | 상태 |
|---|---|
| `packages/workflow-engine/src/ports.ts` | 신규 |
| `packages/workflow-engine/src/ports.test.ts` | 신규 |
| `packages/workflow-engine/src/executor.ts` | 수정 |
| `packages/workflow-engine/src/executor.test.ts` | 수정 |
| `packages/workflow-engine/src/index.ts` | 수정 |
| `apps/desktop/src/components/workflow/WorkflowValidation.ts` | 수정 |
| `apps/desktop/src/components/workflow/WorkflowValidation.test.ts` | 수정 |
| `apps/desktop/src/pages/WorkflowEditor.tsx` | 수정 |
| monorepo versions | 0.3.195 |

---

## Next (PLAN_FOR_V0_4_0)

- Task 10: `docs/migration/v0.4.0.md`, README breaking notes, version **0.4.0**,
  health/banner/UA sync, full typecheck/test/build
