# v0.3.136 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish on top of v0.3.135 harden: **Deployments test
client stability**, **Media / Memory list display scrub**, and monorepo version
bump. (Prefs control-char parse helpers already landed in `daaa156` under
0.3.135.)

기준: v0.3.135 (+ daaa156 prefs harden)

---

## Changes

### Task 1: Deployments test stability

- Stable `engineClient` mock identity so `useCallback([client])` does not
  re-fire `load()` every render

### Task 2: Media / Memory / Deployments display scrub

- Media: scrub filename / kind in list and preview; safe download attribute;
  search haystack uses scrubbed fields
- Memory: scrub type / name / content preview; type color falls back for
  unknown/control types
- Deployments: scrub workflow names in filter dropdown options; control-char
  status/message/provider/project coverage test

### Task 3: Version

- monorepo **0.3.135 → 0.3.136**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/pages/Deployments.tsx` | 수정 |
| `apps/desktop/src/pages/Deployments.test.tsx` | 수정 |
| `apps/desktop/src/pages/Media.tsx` | 수정 |
| `apps/desktop/src/pages/Memory.tsx` | 수정 |
| `apps/desktop/src/pages/Plugins.test.tsx` | 수정 |
| monorepo versions | 0.3.136 |
