# v0.3.39 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **editor right-panel tab prefs** only save
user choices (review fix), **Discord/media validation** aligned with NodeConfig
fields, **multiple_outputs** graph warning, and Design System / Media Escape
hygiene from the post-0.3.38 review.

기준: v0.3.38

---

## Changes

### Task 1: Editor tab prefs (review fix, PLAN Tasks 4 / 14 / 16)

- `selectRightPanelTab` — user tab bar / validation badge → **persists**
- `showRightPanelTab` — run / preflight / node click / preview re-run →
  **ephemeral** (does not overwrite `neos-editor-right-panel-tab`)

### Task 2: Graph validation

- **Discord**: treat `textTemplate` (NodeConfig field) as valid static content
- **Media audio**: require `text` (not image `prompt`) when `mediaType === 'audio'`
- **`multiple_outputs`**: warn when more than one output node is present

### Task 3: Escape / leave hygiene (review)

- Design System editor Escape only when loaded; respects `defaultPrevented`
- Media delete clears selection without double-managing blob URLs

### Task 4: Unit tests

- Discord textTemplate / media audio / multiple_outputs cases
- NodeConfigPanel CLI act-warning quieting

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/pages/WorkflowEditor.tsx` | 수정 |
| `apps/desktop/src/pages/DesignSystemEditor.tsx` | 수정 |
| `apps/desktop/src/pages/Media.tsx` | 수정 |
| `apps/desktop/src/pages/Skills.tsx` | 수정 |
| `apps/desktop/src/components/workflow/WorkflowValidation.ts` | 수정 |
| `apps/desktop/src/components/workflow/WorkflowValidation.test.ts` | 수정 |
| `apps/desktop/src/components/workflow/NodeConfigPanel.test.tsx` | 수정 |
| `docs/implementation/v0.3.38.md` | 수정 (tab persist note) |
| monorepo versions | 0.3.39 |
