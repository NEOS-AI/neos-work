# v0.3.165 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **Media status load error + field scrub**,
**PipelineRunner start failure when stream yields no runId**, and
**RunDetailPanel clipboard copy failure feedback**.

기준: v0.3.164

---

## Changes

### Task 1: Settings MediaStatusSection

- `getMediaConfig` failure → scrubbed red load error banner
- Scrub base URL / surfaces / model labels before display (control-char hygiene)

### Task 2: PipelineRunner

- When `runIdPromise` resolves without a usable runId and no stage events /
  prior failure → set `failed: 'Failed to start pipeline'` (HTTP/stream death)

### Task 3: RunDetailPanel

- Clipboard write failure → button shows `Copy failed` briefly

### Task 4: Unit tests

- Media config load fail + base URL scrub
- Pipeline start failure (null runId)
- RunDetail copy failure
- **63 passed** across related suites

### Task 5: Version

- monorepo **0.3.164 → 0.3.165**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/pages/Settings.tsx` | 수정 |
| `apps/desktop/src/pages/Settings.test.tsx` | 수정 |
| `apps/desktop/src/components/workflow/PipelineRunner.tsx` | 수정 |
| `apps/desktop/src/components/workflow/PipelineRunner.test.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RunDetailPanel.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RunDetailPanel.test.tsx` | 수정 |
| monorepo versions | 0.3.165 |
