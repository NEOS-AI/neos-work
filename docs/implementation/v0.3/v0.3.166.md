# v0.3.166 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **WorkflowEditor blocks-load validation badge**,
**Settings defaults control-char hygiene**, **RunLogPanel copy feedback**,
**PipelineRunner invalid-plugin-id fail-closed**, plus **TradingView MCP preset /
CDP probe** (client, server routes, Settings UI) and **finance chart-analyst harness**.

기준: v0.3.165

---

## Changes

### Task 1: WorkflowEditor

- `listBlocks` failure → scrubbed amber toolbar badge (blocks used for draft
  validation; silent empty list was masking incomplete validation context)

### Task 2: Settings defaults

- `getSettings` loads `defaults.provider` / `defaults.model` only when free of
  control chars; catch keeps built-in defaults

### Task 3: RunLogPanel

- Progress / output Copy buttons: success → `Copied`; clipboard reject /
  missing clipboard API → `Copy failed` (timed clear)

### Task 4: PipelineRunner

- Validate plugin id **before** `setRun` so control-char / empty ids never
  leave a stuck empty run shell

### Task 5: MCP client — TradingView preset

- `presets.ts`: built-in TradingView MCP preset, install path sanitize,
  stdio args template
- `tradingview-cdp.ts`: localhost CDP health probe with scrubbed fields
- Barrel exports from `@neos-work/mcp-client`
- EngineClient: `listMcpPresets`, `createMcpServerFromPreset`, `checkTradingViewCdp`

### Task 6: Server MCP routes

- `GET /api/mcp-servers/presets`
- `POST /api/mcp-servers/from-preset` (installPath validation + entry exists)
- `GET /api/mcp-servers/tradingview/cdp-health`

### Task 7: Settings UI

- TradingView MCP card: install path, Add, CDP test, setup guide

### Task 8: Finance harness

- Built-in `finance_chart_analyst` harness for TradingView MCP chart analysis

### Task 9: Version

- monorepo **0.3.165 → 0.3.166**

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/pages/WorkflowEditor.tsx` | 수정 |
| `apps/desktop/src/pages/Settings.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RunLogPanel.tsx` | 수정 |
| `apps/desktop/src/components/workflow/PipelineRunner.tsx` | 수정 |
| `apps/desktop/src/lib/engine.ts` | 수정 |
| `apps/server/src/routes/mcp.ts` | 수정 |
| `packages/mcp-client/src/presets.ts` | 추가 |
| `packages/mcp-client/src/tradingview-cdp.ts` | 추가 |
| `packages/workflow-engine/src/harness/finance.ts` | 수정 |
| monorepo versions | 0.3.166 |
