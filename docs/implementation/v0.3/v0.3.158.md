# v0.3.158 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **throw-path and residual silent failure
surfaces** — Settings save/MCP/OAuth, WorkflowEditor save/rename/pre-run/export,
PipelineRunner resume, Sessions tool confirm, webhook secret regenerate, and
EngineClient export boolean returns.

기준: v0.3.157

---

## Changes

### Task 1: Settings

- Default provider/model save: catch network throws → scrubbed alert
- Simple key save: catch throws → scrubbed alert (align with API key path)
- MCP create: catch throws → scrubbed alert; form stays open
- MCP OAuth start: catch API throws; catch browser `open()` failure (modal stays)
- MCP OAuth revoke: catch throws → scrubbed alert; keep connected badge
- MCP toggle/delete: use `window.alert` consistently

### Task 2: WorkflowEditor

- Manual save: try/catch/finally so throw cannot leave Save disabled
- Inline rename: catch throws → scrubbed alert
- Pre-run autosave: catch throws → scrubbed alert and abort run
- Export JSON/ZIP: check boolean result → alert on failure

### Task 3: PipelineRunner

- `resumePlugin` throw → clear waiting, set scrubbed `run.failed` (same as `!ok`)

### Task 4: Sessions / NodeConfigPanel / EngineClient

- Sessions `confirmTool` failure → scrubbed alert
- Webhook secret regenerate failure → scrubbed flash (and always clear busy)
- `exportWorkflow` / `exportWorkflowZip` return `boolean` for UI failure surface

### Task 5: Unit tests

- Settings: simple key save throw
- WorkflowEditor: save throw re-enables Save; export JSON/ZIP fail alerts
- PipelineRunner: resume throw clears waiting and shows scrubbed error
- Sessions: confirmTool fail scrub
- NodeConfigPanel: regenerate fail scrub
- EngineClient: export boolean true/false

### Task 6: Version

- monorepo **0.3.157 → 0.3.158**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/pages/Settings.tsx` | 수정 |
| `apps/desktop/src/pages/Settings.test.tsx` | 수정 |
| `apps/desktop/src/pages/WorkflowEditor.tsx` | 수정 |
| `apps/desktop/src/pages/WorkflowEditor.test.tsx` | 수정 |
| `apps/desktop/src/components/workflow/PipelineRunner.tsx` | 수정 |
| `apps/desktop/src/components/workflow/PipelineRunner.test.tsx` | 수정 |
| `apps/desktop/src/pages/Sessions.tsx` | 수정 |
| `apps/desktop/src/pages/Sessions.test.tsx` | 수정 |
| `apps/desktop/src/components/workflow/NodeConfigPanel.tsx` | 수정 |
| `apps/desktop/src/components/workflow/NodeConfigPanel.test.tsx` | 수정 |
| `apps/desktop/src/lib/engine.ts` | 수정 |
| `apps/desktop/src/lib/engine.test.ts` | 수정 |
| monorepo versions | 0.3.158 |
