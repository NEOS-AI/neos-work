# v0.3.72 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **preflight agent provider normalize**
(Tasks 3/14), **MediaNode length caps + option trim** (Task 7), **plugin
manifest id/name hygiene** (Task 5), **CLI env / MCP token trim** (Task 3),
**execution defaults trim**, and **routine run error trim** (Task 2).

기준: v0.3.71

---

## Changes

### Task 1: Workflow preflight (Tasks 3/14)

- Agent `provider` / `llmProvider` trimmed + lower-cased before key checks
- Padded values (` OpenAI `, ` CLI-Claude `) match runtime AgentNode behavior

### Task 2: MediaNode (Task 7)

- Image prompt max 4000 chars (align server route)
- Audio text max 4096 chars
- size/quality/voice/model trim + lower-case before allow-list

### Task 3: Plugin store (Task 5)

- `listPlugins`: trim id/name/description/version
- Blank id falls back to skill directory name
- Skip when both blank

### Task 4: CLI spawn context (Task 3)

- `buildNeosCliEnv`: trim all values; drop whitespace-only
- `loadMcpTokenEnvVars`: trim serverId/accessToken

### Task 5: Settings + routines

- `getExecutionSettings`: trim/lower-case `defaults.provider`; trim model
- `completeRoutineRun`: trim error; blank → null; id trim

### Task 6: Unit tests

- preflight padded OpenAI / CLI providers
- MediaNode length caps + option normalize
- buildNeosCliEnv trim
- listPlugins identity normalize
- completeRoutineRun error trim

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/server/src/lib/workflow-preflight.ts` | 수정 |
| `apps/server/src/lib/workflow-preflight.test.ts` | 수정 |
| `packages/workflow-engine/src/nodes/media.ts` | 수정 |
| `packages/workflow-engine/src/nodes/media.test.ts` | 수정 |
| `apps/server/src/lib/plugin-store.ts` | 수정 |
| `apps/server/src/lib/plugin-store.test.ts` | 수정 |
| `apps/server/src/lib/cli-agents.ts` | 수정 |
| `apps/server/src/lib/cli-agents.test.ts` | 수정 |
| `apps/server/src/db/settings.ts` | 수정 |
| `apps/server/src/db/routines.ts` | 수정 |
| `apps/server/src/db/routines.test.ts` | 수정 |
| monorepo versions | 0.3.72 |
