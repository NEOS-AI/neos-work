# v0.3.139 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **display scrub** for Settings (engine URL/
version, MCP servers, CLI agents), Sidebar connection footer, WorkflowEditor
title, ModeSelection error text; and **null-safe name sort** in list-sort.

기준: v0.3.138

---

## Changes

### Task 1: Settings display

- EngineStatusSection: scrub version + server URL (single-pass)
- MCP server rows: scrub name / transport / URL; stdio command/args drop
  control-char segments before join
- OAuth modal: scrub server name title
- CLI agent rows: scrub name / path / version / id

### Task 2: Shell / editor / mode

- Sidebar: scrub serverUrl + engine version + mode footer
- WorkflowEditor: scrub workflow name in toolbar title
- ModeSelection: scrub connection error (empty scrub → disconnected label)
- exportWorkflowZip: sanitize download filename (align with JSON export)

### Task 3: list-sort

- `sortByName` uses null-byte strip + CR/LF collapse + length cap for stable
  compare keys; non-string → empty key

### Task 4: Unit tests

- list-sort control-char names + non-string keys
- Sidebar control URL/version
- ModeSelection control error
- Settings MCP/CLI/engine scrub display

### Task 5: Version

- monorepo **0.3.138 → 0.3.139**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/pages/Settings.tsx` | 수정 |
| `apps/desktop/src/pages/Settings.test.tsx` | 수정 |
| `apps/desktop/src/components/Sidebar.tsx` | 수정 |
| `apps/desktop/src/components/Sidebar.test.tsx` | 수정 |
| `apps/desktop/src/pages/WorkflowEditor.tsx` | 수정 |
| `apps/desktop/src/pages/ModeSelection.tsx` | 수정 |
| `apps/desktop/src/pages/ModeSelection.test.tsx` | 수정 |
| `apps/desktop/src/lib/list-sort.ts` | 수정 |
| `apps/desktop/src/lib/list-sort.test.ts` | 수정 |
| `apps/desktop/src/lib/engine.ts` | 수정 |
| monorepo versions | 0.3.139 |
