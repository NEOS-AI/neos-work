# v0.3.177 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **PipelineRunner plugin id validation feedback**
and **core package edge tests** (planner coerce, skill parser hygiene, web-search).

기준: v0.3.176

---

## Changes

### Task 1: PipelineRunner — handleStart

- Control-char / non-string plugin id →
  `window.alert('Plugin id contains invalid control characters')`
- Empty-after-trim or length > 100 →
  `window.alert('Plugin id is missing or too long')`
- Still blocks `runPlugin` and does not create run UI

### Task 2: Core residual unit coverage

- Planner: nullish/non-string goal/context, empty models, non-array JSON parse
- Skill parser: optional field hygiene, invalid source fallback, empty triggers,
  path coercion (null-byte files still rejected)
- Web-search tool edge coverage

### Task 3: Unit tests

- PipelineRunner: control-char + overlong plugin id alerts (**19 passed**)
- Core planner / parser / web-search expansions

### Task 4: Version

- monorepo **0.3.176 → 0.3.177**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/components/workflow/PipelineRunner.tsx` | 수정 |
| `apps/desktop/src/components/workflow/PipelineRunner.test.tsx` | 수정 |
| `packages/core/src/agent/planner.test.ts` | 수정 |
| `packages/core/src/skills/parser.test.ts` | 수정 |
| `packages/core/src/tools/web-search.test.ts` | 수정 |
| `docs/implementation/v0.3.177.md` | 신규 |
| monorepo versions | 0.3.177 |
