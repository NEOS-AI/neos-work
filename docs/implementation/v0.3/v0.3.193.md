# v0.3.193 Implementation

## Summary

**PLAN_FOR_V0_4_0 Task 7** — Server API & DB migration: `custom_harness` →
`workers` table (Q1), DomainWorker columns, `/api/workers` + `/api/domain-packs`,
and `/api/harness(es)` deprecation aliases with headers.

기준: v0.3.192  
범위: Task 7 (DB rename, workers CRUD API, domain-packs API, harness alias).  
아직 미구현: desktop pack palette (Task 8), typed ports MVP (Task 9), 0.4.0 cut
(Task 10).

---

## Changes

### Task 1: Schema migration (Q1)

- Canonical table **`workers`** with `permission_profile`, `workspace_json`,
  `default_mode`
- Migrate legacy `custom_harness` → `workers` (rename or copy+drop)
- Column ADD for upgraded DBs

### Task 2: DB module

- `apps/server/src/db/workers.ts` — CRUD + DomainWorker field normalize
  (domains include `research`)
- `harnesses.ts` re-exports deprecated aliases

### Task 3: Routes

- **`GET/POST /api/workers`**, **`GET/PUT/DELETE /api/workers/:id`**
  - merge built-in + custom; built-in mutate → 403
  - supports permissionProfile / workspace / defaultMode
- **`GET /api/domain-packs`**, **`GET /api/domain-packs/:id`**
  - 4 packs with worker/block counts; detail includes custom workers
- **`/api/harness`** + **`/api/harnesses`**: same handlers, headers
  `Deprecation: true`, `Link: </api/workers>`
- Boot: hydrate custom workers into `registerWorker`

### Task 4: Tests & version

- workers / domain-packs / harness deprecation tests
- monorepo **0.3.192 → 0.3.193**
- health / banner / User-Agent / MCP version

---

## Verification

- `@neos-work/server`: **705 tests** passed

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/server/src/db/schema.ts` | 수정 |
| `apps/server/src/db/workers.ts` | 신규 |
| `apps/server/src/db/harnesses.ts` | 수정 (alias) |
| `apps/server/src/db/harnesses.test.ts` | 수정 |
| `apps/server/src/routes/workers.ts` | 신규 |
| `apps/server/src/routes/workers.test.ts` | 신규 |
| `apps/server/src/routes/domain-packs.ts` | 신규 |
| `apps/server/src/routes/domain-packs.test.ts` | 신규 |
| `apps/server/src/routes/harness.ts` | 수정 |
| `apps/server/src/index.ts` | 수정 |
| `packages/workflow-engine/src/nodes/agent.ts` | 수정 (CLI cast) |
| monorepo versions | 0.3.193 |

---

## Next (PLAN_FOR_V0_4_0)

- Task 8: Desktop pack palette, worker selector, Run Log tree polish, templates
- Task 9: Typed ports MVP (strictPorts optional)
- Task 10: migration guide + version **0.4.0**
