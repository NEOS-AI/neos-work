# v0.3.98 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **routine create/update field hygiene**,
**plugin upgrade/resume path and payload bounds**, **planner goal/context
caps**, **message-text body size cap**, **cron expression/timezone bounds**,
**workflow designSystemId hygiene**, and **webhook workflowId bound**.

기준: v0.3.97

---

## Changes

### Task 1: Routines routes

- Name: control-char reject (create/update)
- workflowId length/control-char
- schedule ≤ 200 + control-char reject
- timezone ≤ 100 + control-char reject
- createRoutine DB errors mapped to 400

### Task 2: Plugins routes

- skillId/skillDirName path-safety (no `/`, control-char, length)
- name ≤ 200; description ≤ 2k
- resume: stageId/runId hygiene; HITL response ≤ 200 KiB plain object

### Task 3: Planner / message-text / cron / workflow / webhooks

- Planner: goal ≤ 50k; context ≤ 32k; strip null bytes
- resolveMessageText: cap at 16k
- estimateNextCronRun: expression ≤ 200; control-char reject
- isValidTimeZone: length ≤ 100; control-char reject
- updateWorkflow: designSystemId ≤ 64 + control-char reject
- webhook param: workflowId ≤ 100

### Task 4: Unit tests

- Routines control-char name / overlong schedule
- Plugins path-like skillDirName
- Message text length cap
- Cron control-char/overlong expression & timezone
- Planner null-byte goal handling

### Task 5: Version

- monorepo **0.3.97 → 0.3.98**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/server/src/routes/routines.ts` | 수정 |
| `apps/server/src/routes/plugins.ts` | 수정 |
| `apps/server/src/routes/webhooks.ts` | 수정 |
| `apps/server/src/db/workflows.ts` | 수정 |
| `apps/server/src/lib/cron-next.ts` | 수정 |
| `packages/core/src/agent/planner.ts` | 수정 |
| `packages/workflow-engine/src/nodes/message-text.ts` | 수정 |
| monorepo versions | 0.3.98 |
