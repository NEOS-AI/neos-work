# v0.3.154 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **MCP tool-bridge error scrub**, **browser-tool
toolFailure via scrubErrorMessage**, and **Memory / Harnesses / Blocks delete
failure alerts**.

기준: v0.3.153

---

## Changes

### Task 1: MCP client tool-bridge

- Catch path uses `scrubErrorMessage` (cap 2k)
- MCP `isError` results surface scrubbed text as `error` while keeping raw
  `output` for debugging

### Task 2: Browser tools

- `toolFailure` uses core `scrubErrorMessage`
- Null bytes normalized to spaces first so Playwright messages stay readable
  (`waiting\0for` → `waiting for`)

### Task 3: Desktop delete failures

- Memory / Harnesses / Blocks: on failed delete, alert scrubbed API error
  (was silent success-path only)

### Task 4: Unit tests

- MCP control-char throw + isError scrub
- Memory / Harnesses / Blocks delete failure alerts
- Browser toolFailure control scrub still passes

### Task 5: Version

- monorepo **0.3.153 → 0.3.154**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `packages/mcp-client/src/tool-bridge.ts` | 수정 |
| `packages/mcp-client/src/tool-bridge.test.ts` | 수정 |
| `packages/browser-tool/src/tools.ts` | 수정 |
| `apps/desktop/src/pages/Memory.tsx` | 수정 |
| `apps/desktop/src/pages/Memory.test.tsx` | 수정 |
| `apps/desktop/src/pages/Harnesses.tsx` | 수정 |
| `apps/desktop/src/pages/Harnesses.test.tsx` | 수정 |
| `apps/desktop/src/pages/Blocks.tsx` | 수정 |
| `apps/desktop/src/pages/Blocks.test.tsx` | 수정 |
| monorepo versions | 0.3.154 |
