# v0.3.105 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **workflow/routine/revision/deployment/block/
harness DB lookup id hygiene**, **webhook signature input bounds**, **design-
system / memory-store / plugin-store id sanitize**, **MCP HTTP URL control-char
reject**, **harness resolveHarness control-char before trim**, and **projectName
/ revision label / designSystemId control-char-before-trim**.

기준: v0.3.104

---

## Changes

### Task 1: Workflow / run / webhook DB

- Shared `safeLookupId` (≤100, control-char reject before trim) for workflow
  get/update/delete and run save/get/list/delete/deleteRuns
- `saveRun`: reject control-char / overlong id + workflowId
- `designSystemId` update: control-char check before trim; max 64
- `verifyWebhookSignature`: secret/header control-char reject; body ≤1MiB;
  header ≤512; secret ≤8_192

### Task 2: Routine / revision / deployment DB

- routines: safeLookupId on get/update/delete/setLastRun/runs; create workflowId
  + routineId validation; drop unsafe linked runId
- workflow-revisions: safeLookupId on list/get/create/update/delete; label
  control-char before trim
- deployments: safeLookupId on get/update/delete/list filter; projectName
  control-char drop; capOptionalString control-char reject
- Unsafe deployment list filter → list all (matches session hygiene)

### Task 3: Block / harness / stores / MCP

- blocks + harnesses: safeLookupId on get/update/delete
- harness create: control-char id reject + max length 100; description control
  chars scrubbed
- design-system-store / memory-store / plugin-store: lookup id hygiene
- workflow-engine `resolveHarness`: control-char before trim
- mcp-client HTTP URL: control-char reject before trim
- crypto `isSensitiveKey`: control-char / length bound

### Task 4: Unit tests

- workflows: control-char/overlong lookup + webhook signature bounds
- routines / revisions / deployments / blocks / harnesses lookup hygiene
- web-search result URL + Tavily key edges
- cron horizonDays clamp; media option allow-lists; session null-byte content

### Task 5: Version

- monorepo **0.3.104 → 0.3.105**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/server/src/db/workflows.ts` | 수정 |
| `apps/server/src/db/routines.ts` | 수정 |
| `apps/server/src/db/workflow-revisions.ts` | 수정 |
| `apps/server/src/db/deployments.ts` | 수정 |
| `apps/server/src/db/blocks.ts` | 수정 |
| `apps/server/src/db/harnesses.ts` | 수정 |
| `apps/server/src/db/crypto.ts` | 수정 |
| `apps/server/src/lib/design-system-store.ts` | 수정 |
| `apps/server/src/lib/memory-store.ts` | 수정 |
| `apps/server/src/lib/plugin-store.ts` | 수정 |
| `packages/workflow-engine/src/harness/index.ts` | 수정 |
| `packages/mcp-client/src/client.ts` | 수정 |
| monorepo versions | 0.3.105 |
