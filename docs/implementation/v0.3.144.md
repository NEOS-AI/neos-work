# v0.3.144 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **ArtifactPreview API status/rename seed scrub**,
**webhook flashCopy / secret display scrub**, **Sessions ToolStepCard
toolName/input/output scrub**, and **PipelineRunner failed-error scrub at store**.

기준: v0.3.143

---

## Changes

### Task 1: ArtifactPreview

- Scrub delete / rename / re-run API error strings when setting `statusMsg`
- Scrub re-run meta message
- Rename prompt seed uses scrubbed artifact name

### Task 2: NodeConfigPanel webhook

- `flashCopy` always collapses control chars
- Test-fire failure path scrubs API error
- Clipboard copy strips null bytes
- Secret display scrubbed before mask/show
- `copyMsg` render scrubbed

### Task 3: Sessions ToolStepCard

- Scrub `toolName` (collapse lines)
- Recursively scrub string leaves in tool input/output before JSON.stringify
  (so `\0` never becomes a visible `\u0000` escape in the DOM)
- Size-capped serialization for input (20k) / output (50k)

### Task 4: PipelineRunner

- Store scrubbed `pipeline.failed` error (display already scrubbed)

### Task 5: Residual follow-ups

- DesignSystems: scrub delete-confirm name (defense-in-depth)
- RunInputsDialog: cap confirmed string values (10k); scrub parse-error banner
- Settings CLI: empty-after-scrub error falls back to generic detect failure

### Task 6: Unit tests

- ArtifactPreview delete/rename control error banners + rename prompt seed
- NodeConfigPanel webhook fire control error flash
- Sessions tool step control toolName/input/output

### Task 7: Version

- monorepo **0.3.143 → 0.3.144**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/components/workflow/ArtifactPreview.tsx` | 수정 |
| `apps/desktop/src/components/workflow/ArtifactPreview.test.tsx` | 수정 |
| `apps/desktop/src/components/workflow/NodeConfigPanel.tsx` | 수정 |
| `apps/desktop/src/components/workflow/NodeConfigPanel.test.tsx` | 수정 |
| `apps/desktop/src/components/workflow/PipelineRunner.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RunInputsDialog.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RunInputsDialog.test.tsx` | 수정 |
| `apps/desktop/src/pages/Sessions.tsx` | 수정 |
| `apps/desktop/src/pages/Sessions.test.tsx` | 수정 |
| `apps/desktop/src/pages/DesignSystems.tsx` | 수정 |
| `apps/desktop/src/pages/Settings.tsx` | 수정 |
| `apps/desktop/src/pages/Harnesses.test.tsx` | 수정 |
| monorepo versions | 0.3.144 |
