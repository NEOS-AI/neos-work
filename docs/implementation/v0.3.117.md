# v0.3.117 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **control-char-before-trim** on custom block
DB field normalization (implementationType/domain/category/name/skillId +
null-byte description/prompt/IO), harness domain/tools/description, deployment
provider/status, routine-run error scrub + status, memory search tags, deploy
list limit query, session MCP args load, and workflow-preflight agent provider.

기준: v0.3.116

---

## Changes

### Task 1: Blocks / harnesses / deployments DB

- blocks: `normalizeImplementationType` / `normalizeDomain` control before trim
- blocks create/update: category control → custom; name control before trim;
  skillId control before trim; description/promptTemplate/IO null-byte reject
- blocks list: control-char domain filter → list all (not force general)
- harnesses: domain control → fallback; allowedTools control before trim;
  description always collapse `\0`/`\r`/`\n` before trim
- deployments: provider/status control before trim (reject / fallback)

### Task 2: Routines / memory / routes / preflight

- completeRoutineRun: status control → completed; error CR/LF/`\0` scrub
- memory searchMemory tags: control-char check before trim
- deploy GET list: limit query control-char ignored → default 100
- session loadMcpTools: drop control-char args before trim
- workflow-preflight: agent provider control → anthropic default

### Task 3: Unit tests

- blocks: leading control implementationType/name/category/domain/skillId;
  null-byte description/prompt/IO; control domain list filter
- harnesses: leading-control tools/domain/description collapse
- deployments: leading-control provider reject; status fallback
- routines: error scrub + control status → completed
- memory: leading-control tag filter does not match
- preflight: control agent provider → anthropic key check;
  control deploy provider → vercel token check

### Task 4: Version

- monorepo **0.3.116 → 0.3.117**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/server/src/db/blocks.ts` | 수정 |
| `apps/server/src/db/blocks.test.ts` | 수정 |
| `apps/server/src/db/harnesses.ts` | 수정 |
| `apps/server/src/db/harnesses.test.ts` | 수정 |
| `apps/server/src/db/deployments.ts` | 수정 |
| `apps/server/src/db/deployments.test.ts` | 수정 |
| `apps/server/src/db/routines.ts` | 수정 |
| `apps/server/src/db/routines.test.ts` | 수정 |
| `apps/server/src/db/memory.ts` | 수정 |
| `apps/server/src/db/memory.test.ts` | 수정 |
| `apps/server/src/lib/workflow-preflight.ts` | 수정 |
| `apps/server/src/lib/workflow-preflight.test.ts` | 수정 |
| `apps/server/src/routes/deploy.ts` | 수정 |
| `apps/server/src/routes/session.ts` | 수정 |
| monorepo versions | 0.3.117 |
