# v0.3.47 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **Escape** clears search on Skills / Routines /
Workflows / Media, **preflight** deploy provider default + Ollama skip, and
**agent maxSteps** upper bound (1–200).

기준: v0.3.46

---

## Changes

### Task 1: Escape list search UX

- **Skills**: try-prompt first, else clear search
- **Routines**: create modal first, else clear search
- **Workflows**: create modal first, else clear search (`closeCreateModal` useCallback)
- **Media**: clear preview first, else clear search
- All respect `defaultPrevented`

### Task 2: Workflow preflight (Tasks 3 / 8)

- Deploy without provider treated as **vercel** (matches DeployNode)
- **Ollama** agents skip cloud API key requirements

### Task 3: Agent max steps validation

- **`invalid_agent_max_steps`**: integer **1–200** (was positive only)

### Task 4: Unit tests

- maxSteps 201 / 200 bounds
- preflight default vercel + ollama

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/pages/Skills.tsx` | 수정 |
| `apps/desktop/src/pages/Routines.tsx` | 수정 |
| `apps/desktop/src/pages/Workflows.tsx` | 수정 |
| `apps/desktop/src/pages/Media.tsx` | 수정 |
| `apps/desktop/src/components/workflow/WorkflowValidation.ts` | 수정 |
| `apps/desktop/src/components/workflow/WorkflowValidation.test.ts` | 수정 |
| `apps/server/src/lib/workflow-preflight.ts` | 수정 |
| `apps/server/src/lib/workflow-preflight.test.ts` | 수정 |
| monorepo versions | 0.3.47 |
