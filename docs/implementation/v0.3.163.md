# v0.3.163 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **safe EngineClient JSON parse**, **useEngine
connect error URL scrub**, and **Dashboard all-catalogs-failed banner**.

기준: v0.3.162

---

## Changes

### Task 1: EngineClient

- `readApiResponse(res)` — invalid JSON / non-object → `{ ok: false, error }`
  with scrubbed statusText (never throws SyntaxError into UI)
- `readHealthResponse(res)` — invalid health body throws clean `HTTP n` Error
- All ApiResponse `return res.json()` call sites use `readApiResponse`
- `health()` uses `readHealthResponse`

### Task 2: useEngine

- Connect failure message scrubs `serverUrl` before embedding (control-char
  injection defense in ModeSelection / status chrome)

### Task 3: Dashboard

- When every catalog list fails (`null` throw or `!ok`), show scrubbed
  `loadError` banner (first API error or fallback)

### Task 4: Unit tests

- `readApiResponse` / `readHealthResponse`
- Dashboard all-fail scrub banner
- useEngine control-char URL in connect error
- Docs: align v0.3.162 notes

### Task 5: Version

- monorepo **0.3.162 → 0.3.163**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/lib/engine.ts` | 수정 |
| `apps/desktop/src/lib/engine.test.ts` | 수정 |
| `apps/desktop/src/hooks/useEngine.tsx` | 수정 |
| `apps/desktop/src/hooks/useEngine.test.tsx` | 수정 |
| `apps/desktop/src/pages/Dashboard.tsx` | 수정 |
| `apps/desktop/src/pages/Dashboard.test.tsx` | 수정 |
| `docs/implementation/v0.3.162.md` | 수정 |
| monorepo versions | 0.3.163 |
