# v0.13.0 Implementation

## Summary

**M0 — Contract gates for shared-edit train.** Expands `contract-fe-be.test.ts`
(the suite behind `pnpm e2e:contract` / PR CI) with agent hard-enforce 423, run
→ collab session bind + PUT inherit, and tools/files write wire.

Baseline: **0.12.3**  
**Plan:** [`docs/plans/PLAN_FOR_V0_13_0.md`](../../plans/PLAN_FOR_V0_13_0.md) M0

---

## Task map

| Item | Status |
|---|---|
| Plan file | **Done** |
| Agent 423 holder parse in contract | **Done** |
| Run bind + runId inherit | **Done** |
| tools/files write + 423 | **Done** |
| Version **0.13.0** | **Done** |

---

## Cases added

| Test | Protects |
|---|---|
| agent hard-enforce 423 holder shape | v0.10 M0 `NEOS_SHARED_EDIT` + `_AGENTS` |
| run collabSessionId bind + PUT via runId | v0.11 M0 Q35 |
| tools/files write hash + 423 | v0.11 M2 Q36 |

Contract Hono app now mounts `/api/tools/files` and clears tool tokens in cleanup.

---

## Verify

```bash
pnpm e2e:contract
# or:
pnpm --filter @neos-work/server exec vitest run src/routes/contract-fe-be.test.ts
```

---

## Out of scope (M1–M3)

- Locks snapshot flag assertions (M1)  
- Inventory `v13Features` closeout (M3)  
