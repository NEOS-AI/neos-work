# v0.13.2 Implementation

## Summary

**M2 — CI hygiene.** Documents what `pnpm e2e:contract` covers (header on the
suite + plan/impl), and hardens shared Zod unit tests for run
`collabSessionId` (null) and locks snapshot enforce flags. No CI workflow
change — contract was already on every PR.

Baseline: **0.13.1**  
**Plan:** [`docs/plans/PLAN_FOR_V0_13_0.md`](../../plans/PLAN_FOR_V0_13_0.md) M2

---

## Task map

| Item | Status |
|---|---|
| Document contract cases (suite header + plan) | **Done** |
| Shared parse tests for collabSessionId null + locks flags | **Done** |
| Version **0.13.2** | **Done** |

---

## Artifacts

| Path | Change |
|---|---|
| `apps/server/src/routes/contract-fe-be.test.ts` | File-level coverage list for PR CI discoverability |
| `packages/shared/src/schemas/api-envelopes.test.ts` | Unbound run + locks snapshot flags |
| This note + plan M2 | Checklist closed |

### PR CI (unchanged, still required)

```yaml
# .github/workflows/ci.yml
- pnpm e2e:contract   # → contract-fe-be.test.ts (10 cases as of 0.13.1+)
```

Root script:

```bash
pnpm e2e:contract
```

---

## Verify

```bash
pnpm e2e:contract
pnpm --filter @neos-work/shared test
```

---

## Out of scope (M3)

- Full migration + release + inventory `v13Features`  
