# v0.13.1 Implementation

## Summary

**M1 — Snapshot flags + run summary wire.** Contract suite asserts locks GET
`hardEnforce` / `agentsHardEnforce` and dry-run run create returns
`collabSessionId` (body + header bind).

Baseline: **0.13.0**  
**Plan:** [`docs/plans/PLAN_FOR_V0_13_0.md`](../../plans/PLAN_FOR_V0_13_0.md) M1

---

## Task map

| Item | Status |
|---|---|
| Locks snapshot flags via `parseCollabLocksSnapshot` | **Done** |
| Env-on flags true | **Done** |
| Dry-run `collabSessionId` (body/header/unbound) | **Done** |
| Version **0.13.1** | **Done** |

---

## Cases

| Test | Asserts |
|---|---|
| GET peers/locks (existing) | Locks parse via shared schema; flags **false** by default |
| locks snapshot flags env | Both flags **true** when env set |
| dry-run collabSessionId | Body+header bind, header-only, unbound → null |

---

## Verify

```bash
pnpm e2e:contract
```

---

## Out of scope (M2–M3)

- CI hygiene docs  
- Inventory `v13Features` closeout  
