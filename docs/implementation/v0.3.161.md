# v0.3.161 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **list-load try/finally** so pages cannot stick
on Loading… when the client throws, plus scrubbed load-error banners where UI
exists; **export throw** surfaces for WorkflowEditor and RunHistoryPanel.

기준: v0.3.160

---

## Changes

### Task 1: List load busy cleanup

- DesignSystems / Deployments / Media: try/catch/finally; scrubbed page error on
  `!ok` or throw; clear loading always
- Memory / Harnesses / Blocks / Workflows: try/finally; empty list on failure
- Routines / Sessions load: catch throws → empty list (no stuck partial)
- Skills load: scrubbed `loadError` on `!ok` / throw

### Task 2: Export throw surfaces

- WorkflowEditor export JSON/ZIP: catch throws → scrubbed alert
- RunHistoryPanel single-run JSON export: catch throws → scrubbed alert

### Task 3: Unit tests

- DesignSystems list fail / throw page errors
- Deployments / Media load throw scrub
- WorkflowEditor export throw scrub
- RunHistoryPanel export throw scrub

### Task 4: Version

- monorepo **0.3.160 → 0.3.161**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/pages/DesignSystems.tsx` | 수정 |
| `apps/desktop/src/pages/Deployments.tsx` | 수정 |
| `apps/desktop/src/pages/Media.tsx` | 수정 |
| `apps/desktop/src/pages/Memory.tsx` | 수정 |
| `apps/desktop/src/pages/Harnesses.tsx` | 수정 |
| `apps/desktop/src/pages/Blocks.tsx` | 수정 |
| `apps/desktop/src/pages/Workflows.tsx` | 수정 |
| `apps/desktop/src/pages/Routines.tsx` | 수정 |
| `apps/desktop/src/pages/Skills.tsx` | 수정 |
| `apps/desktop/src/pages/Sessions.tsx` | 수정 |
| `apps/desktop/src/pages/WorkflowEditor.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RunHistoryPanel.tsx` | 수정 |
| monorepo versions | 0.3.161 |
