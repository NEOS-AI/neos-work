# v0.20.0 Implementation — Track C EngineCatalogClient

## Summary

**Track C — Blocks / templates / memory extract.** Moves the last remaining
product domains off `EngineClient` into `EngineCatalogClient`. `EngineClient`
is now a thin re-export entrypoint.

Baseline: **0.19.0**  
Shipped with Track D in monorepo **0.20.0**.

---

## Task map

| Item | Status |
|---|---|
| `engine-catalog.ts` | **Done** |
| Types (`MemoryType`, `MemoryItem`, `CreateMemoryInput`, `UpdateMemoryInput`) | **Done** |
| `EngineCatalogClient extends EngineOpsClient` | **Done** |
| `EngineClient extends EngineCatalogClient` (empty body) | **Done** |

---

## Methods

**Blocks:** `listBlocks`, `createBlock`, `updateBlock`, `deleteBlock`  
**Templates:** `getTemplates`  
**Memory:** `listMemories`, `createMemory`, `updateMemory`, `deleteMemory`, `toggleMemory`

## Hierarchy

```text
… → EngineOpsClient → EngineCatalogClient → EngineClient
```

## Verify

```bash
pnpm --filter @neos-work/desktop typecheck
pnpm --filter @neos-work/desktop exec vitest run src/lib/engine.test.ts
```

## Related

- Track D: [`v0.20.1.md`](./v0.20.1.md)  
- Plan: [`PLAN_FOR_V0_20_0.md`](../../plans/PLAN_FOR_V0_20_0.md)  
