# v0.26.0 Implementation — Web parity for remaining desktop surfaces

## Summary

Web now covers Blocks, Templates, Skills, Design systems (+ editor), Routines, and Deployments. Domain pack zip install and marketplace catalog URL persist on the daemon.

## Routes

`/blocks` `/templates` `/skills` `/design-systems` `/design-systems/:id` `/routines` `/deployments`

## Client

`WebApiClient` adds skills, blocks, templates, design-systems, routines, deploy, pack zip, catalog-url methods.
