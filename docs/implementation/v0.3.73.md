# v0.3.73 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **desktop WorkflowValidation normalize** for
agent/media/deploy (Tasks 3/6/7/8/15), **media option validators trim**,
**saveRun status/error hygiene**, **deleteRuns status allow-list**, **harness
register field trim**, and **resolveMessageText input trim**.

기준: v0.3.72

---

## Changes

### Task 1: WorkflowValidation (Tasks 3/6/7/8)

- Agent provider: trim + lower-case so padded CLI providers skip harness warning
- Media type: trim + lower-case (` Image ` → image)
- Media length warnings: prompt > 4000, audio text > 4096
- Deploy provider: trim + lower-case before vercel/cloudflare check

### Task 2: media-node-options

- `isMediaImageSize`: trim
- `isMediaVoice` / `isMediaImageQuality` / `isMediaTtsModel`: trim + lower-case

### Task 3: Workflow runs DB

- `saveRun`: normalize status to lower-case allow-list; trim error
- Unknown status → `running`
- `deleteRuns`: lower-case status filter; unknown status → 0 deletes

### Task 4: Harness registry

- `registerHarness`: trim name/description/systemPrompt; domain allow-list;
  trim allowedTools
- `listHarnesses`: domain filter lower-case

### Task 5: Message text

- `resolveMessageText`: trim fallback `inputs.text`

### Task 6: Unit tests

- media option pad/case accept
- validation media type pad + length caps + CLI pad
- saveRun status/error normalize
- registerHarness field hygiene
- resolveMessageText padded inputs.text

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/components/workflow/WorkflowValidation.ts` | 수정 |
| `apps/desktop/src/components/workflow/WorkflowValidation.test.ts` | 수정 |
| `apps/desktop/src/lib/media-node-options.ts` | 수정 |
| `apps/desktop/src/lib/media-node-options.test.ts` | 수정 |
| `apps/server/src/db/workflows.ts` | 수정 |
| `apps/server/src/db/workflows-runs.test.ts` | 수정 |
| `packages/workflow-engine/src/harness/index.ts` | 수정 |
| `packages/workflow-engine/src/harness/harness.test.ts` | 수정 |
| `packages/workflow-engine/src/nodes/message-text.ts` | 수정 |
| `packages/workflow-engine/src/nodes/message-text.test.ts` | 수정 |
| monorepo versions | 0.3.73 |
