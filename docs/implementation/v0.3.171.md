# v0.3.171 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **browser-tool edge-case unit coverage** —
`isSafeBrowserUrl` non-string inputs, `toolFailure` fallbacks, navigate title/URL
coercion, and extract_text / extract_links defensive paths.

기준: v0.3.170

---

## Changes

### Task 1: isSafeBrowserUrl

- Cover `null` / number / `undefined` inputs → `null`

### Task 2: toolFailure fallbacks

- Non-Error throw, whitespace-only Error message, non-string `Error.message`
  → `browser_click failed` fallback

### Task 3: navigate / extract coercion

- Non-string page title → stringified; blank final URL falls back to request URL
- Null title → empty string
- `extract_text` null DOM text → empty string
- `extract_links` drops non-string href / null entries

### Task 4: Unit tests

- **30 passed** (browser-tool suite)

### Task 5: Version

- monorepo **0.3.170 → 0.3.171**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `packages/browser-tool/src/tools.test.ts` | 수정 |
| `docs/implementation/v0.3.171.md` | 신규 |
| monorepo versions | 0.3.171 |
