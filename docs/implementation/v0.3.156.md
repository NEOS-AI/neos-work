# v0.3.156 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **Memory toggle + create/update failure
surfaces**, **Blocks/Harnesses create/update API error display** (no silent
modal close), **Sessions create failure alert**, and **RevisionPanel restore /
label failure alerts**.

기준: v0.3.155

---

## Changes

### Task 1: Memory

- `handleToggle`: scrubbed alert on API failure; skip reload (no optimistic flip)
- `handleSave` (create/update): check `res.ok`; throw scrubbed error so modal
  stays open and shows the message

### Task 2: Blocks / Harnesses

- Blocks `handleSave`: check `res.ok`; throw scrubbed error into modal
- Harnesses modal save: check `res.ok` before `onSaved()`; set scrubbed modal
  error and keep open on failure

### Task 3: Sessions

- Create session failure → scrubbed `window.alert`; modal remains open

### Task 4: RevisionPanel

- Restore: scrubbed alert when `getRevision` fails; alert `Invalid snapshot`
  on JSON parse failure
- Label update: scrubbed alert on API failure; remain in edit mode

### Task 5: Unit tests

- Memory toggle + create failure
- Blocks / Harnesses create failure keep modal
- Sessions create failure
- Revision restore fetch / invalid snapshot / label update failures
- **96 passed** across related suites

### Task 6: Version

- monorepo **0.3.155 → 0.3.156**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/pages/Memory.tsx` | 수정 |
| `apps/desktop/src/pages/Memory.test.tsx` | 수정 |
| `apps/desktop/src/pages/Blocks.tsx` | 수정 |
| `apps/desktop/src/pages/Blocks.test.tsx` | 수정 |
| `apps/desktop/src/pages/Harnesses.tsx` | 수정 |
| `apps/desktop/src/pages/Harnesses.test.tsx` | 수정 |
| `apps/desktop/src/pages/Sessions.tsx` | 수정 |
| `apps/desktop/src/pages/Sessions.test.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RevisionPanel.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RevisionPanel.test.tsx` | 수정 |
| monorepo versions | 0.3.156 |
