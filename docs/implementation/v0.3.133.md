# v0.3.133 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **display / extract control-char scrub** on run
UI (RunLogPanel, RunDetailPanel, serializeNodeOutput) and browser tools
(extract_text null-byte strip, navigate title/url hygiene, toolFailure error
scrub).

기준: v0.3.132

---

## Changes

### Task 1: Desktop run UI

- `scrubDisplayText` helper: strip `\0`; optional collapse CR/LF; maxChars
- `serializeNodeOutput`: null-byte scrub + optional/default size cap
  (`NODE_OUTPUT_MAX_CHARS`)
- RunLogPanel: collapse control in labels / errors / nodeType / runId; scrub
  stream accumulated text (multi-line OK, size-capped); safe artifactId;
  linkify null-byte defense; uses `serializeNodeOutput` for copy/display
- RunDetailPanel: scrub status / label / error / runId header; copy uses
  scrubbed serialize; on-screen output capped
- RunHistoryPanel: scrub status / id / errors; normalizeRunStatus for failed
  counts
- run-history-filter: control-char prefs rejected; normalizeRunStatus;
  case-insensitive status filter
- engine SSE: `parseSseDataPayload` / `parseSseEventName` reject null-byte /
  control-char lines; pipeline runId control-char ignored

### Task 2: Browser tools

- browser_extract_text: strip `\0` from DOM text (multi-line OK)
- browser_navigate: scrub control-char page titles; fall back unsafe / overlong
  / non-http(s) final URL after redirect
- toolFailure: collapse control chars in Playwright error messages

### Task 3: Unit tests

- scrubDisplayText + serializeNodeOutput null-byte / maxChars / coerce
- RunLogPanel control-char error collapse + stream null-byte scrub
- RunDetailPanel control-char error / label / output / runId header
- browser extract_text null-byte; navigate control title + unsafe final URL
- run-history-filter control prefs / normalizeRunStatus; engine SSE parsers
- Residual route coverage: harness / mcp / routines

### Task 4: Version

- monorepo **0.3.132 → 0.3.133**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/lib/format-duration.ts` | 수정 |
| `apps/desktop/src/lib/format-duration.test.ts` | 수정 |
| `apps/desktop/src/lib/engine.ts` | 수정 |
| `apps/desktop/src/lib/engine.test.ts` | 수정 |
| `apps/desktop/src/lib/run-history-filter.ts` | 수정 |
| `apps/desktop/src/lib/run-history-filter.test.ts` | 수정 |
| `apps/desktop/src/components/workflow/RunLogPanel.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RunLogPanel.test.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RunDetailPanel.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RunDetailPanel.test.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RunHistoryPanel.tsx` | 수정 |
| `packages/browser-tool/src/tools.ts` | 수정 |
| `packages/browser-tool/src/tools.test.ts` | 수정 |
| `apps/server/src/routes/harness.test.ts` | 수정 |
| `apps/server/src/routes/mcp.test.ts` | 수정 |
| `apps/server/src/routes/routines.test.ts` | 수정 |
| monorepo versions | 0.3.133 |
