# v0.3.2 Implementation

## Summary

PLAN_FOR_V0_3_0 잔여 항목 마감: Routine crystallize, Workflow Schedule 단축 생성,
Artifact Refresh, Deploy preflight, 팔레트·NodeConfig(media/deploy/parallel/CLI),
PipelineRunner 분리, Webhook 설정 패널, Agent CLI `llmProvider` 연동.

기준: v0.3.1 (Deployments 이력·Settings Deploy·타입 폴리시)

---

## Changes

### Task 1: Routine crystallize (PLAN Task 2)

- **`apps/server/src/db/routines.ts`**
  - `getRoutineRun(routineId, runId)` — routine_run PK 또는 workflow run_id 조회

- **`apps/server/src/routes/routines.ts`**
  - `POST /api/routines/:id/runs/:runId/crystallize`
  - 성공(status=completed) run만 허용
  - `~/.config/neos-work/skills/<name>/SKILL.md` 작성 + `skill` 테이블 upsert
  - 워크플로우 run nodeResults 요약을 마크다운으로 포함

- **`apps/desktop/src/lib/engine.ts`**
  - `crystallizeRoutineRun()`

- **`apps/desktop/src/pages/Routines.tsx`**
  - 완료된 run에 **Crystallize** 버튼

---

### Task 2: Workflow Schedule shortcut (PLAN Task 2)

- **`apps/desktop/src/pages/WorkflowEditor.tsx`**
  - 툴바 **⏱ Schedule** → cron 프리셋 모달 → `createRoutine` 후 Routines 이동 옵션

---

### Task 3: Artifact refresh (PLAN Task 4)

- **`apps/server/src/routes/artifacts.ts`**
  - `POST /api/artifacts/:id/refresh` — file_path 있으면 home 하위 재읽기, 없으면 DB 내용 반환

- **`apps/desktop`**
  - `refreshArtifact()` + ArtifactPreview **↻ Refresh** 버튼
  - iframe sandbox에서 `allow-same-origin` 제거 (보안 정렬)

---

### Task 4: Deploy preflight (PLAN Task 8)

- **`apps/server/src/routes/deploy.ts`**
  - `POST /api/deploy/preflight` — Vercel/Cloudflare 토큰·프로젝트명 검사 (배포 없음)

- **`apps/desktop`**
  - `deployPreflight()` + Deploy 노드 설정에서 **Run deploy preflight**

---

### Task 5: Node palette & NodeConfig (PLAN Tasks 7, 8, 11, 3)

- **WorkflowEditor** 팔레트: `media`, `deploy`, `parallel_start`, `parallel_end`, `or_gate` + 색상
- **NodeConfigPanel**
  - CLI providers: `cli-claude` / `cli-gemini` / `cli-codex` (`llmProvider` + `provider` 동기화)
  - Media: type/prompt/size/voice
  - Deploy: provider/projectName/content + preflight
  - Parallel/OR 게이트 안내 문구
  - 노드 미선택 시 **Webhook** URL·secret·재생성

- **`packages/workflow-engine/src/nodes/agent.ts`**
  - CLI 분기에서 `provider` 또는 `llmProvider` 모두 인식

---

### Task 6: PipelineRunner extract (PLAN Tasks 5–6)

- **`apps/desktop/src/components/workflow/PipelineRunner.tsx`** (신규)
- **`apps/desktop/src/pages/Plugins.tsx`** — Runner 분리, 카드 그리드 유지

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/server/src/db/routines.ts` | 수정 |
| `apps/server/src/routes/routines.ts` | 수정 |
| `apps/server/src/routes/artifacts.ts` | 수정 |
| `apps/server/src/routes/deploy.ts` | 수정 |
| `packages/workflow-engine/src/nodes/agent.ts` | 수정 |
| `apps/desktop/src/lib/engine.ts` | 수정 |
| `apps/desktop/src/pages/Routines.tsx` | 수정 |
| `apps/desktop/src/pages/WorkflowEditor.tsx` | 수정 |
| `apps/desktop/src/pages/Plugins.tsx` | 수정 |
| `apps/desktop/src/components/workflow/ArtifactPreview.tsx` | 수정 |
| `apps/desktop/src/components/workflow/NodeConfigPanel.tsx` | 수정 |
| `apps/desktop/src/components/workflow/PipelineRunner.tsx` | 신규 |

---

## Version

- **0.3.1 → 0.3.2** (monorepo + packages + server payload)

---

## Non-Goals

- Artifact refresh 시 노드 재실행(전체 워크플로우 re-run)
- Claude Design ZIP HTML 전용 import 분기 고도화
- Deploy 상태 폴링
