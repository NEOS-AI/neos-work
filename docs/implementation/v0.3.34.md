# v0.3.34 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish after v0.3.33 review: **shared domain filter chip
options**, **Harnesses Escape clears edit target**, and small test hygiene.

기준: v0.3.33

---

## Changes

### Task 1: Shared domain filter options

- **`DOMAIN_FILTER_OPTIONS`** in `domain-filter-prefs.ts` — stable chip order
  `all | finance | coding | general`
- **Blocks / Templates / Harnesses** use the shared constant instead of local
  string arrays (typed handlers, no cast)

### Task 2: Harnesses modal Escape (UX)

- Escape and `closeModal` now clear both `showModal` and `editTarget`, matching
  create/edit open helpers and avoiding a stale edit target after dismiss

### Task 3: Tests

- `domain-filter-prefs.test.ts` — asserts `DOMAIN_FILTER_OPTIONS` chip order
- `RunHistoryPanel.test.tsx` — drop unused `within` import

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/lib/domain-filter-prefs.ts` | 수정 |
| `apps/desktop/src/lib/domain-filter-prefs.test.ts` | 수정 |
| `apps/desktop/src/pages/Blocks.tsx` | 수정 |
| `apps/desktop/src/pages/Templates.tsx` | 수정 |
| `apps/desktop/src/pages/Harnesses.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RunHistoryPanel.test.tsx` | 수정 |
| monorepo versions | 0.3.34 |
