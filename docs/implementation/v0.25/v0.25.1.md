# v0.25.1 Implementation — Track B Cowork skills + new file

## Summary

Close two Cowork “Prompt to Done” gaps:

1. Bundled skills for **PowerPoint** and **Word** document generation (SKILL.md packages).
2. Web Design Project **New file** (empty write via existing `PUT …/files/*`).

## Skills

| Package | Role |
|---|---|
| `skills/pptx-deck/SKILL.md` | Slide outline → `.pptx` brief the agent can execute |
| `skills/docx-report/SKILL.md` | Structured report → `.docx` brief |

Discovery is directory `SKILL.md` (existing inventory `scanSkills`).

## Web New file

`ProjectDetail` **New file** prompts for a relative path and `writeFile`s empty (or minimal) content with collab `sessionId` for hard-enforce.
