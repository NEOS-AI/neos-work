# v0.25.3 Implementation — Track D shared workflow-draft

## Summary

Stop copying `workflow-draft.ts` between desktop and web.

`buildWorkflowDraft` / `toReactFlowNodes` / `toReactFlowEdges` live in `@neos-work/shared` using xyflow-**agnostic** node/edge shapes (no new shared dependency).

Desktop and web keep thin re-exports so existing imports stay valid.

## Files

- `packages/shared/src/workflow-draft.ts` (+ tests)
- `apps/desktop/src/lib/workflow-draft.ts` — re-export
- `apps/web/src/lib/workflow-draft.ts` — re-export
