# v0.25.2 Implementation — Track C collab reconnect + leave-blocker

## Summary

Incremental collab/editor hardening. **Not** CRDT and **not** sticky SSE.

| Item | Behavior |
|---|---|
| SSE reconnect | Shared `nextSseReconnectDelay` / `shouldReconnectSse`; web collab + file streams retry with backoff |
| Leave-blocker | Web Workflow editor `beforeunload` + confirm modal on in-app leave when dirty |
| CRDT | Still deferred (ADR 0001 lock + LWW) |

## Files

- `packages/shared/src/collab-ux.ts` — reconnect policy helpers
- `apps/web/src/lib/api.ts` — `streamProjectCollab` / `streamProjectFileEvents` retry
- `apps/web/src/pages/WorkflowEditor.tsx` — dirty leave guard
- `apps/web/src/pages/ProjectDetail.tsx` — reconnect status chip
