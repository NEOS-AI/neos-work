# v0.3.146 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **Routines detail panel display scrub**,
**create/edit/run API error scrub**, **workflow select option labels**, and
**DesignSystemEditor save failure store-time scrub**.

기준: v0.3.145

---

## Changes

### Task 1: Routines detail panel

- Scrub selected routine `name`, `schedule`, `timezone` in the detail header
- Scrub workflow label (name or id fallback) in the meta card
- Align with list-row scrub already present in v0.3.137+

### Task 2: Routines form / edit / run errors

- `createRoutine` API failure → scrub before `setFormError` (cap 300)
- `updateRoutine` schedule failure → scrub before `setEditError`
- Render-time scrub on formError / editError banners (defense-in-depth)
- `runRoutineNow` failure now alerts a scrubbed error (was silent)

### Task 3: Create-modal workflow options

- Workflow `<option>` labels scrubbed (control-char names never appear raw)

### Task 4: DesignSystemEditor

- Store-time scrub of `res.error` when building `Save failed: …` toast
  (render already scrubbed; store no longer holds control chars)

### Task 5: Unit tests

- Create API control-char error banner
- Schedule update control-char error banner
- Detail panel scrub after list select
- Run-now failure alerts scrubbed error
- RunId / crystallize name+path / empty-error fallback alerts
- DesignSystemEditor save toast store-time scrub comment update

### Task 6: Residual coverage (existing hygiene)

- `ReflectionStrategy`: control-char action → skip; null-byte revisedDescription
  drop; overlong tool name drop; oversized revisedInput truncate; non-object
  revisedInput fall-back; bare step prompt omits input section
- `isDiscordWebhookUrl`: non-string / unparseable URL rejection

### Task 7: Version

- monorepo **0.3.145 → 0.3.146**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/pages/Routines.tsx` | 수정 |
| `apps/desktop/src/pages/Routines.test.tsx` | 수정 |
| `apps/desktop/src/pages/DesignSystemEditor.tsx` | 수정 |
| `apps/desktop/src/pages/DesignSystemEditor.test.tsx` | 수정 |
| `packages/core/src/agent/healing.test.ts` | 수정 |
| `packages/shared/src/messaging.test.ts` | 수정 |
| monorepo versions | 0.3.146 |
