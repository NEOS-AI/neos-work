# v0.3.6 Implementation

## Summary

PLAN_FOR_V0_3_0 UX/완성도 폴리시: `RunLogPanel` 분리, Auto Layout TB/LR 방향 전환,
revision 스냅샷에 `designSystemId` 포함 + 복원, webhook rate-limit 상태 표시.

기준: v0.3.5

---

## Changes

### Task 1: RunLogPanel extract (PLAN Task 14)

- **`apps/desktop/src/components/workflow/RunLogPanel.tsx`** (신규)
  - streaming progress 누적 표시, expand, auto-scroll
- **WorkflowEditor** Run 탭이 해당 컴포넌트 사용

### Task 2: Auto Layout direction (PLAN Task 15)

- Layout 버튼 + Dir 토글 (TB ↔ LR)
- `autoLayout(nodes, edges, direction)` 연동

### Task 3: Revision snapshot completeness (PLAN Task 16)

- 저장 전 스냅샷에 `designSystemId` 포함
- Restore 시 description / designSystemId 복원

### Task 4: Webhook rate-limit visibility (PLAN Task 13)

- Secret 응답에 `rateLimit: { limit, remaining, resetAt, windowMs }`
- `GET /api/webhook/:id/rate-limit`
- Config 패널에 remaining/limit 표시

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/components/workflow/RunLogPanel.tsx` | 신규 |
| `apps/desktop/src/components/workflow/RunLogPanel.test.tsx` | 신규 |
| `apps/desktop/src/pages/WorkflowEditor.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RevisionPanel.tsx` | 수정 |
| `apps/desktop/src/components/workflow/NodeConfigPanel.tsx` | 수정 |
| `apps/desktop/src/lib/engine.ts` | 수정 |
| `apps/server/src/routes/workflow.ts` | 수정 |
| `apps/server/src/routes/webhooks.ts` | 수정 |

---

## Version

- **0.3.5 → 0.3.6**

---

## Non-Goals

- Single-node artifact re-execution
- Video media / Marketplace
- Full OD §22 SSRF hardening
