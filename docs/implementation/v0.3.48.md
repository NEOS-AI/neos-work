# v0.3.48 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **Escape hygiene** across workflow modals/panels
and **Sessions** Escape (close new-session modal / clear search). Handlers respect
`defaultPrevented` and call `preventDefault` so stacked listeners do not double-fire.

기준: v0.3.47

---

## Changes

### Task 1: Sessions Escape (list UX)

- Page-level Escape: close **new session modal** first, else clear **session search**
- **NewSessionModal** Escape: `defaultPrevented` + `preventDefault`

### Task 2: Workflow modal/panel Escape hygiene

All of the following check `defaultPrevented` and call `preventDefault()` when
handling Escape:

- **PipelineRunner** (stop stream + close)
- **ConfirmLeaveModal** (Stay)
- **RunInputsDialog** (cancel)
- **RunDetailPanel** (close)
- **RevisionPanel** (cancel label edit / close panel)
- **WorkflowEditor** schedule + shortcuts overlays

### Task 3: Unit tests

- Existing component Escape tests should still pass (behavior preserved)

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/pages/Sessions.tsx` | 수정 |
| `apps/desktop/src/pages/WorkflowEditor.tsx` | 수정 |
| `apps/desktop/src/components/workflow/PipelineRunner.tsx` | 수정 |
| `apps/desktop/src/components/workflow/ConfirmLeaveModal.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RunInputsDialog.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RunDetailPanel.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RevisionPanel.tsx` | 수정 |
| monorepo versions | 0.3.48 |
