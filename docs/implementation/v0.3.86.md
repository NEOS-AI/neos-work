# v0.3.86 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **memory key control-char reject**, **block
implementationType normalize** (Task 12), **workflow JSON parse safety**,
**deploy host http(s) URL**, **skill discovery path hygiene**, **runtime port
clamp**, **webhook body size cap** (Task 13), and **browser tools non-throwing
errors**.

기준: v0.3.85

---

## Changes

### Task 1: Memory tools

- Reject `\0` / CR / LF in remember/forget keys
- Drop control-char tags during normalize

### Task 2: Block registry / BlockNode (Task 12)

- `normalizeImplementationType`: native|prompt|skill; unknown → native
- Registry meta + BlockNode execution use shared normalizer
- Case-insensitive `NATIVE` / `PROMPT` / `skill`

### Task 3: Workflows DB

- Safe JSON parse for nodes/edges (non-array → `[]`) and nodeResults (non-object → `{}`)
- `updateWorkflow`: typeof guards; non-array nodes/edges → `[]`

### Task 4: Deploy helpers (Task 8)

- `safeDeployHostUrl`: bare host → `https://`; reject file:/javascript:
- Vercel/Cloudflare status URLs use the helper

### Task 5: Skill discovery

- Skip local scan when workspacePath has control characters
- Hidden `.*.md` files already skipped

### Task 6: Runtime context

- `normalizeListenPort`: clamp 1–65535; invalid → fallback
- Trim auth token on set

### Task 7: Webhooks (Task 13)

- Cap body at 1 MiB (Content-Length + raw text)
- JSON arrays/primitives → empty trigger inputs

### Task 8: Browser tools

- Page API failures return `{ success: false }` instead of throwing

### Task 9: Unit tests

- memory key/tags control chars
- implementationType normalize + case
- safeDeployHostUrl
- runtime port clamp
- discovery control-char path / hidden files
- browser non-throwing failures

### Task 10: Version

- monorepo **0.3.85 → 0.3.86**
- health / server banner / web_search User-Agent

---

## File Index

| 파일 | 상태 |
|---|---|
| `packages/core/src/tools/memory.ts` | 수정 |
| `packages/core/src/tools/memory.test.ts` | 수정 |
| `packages/core/src/skills/discovery.ts` | 수정 |
| `packages/core/src/skills/discovery.test.ts` | 수정 |
| `packages/workflow-engine/src/blocks/registry.ts` | 수정 |
| `packages/workflow-engine/src/blocks/registry.test.ts` | 수정 |
| `packages/workflow-engine/src/nodes/block.ts` | 수정 |
| `packages/workflow-engine/src/nodes/block.test.ts` | 수정 |
| `packages/browser-tool/src/tools.ts` | 수정 |
| `packages/browser-tool/src/tools.test.ts` | 수정 |
| `apps/server/src/db/workflows.ts` | 수정 |
| `apps/server/src/lib/deploy.ts` | 수정 |
| `apps/server/src/lib/deploy.test.ts` | 수정 |
| `apps/server/src/lib/runtime-context.ts` | 수정 |
| `apps/server/src/lib/runtime-context.test.ts` | 수정 |
| `apps/server/src/routes/webhooks.ts` | 수정 |
| monorepo versions | 0.3.86 |
