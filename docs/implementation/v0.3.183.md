# v0.3.183 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **Settings MCP OAuth revoke/connect server-id
gates** via `safeEntityId` so control-char MCP ids never hit OAuth/start/revoke
APIs (toggle/delete already gated in v0.3.181).

기준: v0.3.182

---

## Changes

### Task 1: handleOAuthConnect

- Gate `oauthModal.serverId` with `safeEntityId` before start
- Alert `MCP server id contains invalid control characters`
- Use sanitized id for start + post-connect status poll

### Task 2: handleOAuthRevoke

- Gate `serverId` with `safeEntityId` before revoke
- Update oauth status map under safe id only

### Task 3: Unit tests

- Dirty MCP id: Disconnect/revoke, toggle, delete all alert and skip API
- **45 passed** (Settings suite)

### Task 4: Version

- monorepo **0.3.182 → 0.3.183**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/pages/Settings.tsx` | 수정 |
| `apps/desktop/src/pages/Settings.test.tsx` | 수정 |
| monorepo versions | 0.3.183 |
