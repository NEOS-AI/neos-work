# v0.3.157 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **Settings save/MCP/OAuth failure alerts**,
**WorkflowEditor save/rename/pre-run save failure alerts**, and
**PipelineRunner resume API failure surface**.

기준: v0.3.156

---

## Changes

### Task 1: Settings

- Default provider/model save: scrubbed alert on `!ok`
- API key save: check `res.ok`; scrubbed alert; no false "Saved!"
- Simple key inputs (Deploy/workflow secrets): check `res.ok`; scrubbed alert;
  do not mark hasSaved on failure
- MCP create: scrubbed alert; form stays open
- MCP OAuth start: scrubbed alert; modal stays open
- MCP OAuth revoke: scrubbed alert; keep connected badge on failure

### Task 2: WorkflowEditor

- Manual save: scrubbed alert on `!ok`
- Inline rename: scrubbed alert on `!ok`
- Pre-run autosave: scrubbed alert and **abort run** when save fails

### Task 3: PipelineRunner

- `resumePlugin` failure → clear waiting, set scrubbed `run.failed` (shown in UI)

### Task 4: Unit tests

- Settings: MCP create scrub, API key save fail, OAuth start/revoke fail,
  simple key save fail
- WorkflowEditor: save / rename / pre-run save failures
- PipelineRunner: resume API error as failure message
- **74 passed** across related suites

### Task 5: Version

- monorepo **0.3.156 → 0.3.157**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/pages/Settings.tsx` | 수정 |
| `apps/desktop/src/pages/Settings.test.tsx` | 수정 |
| `apps/desktop/src/pages/WorkflowEditor.tsx` | 수정 |
| `apps/desktop/src/pages/WorkflowEditor.test.tsx` | 수정 |
| `apps/desktop/src/components/workflow/PipelineRunner.tsx` | 수정 |
| `apps/desktop/src/components/workflow/PipelineRunner.test.tsx` | 수정 |
| monorepo versions | 0.3.157 |
