# v0.3.153 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **media/harness/plugins publicErrorMessage**,
**plugin-runner LLM stage scrub**, **deploy provider error scrub**, and
**core tools + workflow nodes scrubErrorMessage on catch**.

기준: v0.3.152

---

## Changes

### Task 1: Server routes

- Media image/audio/generate/delete catches → `publicErrorMessage`
- Harness create + plugins route catches → `publicErrorMessage`

### Task 2: Plugin runner / deploy

- Anthropic / OpenAI stage catch paths scrub via `scrubErrorMessage`
- Deploy `networkError` + provider API body messages scrubbed before rethrow

### Task 3: Core tools / workflow nodes

- filesystem / memory / shell / web-search catch → `scrubErrorMessage`
- Agent / deploy / discord / media / slack / web-search nodes scrub catch messages
- Coding + finance blocks scrub operation failures

### Task 4: LLM adapters

- Anthropic / Google / OpenAI chat catch paths scrub error chunks via `scrubErrorMessage`
- OpenAI non-OK body detail scrubbed before SSE error content

### Task 5: Version

- monorepo **0.3.152 → 0.3.153**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/server/src/routes/media.ts` | 수정 |
| `apps/server/src/routes/harness.ts` | 수정 |
| `apps/server/src/routes/plugins.ts` | 수정 |
| `apps/server/src/lib/plugin-runner.ts` | 수정 |
| `apps/server/src/lib/deploy.ts` | 수정 |
| `packages/core/src/tools/*` | 수정 |
| `packages/workflow-engine/src/nodes/*` | 수정 |
| `packages/core/src/llm/*` | 수정 |
| monorepo versions | 0.3.153 |
