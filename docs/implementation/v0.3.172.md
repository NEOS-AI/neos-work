# v0.3.172 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **ModeSelection control-char connect alerts**,
plus residual unit coverage for ArtifactPreview copy race, DesignSystems delete
confirm, coding spawn abort, and concurrent OR-gate empty id.

기준: v0.3.171

---

## Changes

### Task 1: ModeSelection control-char feedback

- Remote URL with control chars → `window.alert('URL contains invalid control characters')` (no silent no-op)
- Bearer token with control chars → `window.alert('Token contains invalid control characters')`
- Still blocks connect / sessionStorage write

### Task 2: Residual test hardening

- ArtifactPreview: wait for Copy enabled before fail-path click
- DesignSystems: force `confirm` true for delete-throw test; assert API call
- coding-spawn: abort signal kills python `code_eval` and `git_diff` spawns
- executor-concurrent: empty branch id → OR gate fails closed

### Task 3: Unit tests

- ModeSelection: control-char URL/token alert assertions
- ArtifactPreview, DesignSystems, coding-spawn, executor-concurrent suites green
- **12 + 42 + 10 related tests** across suites

### Task 4: Version

- monorepo **0.3.171 → 0.3.172**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/pages/ModeSelection.tsx` | 수정 |
| `apps/desktop/src/pages/ModeSelection.test.tsx` | 수정 |
| `apps/desktop/src/components/workflow/ArtifactPreview.test.tsx` | 수정 |
| `apps/desktop/src/pages/DesignSystems.test.tsx` | 수정 |
| `packages/workflow-engine/src/blocks/coding/coding-spawn.test.ts` | 수정 |
| `packages/workflow-engine/src/executor-concurrent.test.ts` | 수정 |
| `docs/implementation/v0.3.172.md` | 신규 |
| monorepo versions | 0.3.172 |
