# v0.8.6 Implementation

## Summary

**Harden (1) + product hooks (2):** CI on Node 22, server vitest isolation,
release notes, multi-replica compose, and **peer canvas outlines** wired from
collab awareness via bridge `neos.measure`.

기준: v0.8.5  

---

## Task map

| Item | Status |
|---|---|
| Node 22 full server tests + vitest `NEOS_DATA_DIR` isolation | **Done** |
| `.github/workflows/ci.yml` (Node 22) | **Done** |
| `docs/releases/v0.8.5.md` | **Done** |
| `deploy/docker-compose.multi.yml` | **Done** |
| Bridge `neos.measure` / `measure-result` | **Done** |
| DesignEditor `peerAwareness` → measured frames | **Done** |
| Web + desktop wire `peerSelections` → `peerAwareness` | **Done** |
| Version **0.8.6** | **Done** |

---

## Peer outlines

```text
collab selection.changed → peerSelections (host)
        │
        ▼
DesignEditor peerAwareness
        │ post neos.measure(selectors)
        ▼
preview bridge → neos.measure-result(bboxes)
        │
        ▼
CanvasOverlay peerFrames (dashed colorHint)
```

- Only peers whose `path` matches the open file (or empty path) are measured.  
- Requires canvas overlay flag + HTML bridge (not JSX parse layers).  

---

## Ops

```bash
# Multi-replica local stack
docker compose -f deploy/docker-compose.multi.yml up -d --build
```

See [`docs/ops/multi-replica-collab.md`](../../ops/multi-replica-collab.md).

---

## CI

`.github/workflows/ci.yml`: install → typecheck → inventory:check → test → e2e:smoke on Node 22.  
