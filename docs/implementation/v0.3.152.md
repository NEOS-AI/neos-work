# v0.3.152 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **ToolRegistry empty-error scrub fix**,
**workflow-engine / plugin-runner / scheduler / webhooks / run-route error scrub
via `scrubErrorMessage`**, and **core helper/orchestrator residual coverage**.

기준: v0.3.151

---

## Changes

### Task 1: ToolRegistry execute

- Scrub returned `error` strings as before
- If scrub empties the string:
  - `success: false` → fallback `"Tool execution failed"`
  - `success: true` → drop `error` field entirely (do not invent a failure)

### Task 2: Executor / pipeline / run SSE errors

- `executeWorkflow` node/run failures use `scrubErrorMessage` (control strip + 4k cap)
- `plugin-runner` pipeline.failed path scrubs thrown messages
- Workflow `POST /:id/run` and webhook fire catch paths scrub before SSE/DB
- Routine scheduler failure path scrubs before persisting failed runs

### Task 3: Unit tests

- `scrubErrorMessage` cases in `base.test.ts`
- Registry control-only failure fallback + success empty-error hygiene
- Orchestrator step_error / fullText cap / fat tool_use truncation
- Healing invalid-JSON skip path; residual OpenAI adapter cases
- Rebuild `@neos-work/core` dist so dependents resolve the new export

### Task 4: Version

- monorepo **0.3.151 → 0.3.152**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `packages/core/src/tools/registry.ts` | 수정 |
| `packages/core/src/tools/registry.test.ts` | 수정 |
| `packages/core/src/tools/base.test.ts` | 추가 |
| `packages/core/src/agent/orchestrator.test.ts` | 수정 |
| `packages/core/src/agent/healing.test.ts` | 수정 |
| `packages/core/src/llm/openai.test.ts` | 수정 |
| `packages/workflow-engine/src/executor.ts` | 수정 |
| `apps/server/src/lib/plugin-runner.ts` | 수정 |
| `apps/server/src/lib/routine-scheduler.ts` | 수정 |
| `apps/server/src/routes/workflow.ts` | 수정 |
| `apps/server/src/routes/webhooks.ts` | 수정 |
| monorepo versions | 0.3.152 |
