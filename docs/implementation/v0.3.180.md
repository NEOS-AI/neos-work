# v0.3.180 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **Skills action id gates** (toggle/delete/upgrade)
and **Media filename gates** (delete/preview) so control-char ids never hit APIs.

기준: v0.3.179

---

## Changes

### Task 1: Skills — toggle / delete / upgrade

- Control-char / blank skill id →
  `window.alert('Skill id contains invalid control characters')`
- Trim safe id before API; never call toggle/delete/upgrade with hostile ids
- Use `window.alert` / `window.confirm` consistently

### Task 2: Media — delete / preview

- Delete: control-char / blank filename →
  `window.alert('Filename contains invalid control characters')` (no confirm/API)
- Preview fetch: control-char / blank filename → setError banner; no blob fetch

### Task 3: Unit tests

- Skills: dirty-id toggle/delete/upgrade alerts
- Media: delete alert + preview error surface
- **40 passed** across Skills + Media suites

### Task 4: Version

- monorepo **0.3.179 → 0.3.180**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/pages/Skills.tsx` | 수정 |
| `apps/desktop/src/pages/Skills.test.tsx` | 수정 |
| `apps/desktop/src/pages/Media.tsx` | 수정 |
| `apps/desktop/src/pages/Media.test.tsx` | 수정 |
| `docs/implementation/v0.3.180.md` | 신규 |
| monorepo versions | 0.3.180 |
