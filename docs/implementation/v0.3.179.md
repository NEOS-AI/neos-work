# v0.3.179 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **Plugins Run control-char id gate** and
**PipelineRunner resume invalid-id failure surface** (no silent resume no-op).

기준: v0.3.178

---

## Changes

### Task 1: Plugins — Run button

- Control-char plugin id → `window.alert('Plugin id contains invalid control characters')`
- Blank / overlong (>100) id → `window.alert('Plugin id is missing or too long')`
- Still blocks opening `PipelineRunner`

### Task 2: PipelineRunner — handleResume

- Invalid stage / run / plugin id after sanitize → set pipeline
  `failed: 'Resume failed: invalid stage, run, or plugin id'` (clears waiting)
- No silent return; never calls `resumePlugin` with hostile ids

### Task 3: Unit tests

- Plugins: control-char + overlong Run alerts; runner not opened
- PipelineRunner: mid-wait control-char plugin id → resume error surface
- **29 passed** across related suites

### Task 4: Version

- monorepo **0.3.178 → 0.3.179**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/pages/Plugins.tsx` | 수정 |
| `apps/desktop/src/pages/Plugins.test.tsx` | 수정 |
| `apps/desktop/src/components/workflow/PipelineRunner.tsx` | 수정 |
| `apps/desktop/src/components/workflow/PipelineRunner.test.tsx` | 수정 |
| monorepo versions | 0.3.179 |
