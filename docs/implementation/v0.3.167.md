# v0.3.167 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **clipboard copy feedback + control-char scrub**
for Workflows ID copy, Skills try-prompt Copy, and Sessions markdown CodeBlock.

기준: v0.3.166

---

## Changes

### Task 1: Workflows — copy ID

- Scrub workflow id before `clipboard.writeText` (null-byte / control-char defense)
- Missing clipboard API or write reject → `Copy failed` title + `!` button glyph
- Success still shows `Copied!` / `✓`
- Clear success vs failure state mutually

### Task 2: Skills — try-prompt Copy

- Modal pre displays scrubbed prompt text
- Copy awaits clipboard write; scrub payload before write
- Button feedback: `Copied` / `Copy failed` (timed clear)
- Escape / Close clears copy status

### Task 3: Sessions — CodeBlock

- Fenced code Copy scrubs null bytes / caps length before clipboard
- Await write; missing API or reject → `Copy failed`
- Success → `Copied!`
- Export `CodeBlock` for unit tests

### Task 4: Unit tests

- Workflows: success feedback, fail feedback, scrub before clipboard
- Skills: success, fail, scrub try-prompt
- Sessions CodeBlock: success, fail, scrub, inline (no copy button)
- **82 passed** across related suites

### Task 5: Version

- monorepo **0.3.166 → 0.3.167**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/pages/Workflows.tsx` | 수정 |
| `apps/desktop/src/pages/Workflows.test.tsx` | 수정 |
| `apps/desktop/src/pages/Skills.tsx` | 수정 |
| `apps/desktop/src/pages/Skills.test.tsx` | 수정 |
| `apps/desktop/src/pages/Sessions.tsx` | 수정 |
| `apps/desktop/src/pages/Sessions.test.tsx` | 수정 |
| monorepo versions | 0.3.167 |
