# v0.6.5 Implementation

## Summary

**M5 — Polish / ops closeout.** Ships an optional **Helm chart snippet**, extends
**capability inventory** with v0.6 feature gates, adds **migration notes** for
0.6.x, and completes the v0.6 plan Task 7 checklist.

기준: v0.6.4  
계획: [`PLAN_FOR_V0_6_0.md`](../../plans/PLAN_FOR_V0_6_0.md) M5 / Task 7

---

## Task map

| Plan item | Status |
|---|---|
| Helm snippet (single replica) | **Done** (`deploy/helm/neos-work`) |
| Inventory v0.6 feature gates | **Done** |
| Migration note | **Done** (`docs/migration/v0.6.0.md`) |
| e2e smoke v0.6 docs | **Done** |
| Version **0.6.5** | **Done** |

---

## Helm

```bash
helm upgrade --install neos ./deploy/helm/neos-work \
  --set authToken="$(openssl rand -hex 32)" \
  --set image.tag=0.6.5
```

See `deploy/helm/neos-work/README.md`.

---

## Inventory

`pnpm inventory:check` now requires `catalogs.v06Features` (collab, canvas,
marketplace, ADR, helm, migration, plan files). Gate id: `v06Features`.

---

## Verification

| Check | Result |
|---|---|
| `pnpm inventory:check` | pass |
| `pnpm test:inventory` | pass |
| `pnpm e2e:smoke` | pass |

---

## v0.6 train complete (M0–M5)

| M | Theme | Version |
|---|---|---|
| M0 | Presence | 0.6.0 |
| M1 | Awareness + JSX layers | 0.6.1 |
| M2 | Canvas overlay | 0.6.2 |
| M3 | File locks + ADR | 0.6.3 |
| M4 | Remote marketplace | 0.6.4 |
| M5 | Helm + inventory + migration | **0.6.5** |

Further product work can start a new plan (e.g. CRDT multi-caret, multi-replica collab).  
