# v0.27.0 Implementation — Web Workflow editor v2

## Summary

`apps/web/src/pages/WorkflowEditor.tsx` gains:

- Full palette (control, agent variants, block, delivery)
- Palette filter tabs
- Per-type config (agent worker/mode/provider, block id, trigger inputs, search, slack/discord, media, deploy)
- Run history via `listWorkflowRuns`

Workers and blocks are loaded from the existing web client (`listWorkers`, `listBlocks`).
