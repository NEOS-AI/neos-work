# v0.3.81 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **media filename allow-list module** (Task 7),
**AgentNode SERVER_URL http(s) guard** (Task 14), **test_runner binary
case-insensitive allow-list** (Task 12), **path-safety control-char reject**,
and **WorkflowValidation blank-edge / self-loop trim** (Task 15).

기준: v0.3.80

---

## Changes

### Task 1: Media filename safety (Task 7)

- Shared `isSafeMediaFilename` in `apps/server/src/lib/media-filename.ts`
- Trim; reject blank, `.` / `..`, leading-dot hidden names, path separators
- Used by media routes GET/DELETE and `listMediaFiles`

### Task 2: AgentNode (Task 14)

- `safeServerUrl`: only `http:` / `https:` for memory export base URL
- Non-http schemes (e.g. `file:`) fall back to localhost default
- Strip trailing slashes; trim harness/node system prompts

### Task 3: Coding test_runner (Task 12)

- Case-insensitive allow-list on binary basename (`PNPM`, `/usr/bin/npm`)
- Split command on whitespace with empty-token drop

### Task 4: Path safety

- `validateWorkspacePath`: reject null bytes / CR / LF after trim

### Task 5: WorkflowValidation (Task 15)

- Blank edge endpoints → `dangling_edge`
- Self-loop compares trimmed endpoints
- Duplicate node ids compare trimmed keys
- Edge pair keys use trimmed source/target (skip blanks)

### Task 6: Unit tests

- media filename pad/hidden/traversal
- path-safety control chars
- agent non-http SERVER_URL fallback
- test_runner case-insensitive bin
- validation blank edge endpoints

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/server/src/lib/media-filename.ts` | 신규 |
| `apps/server/src/lib/media-filename.test.ts` | 수정 |
| `apps/server/src/lib/media-generator.ts` | 수정 |
| `apps/server/src/routes/media.ts` | 수정 |
| `apps/server/src/lib/path-safety.ts` | 수정 |
| `apps/server/src/lib/path-safety.test.ts` | 수정 |
| `packages/workflow-engine/src/nodes/agent.ts` | 수정 |
| `packages/workflow-engine/src/nodes/agent.test.ts` | 수정 |
| `packages/workflow-engine/src/blocks/coding/index.ts` | 수정 |
| `packages/workflow-engine/src/blocks/coding/coding.test.ts` | 수정 |
| `apps/desktop/src/components/workflow/WorkflowValidation.ts` | 수정 |
| `apps/desktop/src/components/workflow/WorkflowValidation.test.ts` | 수정 |
| monorepo versions | 0.3.81 |
