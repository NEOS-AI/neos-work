# v0.3.76 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **session provider/thinkingMode normalize**,
**workspace type allow-list**, **deployment status allow-list**, **routine run
status case**, **core web_search/shell input hygiene**, and **Discord webhook
SSRF case-insensitive prefix** (Tasks 3/8/5).

기준: v0.3.75

---

## Changes

### Task 1: Sessions / workspaces

- `createSession`: provider trim + lower-case (blank → anthropic)
- `thinkingMode` allow-list `none|low|medium|high` (unknown → none)
- `createWorkspace`: type allow-list `local|remote` (unknown → local)

### Task 2: Deployments status

- `pending|deploying|success|failed` lower-case allow-list
- create unknown → pending; update unknown → keep previous
- `rowToDeployment` normalizes legacy status values

### Task 3: Routine runs

- `completeRoutineRun`: status trim + lower-case; non-failed → completed

### Task 4: Core tools

- `web_search`: trim API key/query; reject blank query; clamp maxResults 1–10
  (floor; string-safe); User-Agent → `neos-work/0.3.76`
- `run_command`: trim/reject blank command; clamp timeout; trim blank cwd

### Task 5: Discord SSRF (Task 8)

- `DiscordMessageNode` + preflight: case-insensitive webhook prefix check

### Task 6: Unit tests

- session provider/thinking pad/case + workspace type
- deployment status case/unknown
- web_search blank query + maxResults floor/clamp
- shell blank command/cwd
- Discord/preflight case-insensitive webhook host
- completeRoutineRun status case

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/server/src/db/sessions.ts` | 수정 |
| `apps/server/src/db/sessions.test.ts` | 수정 |
| `apps/server/src/db/deployments.ts` | 수정 |
| `apps/server/src/db/deployments.test.ts` | 수정 |
| `apps/server/src/db/routines.ts` | 수정 |
| `apps/server/src/db/routines.test.ts` | 수정 |
| `packages/core/src/tools/web-search.ts` | 수정 |
| `packages/core/src/tools/web-search.test.ts` | 수정 |
| `packages/core/src/tools/shell.ts` | 수정 |
| `packages/core/src/tools/shell.test.ts` | 수정 |
| `packages/workflow-engine/src/nodes/discord.ts` | 수정 |
| `packages/workflow-engine/src/nodes/discord.test.ts` | 수정 |
| `apps/server/src/lib/workflow-preflight.ts` | 수정 |
| `apps/server/src/lib/workflow-preflight.test.ts` | 수정 |
| monorepo versions | 0.3.76 |
