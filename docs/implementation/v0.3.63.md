# v0.3.63 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **Routine cron schedule validation** (Task 2),
**CLI spawn prompt/env hygiene** (Task 3), **plugin getPlugin id trim** (Task 5),
**MCP OAuth token store blank/sanitize** (Task 9), and **rate-limit key trim**.

기준: v0.3.62

---

## Changes

### Task 1: Routine schedule validation (Task 2)

- Create/update: `node-cron.validate(schedule)` after trim
- Invalid expressions → **400** `Invalid cron schedule` (no silent scheduler skip)

### Task 2: CLI spawn hygiene (Task 3)

- Trim prompt; blank/whitespace → reject `prompt is required`
- Trim `workflowId` / `runId` / `serverUrl` / `authToken` / `cwd`

### Task 3: Plugin store (Task 5)

- `getPlugin(id)` trims id; blank → null

### Task 4: MCP OAuth token store (Task 9)

- Blank serverId rejected; path sanitize after trim
- Blank accessToken rejected; trim tokens on save
- Status: empty access → not connected; safe `tokenTail`

### Task 5: Rate limit + media list

- Rate limiter trims keys; blank key denied (no shared bucket)
- `listMediaFiles` clamps limit 1–500 via single `capped` value

### Task 6: Unit tests

- Routines invalid cron create/update
- Rate limit blank/trimmed keys
- Plugin getPlugin blank id
- MCP oauth store blank serverId/accessToken
- CLI spawn blank prompt

### Also from v0.3.63 review

- **ensureCliWorkspace**: sanitize `runId` (block path traversal); blank → throw
- **upgradeSkillToPlugin**: trim skillDirName / name / description before sanitize
- **loadToken**: normalize whitespace on disk tokens; blank access → null
- **settings** get/set/delete: trim key; blank key no-op
- **design-system create**: trim name/description
- **memory getMemory** / **resumeRun** / **html-artifact**: id trim
- **media list**: trim `?limit=` before clamp
- **coding blocks**: code/file size caps; path trim; file-is-file check

### Task 7: Unit tests (review)

- ensureCliWorkspace traversal / blank / trim
- upgradeSkillToPlugin blank dir name + field trim
- media list limit trim; coding size bounds

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/server/src/routes/routines.ts` | 수정 |
| `apps/server/src/routes/routines.test.ts` | 수정 |
| `apps/server/src/lib/cli-agents.ts` | 수정 |
| `apps/server/src/lib/cli-agents.test.ts` | 수정 |
| `apps/server/src/lib/plugin-store.ts` | 수정 |
| `apps/server/src/lib/plugin-store.test.ts` | 수정 |
| `apps/server/src/lib/mcp-oauth-store.ts` | 수정 |
| `apps/server/src/lib/mcp-oauth-store.test.ts` | 수정 |
| `apps/server/src/lib/rate-limit.ts` | 수정 |
| `apps/server/src/lib/rate-limit.test.ts` | 수정 |
| `apps/server/src/lib/media-generator.ts` | 수정 |
| monorepo versions | 0.3.63 |
