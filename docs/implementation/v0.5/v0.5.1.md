# v0.5.1 Implementation

## Summary

**PLAN_FOR_V0_5_0 Task 1 desktop shell + editor host (E0).** Wired Design
Projects into the Tauri desktop app: list/create/import-path, workspace with
Files tree + Preview/Code/Split host (textarea + sandboxed iframe), save with
revisions via existing server API, and nav dual-surface entry.

기준: v0.5.0  
참고: `PLAN_FOR_V0_5_0` Task 1 deferred UI; Task 1b CodeMirror package still
pending.

---

## Task map

| Plan item | Status |
|---|---|
| Desktop: Projects list page | **Done** |
| Desktop: Project workspace shell (files + editor host slot) | **Done** |
| Engine client project/file/revision APIs | **Done** |
| Sidebar / routes / i18n (en/ko) | **Done** |
| Optional folder import via `baseDir` path field | **Done** (path string; native Tauri dialog deferred — no dialog plugin yet) |
| ZIP / Claude Design import routes | Still deferred |
| Task 1b `@neos-work/design-editor` + CodeMirror 6 | Pending (M2) |

---

## Changes

### Desktop client

- `EngineClient`: `listProjects`, `getProject`, `createProject`, `updateProject`,
  `deleteProject`, `listProjectFiles`, `readProjectFile`, `writeProjectFile`,
  `mkdirProjectPath`, `listProjectRevisions`, `restoreProjectRevision`
- Relative path segment encoding + traversal reject on client
- `Projects` page: search, create modal (name + optional baseDir import),
  delete, navigate to workspace
- `ProjectWorkspace`: Files tree, Preview / Code / Split, dirty state,
  Cmd/Ctrl+S, beforeunload guard, sandboxed `srcDoc` preview
- Routes: `/projects`, `/projects/:id`
- Sidebar: **Projects** nav item (after Workflows)

### i18n

- `nav.projects` + `project.*` keys (en/ko)

### Version

- monorepo **0.5.0 → 0.5.1**
- health / banner / web_search UA / MCP client

---

## Verification

| Check | Result |
|---|---|
| Desktop suite | **1002** passed (Projects + ProjectWorkspace + engine + Sidebar) |
| Server suite | **810** passed (project API unchanged) |
| Shared | **37** passed |
| desktop `tsc --noEmit` | green |

---

## File index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/lib/engine.ts` | 수정 |
| `apps/desktop/src/lib/engine.test.ts` | 수정 |
| `apps/desktop/src/pages/Projects.tsx` | 신규 |
| `apps/desktop/src/pages/Projects.test.tsx` | 신규 |
| `apps/desktop/src/pages/ProjectWorkspace.tsx` | 신규 |
| `apps/desktop/src/pages/ProjectWorkspace.test.tsx` | 신규 |
| `apps/desktop/src/App.tsx` | 수정 |
| `apps/desktop/src/components/Sidebar.tsx` | 수정 |
| `apps/desktop/src/components/Sidebar.test.tsx` | 수정 |
| `packages/ui/src/i18n/locales/en/common.json` | 수정 |
| `packages/ui/src/i18n/locales/ko/common.json` | 수정 |
| monorepo versions | **0.5.1** |

---

## Next

1. **M2 / Task 1b** — `@neos-work/design-editor` with CodeMirror 6, replace textarea
2. Task 2–3 — `agent-runtime`, `/api/runs`, project chat SSE
3. ZIP export/import + Tauri folder dialog plugin
