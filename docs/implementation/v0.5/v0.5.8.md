# v0.5.8 Implementation

## Summary

**PLAN_FOR_V0_5_0 Task 1c residual + Task 5 foundation.** Project workspace
gains a **Context** side tab (read-only DESIGN.md + tokens.css strip, link
design system on the project). Design System store gains OD-compatible
`od-design-system-project/v1` manifest parse, **bundled catalog** (≥2) under
repo `design-systems/`, user>bundled name shadowing, tokens API, and bundled
write protection.

기준: v0.5.7  
참고: Skills package protocol landed in v0.5.7; this slice is design-system
parity + editor context strip.

---

## Task map

| Plan item | Status |
|---|---|
| DESIGN.md / tokens side strip (Project workspace) | **Done** |
| Link project ↔ designSystemId from UI | **Done** |
| manifest schema `od-design-system-project/v1` loader | **Done** |
| provenance fields on manifest | **Done** |
| tokens.css read path | **Done** (`GET …/tokens`) |
| bundled design-systems ≥ 2 | **Done** (neos-default, minimal-mono) |
| user shadows bundled by name | **Done** |
| bundled read-only (no PUT/delete) | **Done** |
| components.html preview UI | Deferred |
| Project run auto-inject DESIGN.md | **Done** (`assembleDesignContextPrompt` on POST /api/runs) |

---

## Bundled catalog

| Package | Notes |
|---|---|
| `neos-default` | product indigo tokens + DESIGN.md |
| `minimal-mono` | wireframe mono system |

---

## API

| Method | Path | Notes |
|---|---|---|
| GET | `/api/design-systems` | user + bundled, shadowed |
| GET | `/api/design-systems/:id/tokens` | **new** tokens.css |
| PUT | `/api/design-systems/:id/content` | 403 if bundled |

---

## Desktop

- ProjectWorkspace side tab **Context**: select DS, preview DESIGN.md + tokens
- EngineClient: `getDesignSystemTokens`
- i18n en/ko: `project.context*`, `project.ds*`

---

## Version

- monorepo **0.5.7 → 0.5.8**

---

## Verification

| Check | Result |
|---|---|
| design-system-store | **24** passed |
| design-systems routes | **9** passed |
| ProjectWorkspace | **4** passed |
| desktop `tsc --noEmit` | green |

---

## File index

| 파일 | 상태 |
|---|---|
| `apps/server/src/lib/design-system-store.ts` | 수정 |
| `apps/server/src/lib/design-system-store.test.ts` | 수정 |
| `apps/server/src/routes/design-systems.ts` | 수정 |
| `design-systems/**` | 신규 |
| `apps/desktop/src/pages/ProjectWorkspace.tsx` | 수정 |
| `apps/desktop/src/pages/ProjectWorkspace.test.tsx` | 수정 |
| `apps/desktop/src/lib/engine.ts` | 수정 |
| `packages/ui/src/i18n/locales/{en,ko}/common.json` | 수정 |
| monorepo versions | **0.5.8** |

---

## Next

1. Inject project designSystem DESIGN.md into `POST /api/runs` prompts  
2. Skill open-folder + package detail drawer  
3. Task 6 plugin-runtime  
4. ZIP export/import + Tauri folder dialog  
