# v0.5.12 Implementation

## Summary

**PLAN_FOR_V0_5_0 Task 6 deepen.** Plugin pipeline runs **pin atom registry
snapshots**, enforce **capability deny** (`capabilityGates` /
`PLUGIN_DENY_CAPS`), and prefer **registry CLI** for execute stages when
`PLUGIN_CLI_AGENT` is set. Bundled **marketplace stubs** under repo `plugins/`
(`_official/*`, `community/*`) are discovered with user-skill shadowing.

기준: v0.5.11  
참고: Atom registry package landed in v0.5.11; this wires apply/run + catalog.

---

## Task map

| Plan item | Status |
|---|---|
| Snapshot pin on plugin run | **Done** (`pipeline.started.snapshotId`) |
| Capability deny before run | **Done** |
| Stage → atom mapping | **Done** (`collectAtomIdsForPipeline`) |
| Prefer CLI agent spawn on execute | **Done** (`PLUGIN_CLI_AGENT=cli-*`) |
| Bundled `plugins/` marketplace stubs | **Done** (3 packages) |
| listPlugins scans marketplace + user | **Done** (channel field) |
| Marketplace UI filters | Partial (API `meta.channels` only) |
| GenUI full surfaces | Deferred |

---

## Bundled plugins

| Id | Channel | Notes |
|---|---|---|
| `landing-gen` | official | discovery HITL → plan → execute → critique |
| `code-critique` | official | staged code review |
| `hello-plugin` | community | minimal stub |

---

## Settings

| Key | Effect |
|---|---|
| `PLUGIN_CLI_AGENT` | Registry agent id for execute stages (e.g. `cli-claude`) |
| `PLUGIN_DENY_CAPS` | Comma-separated capability denies |

---

## Version

- monorepo **0.5.11 → 0.5.12**

---

## Verification

| Check | Result |
|---|---|
| plugin-runtime | **8** passed |
| plugin-runner | **27** passed |
| plugin-store | **25** passed |
| plugins routes | **18** passed |

---

## File index

| 파일 | 상태 |
|---|---|
| `packages/plugin-runtime/src/stage-atoms.ts` | 신규 |
| `packages/plugin-runtime/src/index.ts` | 수정 |
| `packages/plugin-runtime/src/index.test.ts` | 수정 |
| `apps/server/src/lib/plugin-runner.ts` | 수정 |
| `apps/server/src/lib/plugin-store.ts` | 수정 |
| `apps/server/src/lib/plugin-store.test.ts` | 수정 |
| `apps/server/src/routes/plugins.ts` | meta.channels |
| `plugins/**` | 신규 stubs |
| monorepo versions | **0.5.12** |

---

## Next

1. Tauri folder dialog for project baseDir  
2. Plugins marketplace UI channel filter  
3. Media multi-provider / live artifacts (M4)  
4. `apps/cli` headless surface (M5)  
