# v0.5.11 Implementation

## Summary

**PLAN_FOR_V0_5_0 Task 4 residual + Task 6 foundation.** Skills page gains a
**package detail drawer** (source, version, package label, path, triggers,
examples, assets/references, example prompt). New package
`@neos-work/plugin-runtime` provides a **≥15 builtin atom registry**, capability
deny checks, and in-memory snapshot pin. Server exposes `GET /api/plugins/atoms`.

기준: v0.5.10  
참고: Skill package protocol (v0.5.7); plugin store/runner remain in server for
now — runtime package owns atom contracts.

---

## Task map

| Plan item | Status |
|---|---|
| Skill package detail drawer | **Done** |
| Skills list assets/references fields | **Done** (sanitized basenames) |
| `packages/plugin-runtime` scaffold | **Done** |
| Atom registry ≥ 12 | **Done** (15 builtins) |
| Snapshot pin (in-memory) | **Done** |
| Capability deny helper | **Done** |
| `GET /api/plugins/atoms` | **Done** |
| apply/run prefer CLI spawn | Deferred (existing plugin-runner) |
| Local marketplace UI | Deferred |
| GenUI surfaces full | Deferred (atom ids reserved) |

---

## Builtin atoms (sample)

`prompt.system`, `prompt.user`, `tool.shell`, `tool.filesystem`, `tool.web_search`,
`transform.json_parse`, `transform.markdown_extract`, `gate.capability`,
`gate.hitl`, `genui.form`, `genui.choice`, `genui.confirm`, `editor.apply_patch`,
`media.generate_image`, `deploy.preflight`

---

## Version

- monorepo **0.5.10 → 0.5.11**

---

## Verification

| Check | Result |
|---|---|
| plugin-runtime unit | **5** passed |
| plugins routes | **18** passed |
| Skills page | **27** passed |
| desktop `tsc` | green |

---

## File index

| 파일 | 상태 |
|---|---|
| `packages/plugin-runtime/**` | 신규 |
| `apps/server/package.json` | dep plugin-runtime |
| `apps/server/src/routes/plugins.ts` | `/atoms` |
| `apps/server/src/routes/plugins.test.ts` | 수정 |
| `apps/server/src/routes/skills.ts` | assets/references |
| `apps/desktop/src/pages/Skills.tsx` | detail drawer |
| `apps/desktop/src/pages/Skills.test.tsx` | 수정 |
| `apps/desktop/src/lib/engine.ts` | SkillData examples |
| monorepo versions | **0.5.11** |

---

## Next

1. Tauri folder dialog for project baseDir  
2. Plugin apply pipeline use atom registry + snapshot pin on run  
3. Bundled `plugins/` marketplace stubs  
4. Media multi-provider / live artifacts (M4)  
