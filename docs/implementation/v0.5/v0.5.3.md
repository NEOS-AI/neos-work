# v0.5.3 Implementation

## Summary

**PLAN_FOR_V0_5_0 Task 2 + Task 3 foundation.** Introduces
`@neos-work/agent-runtime`: ≥12 coding-agent CLI defs, detect/launch/cancel,
text+jsonl parsers, in-memory run registry, and `editContext` prompt assembly.
Server exposes expanded `/api/cli-agents` catalog and `/api/runs` (create dry-run,
events, cancel).

기준: v0.5.2  
참고: Full CLI spawn for all 12 + BYOK project chat still incremental.

---

## Task map

| Plan item | Status |
|---|---|
| Package `@neos-work/agent-runtime` scaffold | **Done** |
| Def schema + ≥12 CLI defs | **Done** (13 defs) |
| detect + version probe + path overrides | **Done** |
| launch policies (argv / stdin) | **Done** |
| parsers text + jsonl | **Done** |
| cancel helper | **Done** |
| `routes/cli-agents` catalog + detailed detect | **Done** |
| Workflow AgentNode → registry N | Deferred (still uses legacy spawn for 3) |
| Run registry in-memory | **Done** |
| `POST/GET /api/runs`, events, cancel | **Done** |
| `editContext` assembly | **Done** |
| Live CLI/BYOK execute on run | Deferred (`dryRun` / no agentId → succeeded deferred) |
| SSE Last-Event-ID stream | Deferred (poll events for now) |

---

## CLI catalog (13)

claude, codex, gemini, opencode, cursor-agent, aider, copilot, qwen, kimi,
grok, devin, amp, continue (`cn`)

---

## API

| Method | Path | Notes |
|---|---|---|
| GET | `/api/cli-agents` | available only (compat) |
| GET | `/api/cli-agents?detailed=1` | all defs + detect status |
| GET | `/api/cli-agents/catalog` | defs without probe |
| POST | `/api/runs` | create; `dryRun` or missing agent → terminal succeeded |
| GET | `/api/runs?projectId=` | list |
| GET | `/api/runs/:id` | get |
| GET | `/api/runs/:id/events?after=` | event buffer |
| POST | `/api/runs/:id/cancel` | abort |

---

## Version

- monorepo **0.5.2 → 0.5.3**

---

## Verification

| Check | Result |
|---|---|
| agent-runtime unit | **5** passed |
| server runs + cli-agents | **7** passed |
| package build | green |

---

## File index

| 파일 | 상태 |
|---|---|
| `packages/agent-runtime/**` | 신규 |
| `apps/server/src/routes/runs.ts` | 신규 |
| `apps/server/src/routes/runs.test.ts` | 신규 |
| `apps/server/src/routes/cli-agents.ts` | 수정 |
| `apps/server/src/routes/cli-agents.test.ts` | 수정 |
| `apps/server/src/index.ts` | mount + version |
| `apps/server/package.json` | dep agent-runtime |
| monorepo versions | **0.5.3** |

---

## Next

1. Wire `spawnCliAgent` / BYOK through run registry for real execute  
2. ProjectWorkspace chat panel → `POST /api/runs` + events poll/SSE  
3. Refactor workflow AgentNode to use registry launch for N CLIs  
4. Task 1c Layers / Inspect  
