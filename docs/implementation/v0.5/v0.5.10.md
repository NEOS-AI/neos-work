# v0.5.10 Implementation

## Summary

**PLAN_FOR_V0_5_0 Task 1 archive UI + Task 7 memory inject.** Desktop Projects
page gains **Import ZIP** / **Export ZIP** for `neos-project` archives. Project
chat runs inject **enabled Agent Memory** into the assembled prompt (alongside
design system + comments).

기준: v0.5.9  
참고: ZIP server APIs landed in v0.5.9; this exposes them in the UI and closes
memory injection for project chat.

---

## Task map

| Plan item | Status |
|---|---|
| Desktop ZIP export UI | **Done** |
| Desktop ZIP import (file picker) | **Done** |
| Tauri native folder dialog | Deferred (path string import remains) |
| Memory prompt inject for project runs | **Done** |
| Memory SSE / extraction LLM | Deferred |
| Agent node memory path | Already via AgentNode export fetch |

---

## Desktop

- Projects header: **Import ZIP** → hidden `<input type="file" accept=.zip>`
- Project card: **Export ZIP** → blob download via EngineClient
- Errors surface in alert banner; import navigates to new project workspace

## Server

- `POST /api/runs` with `projectId`: appends `## Agent Memory` from `exportMemories()`
- `run.started.memory` boolean flag

## Version

- monorepo **0.5.9 → 0.5.10**

---

## Verification

| Check | Result |
|---|---|
| Projects page | **7** passed |
| runs routes | **16** passed |

---

## File index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/pages/Projects.tsx` | 수정 |
| `apps/desktop/src/pages/Projects.test.tsx` | 수정 |
| `apps/server/src/routes/runs.ts` | 수정 |
| `apps/server/src/routes/runs.test.ts` | 수정 |
| `packages/ui/src/i18n/locales/{en,ko}/common.json` | 수정 |
| monorepo versions | **0.5.10** |

---

## Next

1. Tauri folder dialog plugin for baseDir import  
2. Skill package detail drawer  
3. Task 6 `plugin-runtime` package  
4. Media multi-provider / live artifacts (M4)  
