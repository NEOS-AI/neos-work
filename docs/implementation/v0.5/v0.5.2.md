# v0.5.2 Implementation

## Summary

**PLAN_FOR_V0_5_0 Task 1b (E1) — Design Editor package.** Adds
`@neos-work/design-editor` with CodeMirror 6 (Q9), Preview iframe + device
presets, dirty/conflict state machine (Keep mine / Take agent / Diff), bridge
types for M3 Inspect, and wires it into the desktop Project workspace.

기준: v0.5.1  
참고: `PLAN_FOR_V0_5_0` Task 1b; Task 1c Layers/Inspect still pending.

---

## Task map

| Plan item | Status |
|---|---|
| Package `@neos-work/design-editor` scaffold | **Done** |
| CodeMirror 6 code pane (not Monaco) | **Done** |
| Preview sandbox iframe + device width presets | **Done** |
| Dirty state machine + conflict Keep/Take/Diff | **Done** |
| Split mode chrome | **Done** |
| ProjectWorkspace integration | **Done** |
| Bridge types for Inspect/Layers (types only) | **Done** |
| file.changed SSE / chat panel / autosave debounce | Deferred (M2 runs) |
| Task 1c Layers tree + Inspect inject | Pending |
| Task 2 agent-runtime | Pending |

---

## Package surface

```ts
// state
createEmptyBuffer, reduceEditorBuffer, isDirty, isConflict, simpleDiffLines
// UI
DesignEditor, CodeEditor, PreviewFrame, toPreviewDocument, DEVICE_PRESETS
// M3 prep
isBridgeInbound, BridgeDomNode, …
```

---

## Desktop

- Depends on `@neos-work/design-editor`
- `ProjectWorkspace` uses buffer reducer + `DesignEditor` (replaces textarea/iframe host)

---

## Version

- monorepo **0.5.1 → 0.5.2**

---

## Verification

| Check | Result |
|---|---|
| design-editor unit tests | **9** passed |
| desktop ProjectWorkspace | **2** passed |
| design-editor `tsc` build | green |

---

## File index

| 파일 | 상태 |
|---|---|
| `packages/design-editor/**` | 신규 |
| `apps/desktop/src/pages/ProjectWorkspace.tsx` | 수정 |
| `apps/desktop/src/pages/ProjectWorkspace.test.tsx` | 수정 |
| `apps/desktop/package.json` | dep + version |
| monorepo versions | **0.5.2** |

---

## Next

1. Task 2 — `@neos-work/agent-runtime` + `/api/runs` + project chat SSE  
2. Task 1c — Inspect bridge inject + Layers tree  
3. Project ZIP export/import + Tauri folder dialog  
