# v0.5.9 Implementation

## Summary

**PLAN_FOR_V0_5_0 Task 5 inject + Task 1 archive.** Project runs now prepend
linked **DESIGN.md** (+ optional `tokens.css`) via
`assembleDesignContextPrompt`, unifying design-system injection with workflow
AgentNode. Design Projects gain **neos-project ZIP** export/import
(`project.json` + `files/**`, symlink/binary blocked).

기준: v0.5.8  
참고: Context strip / bundled DS landed in v0.5.8; this wires DS into runs and
closes Project gate archive.

---

## Task map

| Plan item | Status |
|---|---|
| Inject project designSystem into `POST /api/runs` | **Done** |
| tokens.css optional inject | **Done** (capped) |
| run.started flags `designSystem` | **Done** |
| Project ZIP export | **Done** `GET …/export.zip` |
| Project ZIP import | **Done** `POST …/import.zip` |
| EngineClient export/import | **Done** |
| Desktop ZIP UI (file picker) | Deferred |
| Tauri folder dialog | Deferred |

---

## Prompt assembly order

1. DESIGN CONTEXT (if `project.designSystemId` resolves)  
2. User prompt + editContext  
3. Preview comments  

---

## Archive format (`neos-project`)

```text
project.json   # name, entryFile, designSystemId, meta
README.md
files/**       # text files only, path-sandbox safe
```

Guards: max 50MB, 500 files, no `..` / symlinks / null-byte content.

---

## Version

- monorepo **0.5.8 → 0.5.9**

---

## Verification

| Check | Result |
|---|---|
| agent-runtime | **22** passed |
| project-archive | **3** passed |
| runs routes | **15** passed |
| projects routes | **17** passed |

---

## File index

| 파일 | 상태 |
|---|---|
| `packages/agent-runtime/src/edit-context.ts` | 수정 |
| `packages/agent-runtime/src/index.ts` | 수정 |
| `packages/agent-runtime/src/index.test.ts` | 수정 |
| `apps/server/src/routes/runs.ts` | 수정 |
| `apps/server/src/routes/runs.test.ts` | 수정 |
| `apps/server/src/lib/project-archive.ts` | 신규 |
| `apps/server/src/lib/project-archive.test.ts` | 신규 |
| `apps/server/src/routes/projects.ts` | 수정 |
| `apps/desktop/src/lib/engine.ts` | 수정 |
| monorepo versions | **0.5.9** |

---

## Next

1. Desktop ZIP import/export UI + Tauri folder dialog  
2. Skill package detail drawer  
3. Task 6 `plugin-runtime`  
4. Task 7 memory injection for project chat  
