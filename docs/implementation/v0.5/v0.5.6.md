# v0.5.6 Implementation

## Summary

**PLAN_FOR_V0_5_0 Task 2 residual — Workflow AgentNode → registry N CLIs.**
Widens agent CLI spawn from the legacy 3 (`cli-claude|gemini|codex`) to any
`cli-*` id registered in `@neos-work/agent-runtime`. Server host paths
(workflow run, webhooks, routines) inject `spawnRegistryAgent`. Desktop
NodeConfig / Settings / project chat list the full catalog.

기준: v0.5.5  
참고: Live project `/api/runs` already used registry spawn (v0.5.3–0.5.5).

---

## Task map

| Plan item | Status |
|---|---|
| AgentNode accepts any `cli-*` provider | **Done** (`isCliProvider`) |
| `cliSpawn` type: string agent id | **Done** |
| workflow / webhooks / routines → `spawnRegistryAgent` | **Done** |
| NodeConfigPanel CLI options ≥ registry set | **Done** (13) |
| Settings manual path overrides expanded | **Done** |
| Project chat agent dropdown expanded | **Done** |
| Tests: AgentNode N CLI + existing suite | **Done** |
| Legacy `spawnCliAgent` (3-only helper) | Retained for any residual callers / detect |

---

## Changes

### workflow-engine

- `NodeContext.cliSpawn(cliId: string, …)`
- `isCliProvider()` — `cli-` prefix + non-empty suffix
- AgentNode CLI branch no longer hard-codes 3 ids

### server

- `routes/workflow.ts`, `routes/webhooks.ts`, `lib/routine-scheduler.ts`
  use `spawnRegistryAgent({ agentId, … })`

### desktop

- Agent provider select: full CLI catalog options
- Settings: more `CLI_PATH_*` overrides
- Project workspace chat agent list expanded

---

## Version

- monorepo **0.5.5 → 0.5.6**

---

## Verification

| Check | Result |
|---|---|
| workflow-engine agent tests | **71** passed (incl. N CLI id) |
| routine-scheduler | **19** passed |
| runs routes | **14** passed |
| NodeConfigPanel | **35** passed |
| server `tsc --noEmit` | green |

---

## File index

| 파일 | 상태 |
|---|---|
| `packages/workflow-engine/src/types.ts` | 수정 |
| `packages/workflow-engine/src/nodes/agent.ts` | 수정 |
| `packages/workflow-engine/src/nodes/agent.test.ts` | 수정 |
| `packages/workflow-engine/src/index.ts` | 수정 |
| `apps/server/src/routes/workflow.ts` | 수정 |
| `apps/server/src/routes/webhooks.ts` | 수정 |
| `apps/server/src/lib/routine-scheduler.ts` | 수정 |
| `apps/server/src/lib/routine-scheduler.test.ts` | 수정 |
| `apps/desktop/src/components/workflow/NodeConfigPanel.tsx` | 수정 |
| `apps/desktop/src/pages/Settings.tsx` | 수정 |
| `apps/desktop/src/pages/ProjectWorkspace.tsx` | 수정 |
| monorepo versions | **0.5.6** |

---

## Next

1. DESIGN.md / tokens read-only side strip  
2. Task 4 Skill package protocol (`SKILL.md`)  
3. ZIP export/import + Tauri folder dialog  
4. `apps/cli` / `apps/web` (M5 topology)  
