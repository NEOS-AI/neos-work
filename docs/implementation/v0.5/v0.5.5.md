# v0.5.5 Implementation

## Summary

**PLAN_FOR_V0_5_0 Task 1c remainder — Preview comments + revision timeline.**
Server injects project preview annotations into run prompts. Desktop workspace
gains Chat / Comments / Revisions side tabs; EngineClient covers comment CRUD;
restore reloads the open buffer.

기준: v0.5.4  
참고: Layers/Inspect landed in v0.5.4; this slice closes comment inject + history UX.

---

## Task map

| Plan item | Status |
|---|---|
| Preview comments create/list/delete API (server) | **Done** (v0.5.0) |
| Desktop EngineClient comment CRUD | **Done** |
| Comments UI (selection-scoped add/list/delete) | **Done** |
| Inject comments into next run system prompt | **Done** (`assemblePreviewCommentsPrompt`) |
| Revision list API | **Done** (v0.5.0) |
| Revision timeline UI + restore | **Done** |
| DESIGN.md side panel | Deferred |
| sibling reorder | Deferred |

---

## Server / runtime

- `assemblePreviewCommentsPrompt` in `@neos-work/agent-runtime`
- `POST /api/runs` loads `listPreviewComments(projectId[, filePath])` and appends
  annotations to assembled prompt; `run.started` includes `previewComments` count

## Desktop

- Side panel tabs: **Chat · Comments · Revisions**
- Comments require Layers/Inspect selection + open file
- Revisions list for open path; **Restore** writes via API and reopens buffer
- Save refreshes revision list

## Version

- monorepo **0.5.4 → 0.5.5**

---

## Verification

| Check | Result |
|---|---|
| agent-runtime unit | **19** passed |
| runs routes (incl. comment inject) | **11** passed |
| desktop ProjectWorkspace | **3** passed |
| desktop `tsc --noEmit` | green |

---

## File index

| 파일 | 상태 |
|---|---|
| `packages/agent-runtime/src/edit-context.ts` | 수정 |
| `packages/agent-runtime/src/index.ts` | 수정 |
| `packages/agent-runtime/src/index.test.ts` | 수정 |
| `apps/server/src/routes/runs.ts` | 수정 |
| `apps/server/src/routes/runs.test.ts` | 수정 |
| `apps/desktop/src/lib/engine.ts` | 수정 |
| `apps/desktop/src/pages/ProjectWorkspace.tsx` | 수정 |
| `packages/ui/src/i18n/locales/{en,ko}/common.json` | 수정 |
| monorepo versions | **0.5.5** |

---

## Next

1. DESIGN.md / tokens read-only side strip  
2. Workflow AgentNode → registry N CLIs  
3. Task 4 Skill package protocol (`SKILL.md`)  
4. ZIP export/import + Tauri folder dialog  
