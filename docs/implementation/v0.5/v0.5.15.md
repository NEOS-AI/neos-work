# v0.5.15 Implementation

## Summary

**PLAN_FOR_V0_5_0 Task 9 (M4 Live Artifact full).** Project-scoped **live
artifacts** with template + inputs separation, file sidecar under
`.neos-work/live-artifacts/`, refresh history, user/UI REST API, and agent
**tool-token** routes that derive `projectId` from the token only (body
override → **403**). Desktop Project workspace gains a **Live** side tab.

기준: v0.5.14  
참고: Shared `LiveArtifact` type existed since v0.5.0; this lands store + API + UI.

---

## Task map

| Plan item | Status |
|---|---|
| DB + file sidecar model | **Done** (`live_artifacts`, `live_artifact_refreshes`) |
| UI CRUD/preview/refresh | **Done** (workspace Live tab) |
| `/api/tools/live-artifacts/*` tool-token | **Done** |
| projectId override → 403 | **Done** (`assertNoScopeOverride`) |
| Tests: token scope, refresh | **Done** |
| Tool token issue endpoint | **Done** (`POST /api/live-artifacts/tool-tokens`) |

---

## Data model

```sql
live_artifacts (
  id, project_id, name, source_template, inputs_json, content,
  content_type, sidecar_path, refresh_count, last_refreshed_at, …
)
live_artifact_refreshes (
  id, artifact_id, status, content_hash, error, created_at
)
```

Sidecar path (project-relative): `.neos-work/live-artifacts/<id>.html`  
Render: `{{key}}` substitution from inputs JSON.

---

## API

### User / UI (`Bearer` daemon auth)

| Method | Path |
|---|---|
| GET | `/api/live-artifacts?projectId=` |
| POST | `/api/live-artifacts` |
| GET | `/api/live-artifacts/:id?projectId=` |
| PATCH | `/api/live-artifacts/:id?projectId=` |
| DELETE | `/api/live-artifacts/:id?projectId=` |
| GET | `/api/live-artifacts/:id/preview?projectId=` |
| POST | `/api/live-artifacts/:id/refresh?projectId=` |
| GET | `/api/live-artifacts/:id/refreshes?projectId=` |
| POST | `/api/live-artifacts/tool-tokens` |

### Tool (`Bearer` tool token)

| Method | Path |
|---|---|
| POST | `/api/tools/live-artifacts/create` |
| GET | `/api/tools/live-artifacts/list` |
| POST | `/api/tools/live-artifacts/update` |
| POST | `/api/tools/live-artifacts/refresh` |

Tool tokens: in-memory, TTL default 1h, capability `live-artifacts`.  
Daemon auth middleware skips `/api/tools/*` so tools routes validate tokens themselves.

---

## Desktop

- Project workspace side tab **Live**
- Create (name + template + inputs JSON), list, preview strip, refresh, delete
- EngineClient: `listLiveArtifacts`, `createLiveArtifact`, `refreshLiveArtifact`,
  `deleteLiveArtifact`, `createProjectToolToken`
- i18n en/ko: `project.live*`

---

## Version

- monorepo **0.5.14 → 0.5.15**
- engine banner version **0.5.15**

---

## Verification

| Check | Result |
|---|---|
| tool-tokens unit | **4** passed |
| live-artifacts db | **3** passed |
| live-artifacts routes (+ tools) | **2** passed |
| i18n en/ko parity | **6** passed |
| server + desktop `tsc --noEmit` | green |

---

## File index

| 파일 | 상태 |
|---|---|
| `apps/server/src/lib/tool-tokens.ts` | 신규 |
| `apps/server/src/lib/tool-tokens.test.ts` | 신규 |
| `apps/server/src/db/live-artifacts.ts` | 신규 |
| `apps/server/src/db/live-artifacts.test.ts` | 신규 |
| `apps/server/src/db/schema.ts` | live tables |
| `apps/server/src/routes/live-artifacts.ts` | 신규 |
| `apps/server/src/routes/tools-live-artifacts.ts` | 신규 |
| `apps/server/src/routes/live-artifacts.test.ts` | 신규 |
| `apps/server/src/index.ts` | mount + tools auth skip |
| `packages/shared/src/types/project.ts` | LiveArtifact + Refresh |
| `apps/desktop/src/lib/engine.ts` | client methods |
| `apps/desktop/src/pages/ProjectWorkspace.tsx` | Live tab |
| `packages/ui/src/i18n/locales/{en,ko}/common.json` | keys |
| monorepo versions | **0.5.15** |

---

## Next

1. Deploy check-link polish (Task 10 / M4)  
2. Project-relative media output  
3. `apps/cli` headless surface (Task 11 / M5)  
4. Optional: persist tool tokens to disk / mcp-tokens layout  
