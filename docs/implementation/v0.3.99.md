# v0.3.99 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **plugin stage id/name hygiene + pipeline
error truncate**, **CLI spawn prompt/env bounds**, **agent-step sessionId/
stepIndex validation**, **orchestrator tool-call/result caps**, **memory
route name hygiene**, **session toolUseId bound**, **deploy workflowId/runId
hygiene**, **workflow route control-char names + ZIP import caps**,
**DESIGN.md id param safety**, **media generator control-chars**, **message
null-byte reject**, and **OpenAI baseUrl length/control-char**.

기준: v0.3.98

---

## Changes

### Task 1: Plugin runner / CLI

- Skip unsafe stage ids; cap stage name/outputKey
- pipeline.failed error ≤ 4k; resumeRun id hygiene
- CLI prompt ≤ 400k; drop unsafe workflowId/runId/serverUrl/authToken

### Task 2: Agent steps / orchestrator / session

- sessionId control-char/length; stepIndex ≤ 10_000
- Orchestrator: null-strip goal; outer error ≤ 4k; toolCalls ≤ 20;
  tool result ≤ 64k; step text ≤ 256k
- tool-confirm toolUseId length/control-char

### Task 3: Memory / deploy / workflow / design / media / skills

- Memory create/update name control-char/length
- Deploy workflowId/runId bounds
- Workflow create/update control-char names; ZIP workflow.json size +
  artifact path/count caps; import name sanitization
- Design system id path-safety on routes
- Media generator prompt/text control-char reject
- addMessage null-byte reject
- Skill source hygiene; OpenAI baseUrl max 2048 + control-char

### Task 4: Unit tests

- Agent-step invalid sessionId / overlong stepIndex
- CLI prompt max export
- Existing suites green for concurrent route/workflow coverage

### Task 5: Version

- monorepo **0.3.98 → 0.3.99**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/server/src/lib/plugin-runner.ts` | 수정 |
| `apps/server/src/lib/cli-agents.ts` | 수정 |
| `apps/server/src/db/agent-steps.ts` | 수정 |
| `packages/core/src/agent/orchestrator.ts` | 수정 |
| `apps/server/src/routes/session.ts` | 수정 |
| `apps/server/src/routes/memory.ts` | 수정 |
| `apps/server/src/routes/deploy.ts` | 수정 |
| `apps/server/src/routes/workflow.ts` | 수정 |
| `apps/server/src/routes/design-systems.ts` | 수정 |
| `apps/server/src/lib/media-generator.ts` | 수정 |
| `apps/server/src/db/sessions.ts` | 수정 |
| monorepo versions | 0.3.99 |
