# v0.3.162 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **editor selector/panel list-load errors** and
**NodeConfig throw surfaces** — BlockSelector / HarnessSelector / RunHistoryPanel
/ ArtifactPreview / WorkflowEditor load / Sessions message history, plus deploy
preflight and webhook fire/regenerate catch paths.

기준: v0.3.161

---

## Changes

### Task 1: Selectors

- BlockSelector: scrubbed `loadError` on listBlocks `!ok` / throw
- HarnessSelector: scrubbed `loadError` on listHarnesses `!ok` / throw

### Task 2: Panels / editor

- RunHistoryPanel: first-page list fail → scrubbed red empty state
- ArtifactPreview: listArtifacts fail → scrubbed list error (not “No artifacts”)
- WorkflowEditor: getWorkflow fail/throw → scrubbed load error + back link
- Sessions ChatArea: listMessages fail → scrubbed history error

### Task 3: NodeConfigPanel

- listDesignSystems: clear list on fail/throw
- deployPreflight: try/catch scrubbed alert on throw
- Webhook test fire / regenerate: catch throws → scrubbed flash; finally clears busy

### Task 4: Unit tests

- BlockSelector / HarnessSelector / RunHistoryPanel / ArtifactPreview load fails
- WorkflowEditor load scrub
- Sessions history scrub
- NodeConfig regenerate throw
- Residual core orchestrator / discovery coverage

### Task 5: Version

- monorepo **0.3.161 → 0.3.162**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/components/workflow/BlockSelector.tsx` | 수정 |
| `apps/desktop/src/components/workflow/HarnessSelector.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RunHistoryPanel.tsx` | 수정 |
| `apps/desktop/src/components/workflow/ArtifactPreview.tsx` | 수정 |
| `apps/desktop/src/components/workflow/NodeConfigPanel.tsx` | 수정 |
| `apps/desktop/src/pages/WorkflowEditor.tsx` | 수정 |
| `apps/desktop/src/pages/Sessions.tsx` | 수정 |
| related `*.test.tsx` | 수정 |
| residual package tests | 수정 |
| monorepo versions | 0.3.162 |
