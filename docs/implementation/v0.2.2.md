# v0.2.2 Implementation

## Summary
Dev DX 개선 및 워크플로우 실행 UX 향상을 위한 패치 릴리스.

## Changes

### Task 1: Dev Auth Token UI
- **`apps/desktop/src/pages/ModeSelection.tsx`** — Host/Client 연결 화면에 "Dev Auth Token" 비밀번호 입력 필드 추가. 입력 시 `sessionStorage.setItem('devAuthToken', token)` 저장
- **`apps/desktop/src/hooks/useEngine.tsx`** — `connect()` 내 토큰 우선순위: `sessionStorage.devAuthToken` → `getAuthToken()` (Tauri invoke)
- **`apps/desktop/src/pages/Settings.tsx`** — 설정 페이지 최하단 `DevToolsSection` 추가. 런타임에 devAuthToken 갱신 및 `client.setAuthToken()` 즉시 적용

### Task 2: Component Test Infrastructure
- **`apps/desktop/package.json`** — vitest, jsdom, @testing-library/react/jest-dom/user-event devDependencies 추가; `test` / `test:watch` 스크립트
- **`apps/desktop/vite.config.ts`** — `test.environment: 'jsdom'`, `globals: true`, `setupFiles` 설정
- **`apps/desktop/src/test-setup.ts`** — `@testing-library/jest-dom` import
- **`apps/desktop/src/components/workflow/WorkflowValidation.test.ts`** — 10 unit tests (validation rule 전체 커버)
- **`apps/desktop/src/components/workflow/BlockParamForm.test.tsx`** — 6 unit tests (render + onChange)

### Task 3: Trigger Runtime Input Injection
- **`packages/workflow-engine/src/executor.ts`** — `ExecutorOptions.triggerInputs?: Record<string, unknown>` 추가. trigger 노드 실행 시 `effectiveInputs = triggerInputs`로 교체
- **`packages/workflow-engine/src/executor.test.ts`** — `triggerInputs` 전달 테스트 케이스 추가
- **`apps/server/src/routes/workflow.ts`** — `POST /:id/run` body에서 `{ inputs }` 파싱 → executor에 `triggerInputs` 전달; `GET /:id/runs/:runId` 엔드포인트 신규 추가 (workflowId ownership 검증)
- **`apps/desktop/src/lib/engine.ts`** — `runWorkflow(id, onEvent, inputs?)` inputs 파라미터 추가; `getWorkflowRun(workflowId, runId)` 메서드 신규 추가
- **`apps/desktop/src/components/workflow/RunInputsDialog.tsx`** — 신규 모달 다이얼로그. JSON textarea로 trigger inputs 입력; parse 오류 표시; Cancel/Run 버튼
- **`apps/desktop/src/pages/WorkflowEditor.tsx`** — Run 버튼 클릭 시 `RunInputsDialog` 먼저 표시; `handleRun(inputs?)` 시그니처 변경; trigger 노드 `config.initialInputs`를 dialog 기본값으로 전달

### Task 4: Run History Node Output Detail
- **`apps/desktop/src/components/workflow/RunDetailPanel.tsx`** — 신규 패널. `client.getWorkflowRun()` 호출 후 노드별 status/output/error 표시
- **`apps/desktop/src/components/workflow/RunHistoryPanel.tsx`** — `selectedRunId` state 추가; 각 run 행 클릭 시 `RunDetailPanel` 토글 표시

## Test Results
```
packages/workflow-engine:  5 tests passed (graph.test.ts 2 + executor.test.ts 3)
apps/desktop:             16 tests passed (WorkflowValidation.test.ts 10 + BlockParamForm.test.tsx 6)
pnpm typecheck:           14/14 tasks successful (0 type errors)
```
