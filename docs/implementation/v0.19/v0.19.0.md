# v0.19.0 Implementation — Track A EngineOpsClient

## Summary

**Track A — Design systems / artifacts / routines / deploy client extract.**
Moves ops surface off `EngineClient` into `EngineOpsClient`.

Baseline: **0.18.0**  
Shipped with Track B in monorepo **0.19.0**.

---

## Task map

| Item | Status |
|---|---|
| `engine-ops.ts` (`EngineOpsClient`) | **Done** |
| Types (`DesignSystem`, `RoutineRun`) | **Done** |
| `EngineOpsClient extends EnginePluginsClient` | **Done** |
| `EngineClient extends EngineOpsClient` | **Done** |
| Public re-exports from `engine.js` | **Done** |

---

## Hierarchy

```text
EngineTransport
  → EngineProjectClient
    → EngineWorkflowClient
      → EngineSettingsClient
        → EngineMediaClient
          → EngineSessionsClient
            → EnginePluginsClient
              → EngineOpsClient   // NEW
                → EngineClient    // blocks, templates, memory
```

## Methods on `EngineOpsClient`

**Design systems:** `listDesignSystems`, `createDesignSystem`, `deleteDesignSystem`,
`getDesignSystemContent`, `getDesignSystemTokens`, `saveDesignSystemContent`

**Artifacts:** `listArtifacts`, `getArtifact`, `refreshArtifact`, `deleteArtifact`,
`updateArtifact`

**Routines:** `listRoutines`, `createRoutine`, `updateRoutine`, `deleteRoutine`,
`runRoutineNow`, `listRoutineRuns`, `crystallizeRoutineRun`

**Deploy:** `refreshDeployment`, `deployPreflight`, `checkDeployLink`

## Remaining on `EngineClient`

Blocks, templates, memory.

## Verify

```bash
pnpm --filter @neos-work/desktop exec vitest run src/lib/engine.test.ts
pnpm --filter @neos-work/desktop typecheck
```

## Related

- Track B: [`v0.19.1.md`](./v0.19.1.md)  
- Plan: [`PLAN_FOR_V0_19_0.md`](../../plans/PLAN_FOR_V0_19_0.md)  
