# v0.9.3 Implementation

## Summary

**M3 — Dual-surface policy + shared wire helpers.** Documents Desktop vs Web
product scope, shares preview-comment Zod parsers, validates web revisions /
comments through `@neos-work/shared`, and marks marketplace **desktop-only**
(badge on web Settings).

기준: v0.9.2  
**Plan:** [`docs/plans/PLAN_FOR_V0_9_0.md`](../../plans/PLAN_FOR_V0_9_0.md)  
**Policy:** [`docs/reference/dual-surface.md`](../../reference/dual-surface.md)

---

## Task map

| Item | Status |
|---|---|
| `docs/reference/dual-surface.md` | **Done** |
| Preview comment Zod + parse helpers | **Done** |
| Web `listRevisions` / `getRevision` shared parse | **Done** |
| Web `listPreviewComments` / `createPreviewComment` shared parse | **Done** |
| conventions.md dual-surface + helper table | **Done** |
| Q29 marketplace desktop-only + web badge | **Done** |
| Tests (shared + web) | **Done** |
| Version **0.9.3** | **Done** |

---

## Decisions locked

| ID | Choice |
|---|---|
| **Q25** | Web = Design Project loop only |
| **Q26** | Share Zod/types; no mega EngineClient merge |
| **Q29** | Full marketplace UI **desktop-only**; web badge |

---

## Wire helpers added

```ts
parsePreviewCommentListResponse
parsePreviewCommentDetailResponse
// + openApiWireFragments.PreviewComment
```

Web client now fail-closes on schema mismatch for:

- revision list/detail (`contentHash`)  
- preview comment list/create  

Live write path already used `parseProjectFileWriteResponse` (`hash`).

---

## UX

Web Settings → **Desktop-only surfaces** card:

- Plugins / marketplace  
- Workflow · Domain packs · Media · Sessions · Memory  

---

## Verify

```bash
pnpm --filter @neos-work/shared test
pnpm --filter @neos-work/web test
```

---

## Next (M4)

Migration note, inventory `v09Features` gates, train closeout **0.9.4**.
