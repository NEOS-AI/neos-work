# v0.3.173 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **submit-time control-char feedback** for MCP
add, DevTools token, Workflows create, WorkflowEditor rename, and RevisionPanel
label — no silent no-ops on invalid control characters.

기준: v0.3.172

---

## Changes

### Task 1: Settings — MCP create

- Control-char name → `Name contains invalid control characters`
- Control-char stdio command → `Command contains invalid control characters`
- Control-char HTTP URL → `URL contains invalid control characters`
- Still blocks `createMcpServer` API call

### Task 2: Settings — DevTools token

- Control-char bearer override → `Token contains invalid control characters`
- Still blocks sessionStorage / `setAuthToken`

### Task 3: Workflows create

- Control-char workflow name → `Name contains invalid control characters`
- Still blocks `createWorkflow`

### Task 4: WorkflowEditor rename

- Control-char rename commit → alert + exit edit mode (no API call)

### Task 5: RevisionPanel label

- Control-char label commit → `Label contains invalid control characters`
- Still blocks `updateRevisionLabel`

### Task 6: Unit tests

- Settings MCP / DevTools alert assertions
- Workflows create control-char alert
- WorkflowEditor rename control-char alert
- RevisionPanel control-char label alert
- **123 passed** across related suites

### Task 7: Version

- monorepo **0.3.172 → 0.3.173**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/pages/Settings.tsx` | 수정 |
| `apps/desktop/src/pages/Settings.test.tsx` | 수정 |
| `apps/desktop/src/pages/Workflows.tsx` | 수정 |
| `apps/desktop/src/pages/Workflows.test.tsx` | 수정 |
| `apps/desktop/src/pages/WorkflowEditor.tsx` | 수정 |
| `apps/desktop/src/pages/WorkflowEditor.test.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RevisionPanel.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RevisionPanel.test.tsx` | 수정 |
| monorepo versions | 0.3.173 |
