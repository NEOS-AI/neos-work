# v0.3.173 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **control-char rejection feedback** for MCP
server add fields, Dev Tools bearer token, Workflows create name, WorkflowEditor
inline rename, and RevisionPanel label edit — no silent no-ops on invalid input.

기준: v0.3.172

---

## Changes

### Task 1: Settings — MCP add form

- Name with control chars → alert `Name contains invalid control characters`
- Stdio command with control chars → alert `Command contains invalid control characters`
- HTTP URL with control chars → alert `URL contains invalid control characters`
- Still blocks `createMcpServer` API call

### Task 2: Settings — Dev Tools token

- Control-char override token → alert `Token contains invalid control characters`
- Never writes sessionStorage / `setAuthToken`

### Task 3: Workflows — create name

- Control-char name → alert `Name contains invalid control characters`
- Empty name still silent no-op (form validation)

### Task 4: WorkflowEditor / RevisionPanel

- Inline workflow rename control-char name → alert `Name contains invalid control characters`
- Revision label edit control-char → alert `Label contains invalid control characters`

### Task 5: Unit tests

- Settings: MCP name/command/url alerts; dev token alert
- Workflows: create control-char name alert
- **65 passed** across Settings + Workflows suites

### Task 6: Version

- monorepo **0.3.172 → 0.3.173**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/pages/Settings.tsx` | 수정 |
| `apps/desktop/src/pages/Settings.test.tsx` | 수정 |
| `apps/desktop/src/pages/Workflows.tsx` | 수정 |
| `apps/desktop/src/pages/Workflows.test.tsx` | 수정 |
| `apps/desktop/src/pages/WorkflowEditor.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RevisionPanel.tsx` | 수정 |
| `docs/implementation/v0.3.173.md` | 신규 |
| monorepo versions | 0.3.173 |
