# v0.11.3 Implementation

## Summary

**M3 — Workers rename · docs · inventory closeout.** Freezes the v0.11 train with
primary `/workers` route (alias `/harnesses`), migration, release note, and
inventory `v11Features` gates. Version **0.11.3**.

Baseline: **0.11.2**  
**Plan:** [`docs/plans/PLAN_FOR_V0_11_0.md`](../../plans/PLAN_FOR_V0_11_0.md) M3  
**Migration:** [`docs/migration/v0.11.0.md`](../../migration/v0.11.0.md)  
**Release:** [`docs/releases/v0.11.3.md`](../../releases/v0.11.3.md)

---

## Task map

| Item | Status |
|---|---|
| Desktop `/workers` primary + `/harnesses` alias | **Done** |
| Nav id `workers` + i18n | **Done** |
| `Workers` export + `Harnesses` alias | **Done** |
| Domain filter `workers` scope (+ harnesses fallback) | **Done** |
| Migration / release / this note | **Done** |
| Inventory `v11Features` | **Done** |
| Security / dual-surface touch | **Done** |
| Version **0.11.3** | **Done** |

---

## UI (Q37)

| Surface | Behavior |
|---|---|
| Sidebar | `nav.workers` → `/workers` |
| Routes | `workers` + `harnesses` both render same page |
| Component | `export function Workers`; `export const Harnesses = Workers` |
| i18n | Already product language “Workers”; added `nav.workers` |
| Prefs | `loadDomainFilter('workers')` with legacy harnesses key fallback |

---

## Inventory

`pnpm inventory:check` requires `catalogs.v11Features`:

| Key | Surface |
|---|---|
| `planV11` | `docs/plans/PLAN_FOR_V0_11_0.md` |
| `migrationV11` | `docs/migration/v0.11.0.md` |
| `releaseV11` | `docs/releases/v0.11.3.md` |
| `runSessionBind` | runs bind + hard-enforce fallbackRunId |
| `lockEnforceUx` | `packages/shared/src/collab-ux.ts` |
| `toolPathLockParity` | tools-files + files capability + mount |
| `workersUiRename` | App routes + Sidebar `/workers` + `Workers` export |
| `implM0`…`implM3` | `docs/implementation/v0.11/v0.11.{0–3}.md` |

---

## Verify

```bash
pnpm inventory:check
pnpm test:inventory
pnpm --filter @neos-work/desktop exec vitest run \
  src/pages/Harnesses.test.tsx \
  src/components/Sidebar.test.tsx \
  src/lib/domain-filter-prefs.test.tsx
```

---

## v0.11 train complete (M0–M3)

| M | Theme | Version |
|---|---|---|
| M0 | Run→session bind | 0.11.0 |
| M1 | Lock / enforce UX | 0.11.1 |
| M2 | Tool-path lock parity | 0.11.2 |
| M3 | Docs / inventory / Workers UI | **0.11.3** |
