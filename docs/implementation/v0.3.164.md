# v0.3.164 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **readApiResponse array reject + envelope
error scrub**, plus **NodeConfigPanel design-systems / webhook secret load
errors** and **ArtifactPreview content load error**.

기준: v0.3.163

---

## Changes

### Task 1: EngineClient safe parse harden

- `readApiResponse`: reject JSON arrays (`!Array.isArray`); scrub
  `envelope.error` via `scrubApiErrorMessage` when present
- `readHealthResponse`: reject array bodies the same way

### Task 2: NodeConfigPanel — design systems

- `listDesignSystems` failure → scrubbed red note under Design System select
- Empty list + clear options on failure (no silent empty dropdown)

### Task 3: NodeConfigPanel — webhook secret

- `getWebhookSecret` failure → scrubbed error in secret row (replaces
  endless “Loading secret…”)

### Task 4: ArtifactPreview — content

- `getArtifact` failure → scrubbed `contentError` in preview pane
- Separate from list errors / transient status toast

### Task 5: Unit tests

- Engine array body / envelope error scrub
- Design systems load fail scrub
- Webhook secret load fail scrub
- Artifact content load fail scrub
- Residual package coverage (discovery, finance)

### Task 6: Version

- monorepo **0.3.163 → 0.3.164**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/lib/engine.ts` | 수정 |
| `apps/desktop/src/lib/engine.test.ts` | 수정 |
| `apps/desktop/src/components/workflow/NodeConfigPanel.tsx` | 수정 |
| `apps/desktop/src/components/workflow/NodeConfigPanel.test.tsx` | 수정 |
| `apps/desktop/src/components/workflow/ArtifactPreview.tsx` | 수정 |
| `apps/desktop/src/components/workflow/ArtifactPreview.test.tsx` | 수정 |
| residual package tests | 수정 |
| monorepo versions | 0.3.164 |
