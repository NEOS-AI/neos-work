# v0.3.164 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **readApiResponse / readHealthResponse harden** —
reject JSON arrays (typeof object false-positive), scrub envelope `error` strings
at parse time so control chars never leave EngineClient.

기준: v0.3.163

---

## Changes

### Task 1: EngineClient safe parse

- `readApiResponse`: require plain object (`!Array.isArray`); scrub
  `envelope.error` via `scrubApiErrorMessage` when present
- `readHealthResponse`: reject array bodies the same way

### Task 2: Unit tests

- Array body → `{ ok: false }`
- Control-char envelope error scrubbed to single-line
- Health array body throws clean HTTP error

### Task 3: Version

- monorepo **0.3.163 → 0.3.164**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/lib/engine.ts` | 수정 |
| `apps/desktop/src/lib/engine.test.ts` | 수정 |
| monorepo versions | 0.3.164 |
