# v0.3.29 Implementation

## Summary

PLAN_FOR_V0_3_0 residual UX polish: **turbo test** monorepo task, **Workflows domain
filter prefs** (Task 10), **Media/Routines relative timestamps** (Tasks 2/7),
**Escape closes RevisionPanel** (Task 16), and **duplicate_edge_id** validation.

기준: v0.3.28

---

## Changes

### Task 1: Monorepo `pnpm test` via Turbo

- **`turbo.json`**: add `test` task (no build dependency — packages use vitest on sources)
- Root `pnpm test` dispatches package test scripts (desktop, server, packages)

### Task 2: Workflows domain filter persistence (PLAN Task 10)

- **`loadWorkflowListDomain` / `saveWorkflowListDomain`** in `workflow-list-prefs.ts`
- localStorage key: `neos-workflows-domain` (`all` | `finance` | `coding` | `general`)
- Workflows page restores last domain chip on mount

### Task 3: Media + Routines relative time (PLAN Tasks 2 / 7)

- Media list: newest-first by `createdAt`, relative time with absolute `title`
- Routines list/detail/run history: last/next/created/started use relative time

### Task 4: RevisionPanel Escape (PLAN Task 16)

- **Escape** closes history panel; when editing a label, Escape cancels edit first
- Shortcuts help documents **Esc** → Close History
- Unit test for Escape → `onClose`

### Task 5: Graph validation

- **`duplicate_edge_id`** error when two edges share the same id

### Task 6: Unit tests

- `workflow-list-prefs.test.ts` — domain round-trip / invalid
- `WorkflowValidation.test.ts` — duplicate_edge_id
- `RevisionPanel.test.tsx` — Escape closes

---

## File Index

| 파일 | 상태 |
|---|---|
| `turbo.json` | 수정 |
| `apps/desktop/src/lib/workflow-list-prefs.ts` | 수정 |
| `apps/desktop/src/lib/workflow-list-prefs.test.ts` | 수정 |
| `apps/desktop/src/pages/Workflows.tsx` | 수정 |
| `apps/desktop/src/pages/Media.tsx` | 수정 |
| `apps/desktop/src/pages/Routines.tsx` | 수정 |
| `apps/desktop/src/pages/WorkflowEditor.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RevisionPanel.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RevisionPanel.test.tsx` | 수정 |
| `apps/desktop/src/components/workflow/WorkflowValidation.ts` | 수정 |
| `apps/desktop/src/components/workflow/WorkflowValidation.test.ts` | 수정 |
| `apps/server/src/index.ts` | 버전 |
| `apps/server/src/routes/health.ts` | 버전 |
| monorepo `package.json` versions | 0.3.29 |
| `docs/implementation/v0.3.29.md` | 신규 |

---

## Version

- **0.3.28 → 0.3.29**

---

## Non-Goals

- Single-node-only artifact re-execution
- Video media / Marketplace
- Full OD §22 SSRF hardening
- Full OD Atom pipeline atoms
