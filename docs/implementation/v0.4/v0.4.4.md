# v0.4.4 Implementation

## Summary

Residual **PLAN_FOR_V0_4_0 Q2 / Task 8** polish: desktop create/list surfaces
emit **`primaryDomain` + `domainPackIds`**, research pack on workflow create
and list filters, and list filter prefers `primaryDomain` over `domain`.

기준: v0.4.3  
참고: `PLAN_FOR_V0_4_0` Tasks 1–10 complete at 0.4.0; 0.4.1–0.4.4 close
export/multi-pack/workers-UI/primaryDomain client gaps.

---

## Changes

### Task 1: Engine client workflow create/update

- `createWorkflow` accepts `primaryDomain` / `domainPackIds`
- When only `domain` is passed, body also sets `primaryDomain` (v2 shape)
- `updateWorkflow` accepts `domain` / `primaryDomain` / `domainPackIds`

### Task 2: Workflows list + create modal

- Create sends `primaryDomain` + `domainPackIds: [pack]`
- Create domain select includes **research**
- List domain chips: all / finance / coding / **research** / general
- Card badge uses `primaryDomain ?? domain`

### Task 3: Templates + list filter

- Template → workflow create uses `primaryDomain` + template `domainPackIds`
- `filterWorkflowList` matches chip against `primaryDomain` first
- `workflow-list-prefs` allows research domain filter persistence

### Task 4: Version

- monorepo **0.4.3 → 0.4.4**
- health / banner / web_search UA / MCP client version

---

## Verification

- Desktop: engine + list prefs/filter + Workflows + Templates — **105** tests passed

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/lib/engine.ts` | 수정 |
| `apps/desktop/src/lib/engine.test.ts` | 수정 |
| `apps/desktop/src/lib/workflow-list-prefs.ts` | 수정 |
| `apps/desktop/src/lib/workflow-list-prefs.test.ts` | 수정 |
| `apps/desktop/src/lib/workflow-list-filter.ts` | 수정 |
| `apps/desktop/src/lib/workflow-list-filter.test.ts` | 수정 |
| `apps/desktop/src/pages/Workflows.tsx` | 수정 |
| `apps/desktop/src/pages/Workflows.test.tsx` | 수정 |
| `apps/desktop/src/pages/Templates.tsx` | 수정 |
| monorepo versions | 0.4.4 |

---

## Note

v0.4 Tasks 1–10 complete at 0.4.0; residual client polish closed through 0.4.4.
Next plan: [`docs/plans/PLAN_FOR_V0_5_0.md`](../../plans/PLAN_FOR_V0_5_0.md)
(Open Design full parity + Design Editor).
