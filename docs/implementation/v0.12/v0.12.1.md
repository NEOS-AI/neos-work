# v0.12.1 Implementation

## Summary

**M1 — Further EngineClient slice (Workflows).** `engine.ts` remained &gt; 2k lines
after M0, so the Workflow / webhook / revision / deployment API moves to
`EngineWorkflowClient`. Public `engine.js` imports unchanged.

Baseline: **0.12.0**  
**Plan:** [`docs/plans/PLAN_FOR_V0_12_0.md`](../../plans/PLAN_FOR_V0_12_0.md) M1

---

## Task map

| Item | Status |
|---|---|
| Measure remaining line count | **Done** (~2.4k after M0) |
| Extract one more domain module | **Done** — workflows |
| Typecheck + `engine.test.ts` | **Done** |
| Version **0.12.1** | **Done** |

---

## Layout (after M1)

```text
EngineTransport          engine-transport.ts   (~240)
    ↓
EngineProjectClient      engine-project.ts     (~1,300)
    ↓
EngineWorkflowClient     engine-workflow.ts    (~640)
    ↓
EngineClient             engine.ts             (~1,790)
```

| Module | Domain |
|---|---|
| `engine-transport` | HTTP helpers, base URL/auth, path segments |
| `engine-project` | Design Projects, files, collab, runs |
| `engine-workflow` | Workflows, webhook, revisions, deployments + related types |
| `engine` | Sessions, MCP, media, plugins, workers, blocks, memory, … |

---

## Line counts

| File | ~Lines |
|---|---:|
| `engine-transport.ts` | 240 |
| `engine-project.ts` | 1,310 |
| `engine-workflow.ts` | 640 |
| `engine.ts` | 1,790 |
| **was** single `engine.ts` (pre-v0.12) | ~3,840 |

M1 exit criterion met: largest remaining file under ~2k; hierarchy is clear.

---

## Verify

```bash
pnpm --filter @neos-work/desktop exec tsc -p tsconfig.json --noEmit
pnpm --filter @neos-work/desktop exec vitest run src/lib/engine.test.ts
```

---

## Out of scope (M2–M3)

- Sticky SSE design note + multi-replica data-dir ops  
- Inventory `v12Features` closeout  
- Further slices (workers/memory) — optional later, not train blockers  
