# v0.3.97 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **blocks domain filter allow-list**, **MCP
OAuth callback param bounds**, **browser URL max/control-char reject**,
**filesystem path max length**, **coding test_runner command length + output
cap**, **executor progress SSE bounds**, **agent error truncate**, **safe
SERVER_URL control-char/length**, **DESIGN.md null-byte reject**, and
**Slack/Discord content control-char + channel length tests**.

기준: v0.3.96

---

## Changes

### Task 1: Blocks / MCP OAuth

- GET /api/blocks: only finance|coding|general filter; unknown → all
- OAuth callback: code ≤ 4k, state ≤ 512; reject control chars; cap error param

### Task 2: Browser / filesystem / coding

- isSafeBrowserUrl: max 2048; reject control chars
- safePath: relative path max 4096
- test_runner: command max 10k; argv max 50; code_eval output/error caps

### Task 3: Executor / agent / server-url / design

- node.progress chunk ≤ 32k, accumulated ≤ 256k
- AgentNode error truncate 4k
- safeServerUrl: control-char / length > 2048 → fallback
- updateDesignSystemContent: reject null bytes

### Task 4: Unit tests

- Blocks unknown domain → all
- Browser URL overlong/control-char
- FS overlong path
- Coding overlong command
- Server URL control-char/overlong
- Slack channel length + null content; Discord null content
- Design null-byte DESIGN.md

### Task 5: Version

- monorepo **0.3.96 → 0.3.97**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/server/src/routes/blocks.ts` | 수정 |
| `apps/server/src/routes/mcp.ts` | 수정 |
| `apps/server/src/lib/design-system-store.ts` | 수정 |
| `packages/browser-tool/src/tools.ts` | 수정 |
| `packages/core/src/tools/filesystem.ts` | 수정 |
| `packages/workflow-engine/src/blocks/coding/index.ts` | 수정 |
| `packages/workflow-engine/src/executor.ts` | 수정 |
| `packages/workflow-engine/src/nodes/agent.ts` | 수정 |
| `packages/workflow-engine/src/nodes/server-url.ts` | 수정 |
| monorepo versions | 0.3.97 |
