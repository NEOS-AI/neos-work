# v0.3.184 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: extend **`safeEntityId` gates** to remaining
id-based workflow panel APIs — artifact list/content/reload/rerun/rename/delete,
run history list/clear/export/delete, run detail load, webhook secret/fire/
regenerate, and routine schedule update.

기준: v0.3.183

---

## Changes

### Task 1: ArtifactPreview

- List load: gate `workflowId` / `runId` before `listArtifacts`
- Content load: gate `selectedId` before `getArtifact`
- Reload / rerun / rename / delete: gate `selectedId` before API
- Invalid id → status toast / content error (no API call)

### Task 2: RunHistoryPanel

- List runs: gate `workflowId` before `listWorkflowRuns`
- Clear completed / failed / cancelled: gate before confirm/API
- Export / delete run: gate `workflowId` + `run.id`

### Task 3: RunDetailPanel

- Load effect gates `workflowId` + `runId`; invalid → error banner, no API call

### Task 4: NodeConfigPanel webhook section

- Load secret: gate workflow id
- Invalid route id → red banner (no secret fetch)
- Test fire / regenerate: gate before API; flash message on invalid id
- Webhook URL built from safe id only

### Task 5: Routines schedule edit

- `handleSaveSchedule` gates `selectedId` before `updateRoutine`

### Task 6: Unit tests

- RunDetailPanel: control-char workflow/run id skips API + shows error
- ArtifactPreview delete dirty id (content + toast message)
- Residual LLM adapter / mock-adapter edge tests
- **117 + 59 related tests** green

### Task 7: Version

- monorepo **0.3.183 → 0.3.184**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/components/workflow/ArtifactPreview.tsx` | 수정 |
| `apps/desktop/src/components/workflow/ArtifactPreview.test.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RunHistoryPanel.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RunDetailPanel.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RunDetailPanel.test.tsx` | 수정 |
| `apps/desktop/src/components/workflow/NodeConfigPanel.tsx` | 수정 |
| `apps/desktop/src/pages/Routines.tsx` | 수정 |
| monorepo versions | 0.3.184 |
