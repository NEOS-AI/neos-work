# v0.3.28 Implementation

## Summary

PLAN_FOR_V0_3_0 residual UX polish: Dashboard **Recent deployments** (Task 8),
**relative timestamps** on Deployments / RevisionPanel / Run History / Workflows
cards, **duplicate_edge** graph validation, and **Workflows sort prefs**
persistence (Task 10).

기준: v0.3.27

---

## Changes

### Task 1: Dashboard recent deployments (PLAN Task 8)

- **`pickRecentDeployments`** — newest-first by `createdAt`, limit N
- Dashboard lists up to 5 recent deployments with provider, status, relative time
- Links into the related workflow (or Deployments list when no workflow)

### Task 2: Relative time expansion (list/history polish)

- Deployments table **When** column uses `formatRelativeTime` (absolute in `title`)
- RevisionPanel timestamps use relative time + absolute `title`
- Run History start time uses relative time + absolute `title`
- Workflows cards show relative `updatedAt` instead of locale date only

### Task 3: Workflows sort prefs (PLAN Task 10 / UX)

- **`loadWorkflowListSort` / `saveWorkflowListSort`** in `workflow-list-prefs.ts`
- localStorage key: `neos-workflows-sort` (`updated` | `name`)
- Workflows page restores last sort mode on mount

### Task 4: Duplicate edge validation

- **`duplicate_edge`** warning when multiple edges share the same source→target
- Unit coverage in `WorkflowValidation.test.ts`

### Task 5: Unit tests

- `recent-workflows.test.ts` — pickRecentDeployments order / empty limits
- `workflow-list-prefs.test.ts` — default updated, round-trip, invalid value

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/lib/recent-workflows.ts` | 수정 |
| `apps/desktop/src/lib/recent-workflows.test.ts` | 수정 |
| `apps/desktop/src/lib/workflow-list-prefs.ts` | 신규 |
| `apps/desktop/src/lib/workflow-list-prefs.test.ts` | 신규 |
| `apps/desktop/src/pages/Dashboard.tsx` | 수정 |
| `apps/desktop/src/pages/Deployments.tsx` | 수정 |
| `apps/desktop/src/pages/Workflows.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RevisionPanel.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RunHistoryPanel.tsx` | 수정 |
| `apps/desktop/src/components/workflow/WorkflowValidation.ts` | 수정 |
| `apps/desktop/src/components/workflow/WorkflowValidation.test.ts` | 수정 |
| `apps/server/src/index.ts` | 버전 |
| `apps/server/src/routes/health.ts` | 버전 |
| monorepo `package.json` versions | 0.3.28 |
| `docs/implementation/v0.3.28.md` | 신규 |

---

## Version

- **0.3.27 → 0.3.28**

---

## Non-Goals

- Single-node-only artifact re-execution
- Video media / Marketplace
- Full OD §22 SSRF hardening
- Full OD Atom pipeline atoms
