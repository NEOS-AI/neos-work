# v0.3.160 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **throw-path / busy-state cleanup** for
WorkflowEditor schedule create, Blocks/Harnesses delete+save, Sessions
create/delete/confirm, RevisionPanel restore/label/delete/load; plus load-error
surfaces for DesignSystemEditor, Plugins, and Settings CLI agents.

기준: v0.3.159

---

## Changes

### Task 1: WorkflowEditor schedule

- Create routine: try/catch/finally so `scheduleBusy` always clears
- Scrubbed alert on network throw; re-enable Create routine button

### Task 2: Blocks / Harnesses

- Blocks parent save: catch re-throw scrubbed error into modal
- Blocks delete: catch throw → scrubbed alert; row kept
- Harnesses delete: catch throw → scrubbed alert
- Harnesses modal save: `finally { setSaving(false) }` (no stuck Save)

### Task 3: Sessions

- Create / delete / confirmTool: catch throws → scrubbed alerts; modal stays

### Task 4: RevisionPanel

- listRevisions load: try/finally clears loading
- Restore: try/catch/finally clears restoring; scrubbed throw alert
- Label update / delete revision: catch throws → scrubbed alerts

### Task 5: Load-error residuals

- DesignSystemEditor: load error for missing DS / list fail / content fail
- Plugins: listPlugins `!ok` / throw → scrubbed load error
- Settings CLI agents: scrub API/network load errors

### Task 6: Unit tests

- WorkflowEditor schedule create throw
- Blocks / Harnesses delete throw
- Sessions create/delete throw
- RevisionPanel restore throw
- DesignSystemEditor / Plugins / Settings CLI residual load tests
- Residual package coverage (filesystem, graph, coding-spawn, shared barrel)

### Task 7: Version

- monorepo **0.3.159 → 0.3.160**
- health / server banner / web_search User-Agent / MCP client version

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/pages/WorkflowEditor.tsx` | 수정 |
| `apps/desktop/src/pages/WorkflowEditor.test.tsx` | 수정 |
| `apps/desktop/src/pages/Blocks.tsx` | 수정 |
| `apps/desktop/src/pages/Blocks.test.tsx` | 수정 |
| `apps/desktop/src/pages/Harnesses.tsx` | 수정 |
| `apps/desktop/src/pages/Harnesses.test.tsx` | 수정 |
| `apps/desktop/src/pages/Sessions.tsx` | 수정 |
| `apps/desktop/src/pages/Sessions.test.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RevisionPanel.tsx` | 수정 |
| `apps/desktop/src/components/workflow/RevisionPanel.test.tsx` | 수정 |
| `apps/desktop/src/pages/DesignSystemEditor.tsx` | 수정 |
| `apps/desktop/src/pages/Plugins.tsx` | 수정 |
| `apps/desktop/src/pages/Settings.tsx` | 수정 |
| monorepo versions | 0.3.160 |
