# v0.7.4 Implementation

## Summary

**M4 — Docs / inventory closeout.** Ships the v0.7 migration note, dedicated
**`v07Features`** inventory gates (plan, migration, M0–M3 code, impl notes),
README/Helm pointers, and freezes the v0.7 train checklist.

기준: v0.7.3  
계획: [`PLAN_FOR_V0_7_0.md`](../../plans/PLAN_FOR_V0_7_0.md) M4  
마이그레이션: [`docs/migration/v0.7.0.md`](../../migration/v0.7.0.md)

---

## Task map

| Plan item | Status |
|---|---|
| `docs/migration/v0.7.0.md` | **Done** |
| Inventory `v07Features` gate | **Done** |
| Impl notes M0–M4 present | **Done** |
| README / plan / Helm pointers | **Done** |
| Version **0.7.4** | **Done** |

---

## Inventory

`pnpm inventory:check` requires `catalogs.v07Features` in addition to
`v06Features`. Features:

| Key | Surface |
|---|---|
| `planV07` | `docs/plans/PLAN_FOR_V0_7_0.md` |
| `migrationV07` | `docs/migration/v0.7.0.md` |
| `canvasResize` | `applySizeDeltaToHtml` + CanvasOverlay |
| `collabBus` | `collab-bus.ts` + redis adapter |
| `selectionAwareness` | `setSessionSelection` + `selection.changed` + peers UI |
| `canvasMultiSelect` | `extraBboxes` + `highlight-multi` + toggle helpers |
| `implM0`…`implM4` | `docs/implementation/v0.7/v0.7.{0–4}.md` |

v0.7 keys were **moved out** of `v06Features` so each train has a clean gate.

---

## Verification

| Check | Result |
|---|---|
| `pnpm inventory:check` | pass |
| `pnpm test:inventory` | pass |
| design-editor / server collab suites | pass (prior M0–M3) |

---

## v0.7 train complete (M0–M4)

| M | Theme | Version |
|---|---|---|
| M0 | Canvas resize | 0.7.0 |
| M1 | Collab bus | 0.7.1 |
| M2 | Selection awareness | 0.7.2 |
| M3 | Canvas multi-select | 0.7.3 |
| M4 | Docs + inventory | **0.7.4** |

Deferred product work (not blocking closeout): multi-replica shared presence
store; full CRDT multi-caret (Q19).  
