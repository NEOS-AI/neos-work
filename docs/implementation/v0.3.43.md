# v0.3.43 Implementation

## Summary

PLAN_FOR_V0_3_0 residual polish: **empty harnessId validation** (Task 3 agents),
**NodeConfig media options DRY** (Task 7), **DeployNode project/provider hygiene**
(Task 8), and **Escape** clears Plugins/Deployments search.

기준: v0.3.42

---

## Changes

### Task 1: Agent harness validation

- **`missing_harness_id`** also fires when `harnessId` is `""` or whitespace
  (not only when missing / non-string)

### Task 2: NodeConfigPanel media options (Task 7 DRY)

- Size and voice `<select>` options come from shared
  `MEDIA_IMAGE_SIZES` / `MEDIA_VOICES` in `media-node-options.ts`
  (same allow-list as graph validation)

### Task 3: DeployNode runtime hygiene (Task 8)

- Trim `projectName` (fallback `neos-deploy` when blank)
- Unknown `provider` values fall back to `vercel`

### Task 4: Escape list search UX

- **Plugins**: Escape clears search when pipeline runner is closed
- **Deployments**: Escape clears search text

### Task 5: Unit tests

- DeployNode trim / provider fallback
- WorkflowValidation empty harnessId

---

## File Index

| 파일 | 상태 |
|---|---|
| `apps/desktop/src/components/workflow/WorkflowValidation.ts` | 수정 |
| `apps/desktop/src/components/workflow/WorkflowValidation.test.ts` | 수정 |
| `apps/desktop/src/components/workflow/NodeConfigPanel.tsx` | 수정 |
| `apps/desktop/src/pages/Plugins.tsx` | 수정 |
| `apps/desktop/src/pages/Deployments.tsx` | 수정 |
| `packages/workflow-engine/src/nodes/deploy.ts` | 수정 |
| `packages/workflow-engine/src/nodes/deploy.test.ts` | 수정 |
| monorepo versions | 0.3.43 |

Also includes uncommitted **v0.3.42** residual (template secrets, media-node-options
validation, v0.3.41 review fixes) if not yet released.
