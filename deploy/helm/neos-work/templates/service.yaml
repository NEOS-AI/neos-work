apiVersion: v1
kind: Service
metadata:
  name: {{ include "neos-work.fullname" . }}
  labels:
    {{- include "neos-work.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "neos-work.selectorLabels" . | nindent 4 }}
